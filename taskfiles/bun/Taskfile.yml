---
version: "3.5"

output:

  group:
    begin: "::group::{{.TASK}}"
    end: "::endgroup::"
    error_only: false

silent: true

includes:

  nix:
    taskfile: ../nix/Taskfile.yml

vars:

  BUN_NIX_INSTALLABLE: '{{.BUN_NIX_INSTALLABLE | default "nixpkgs#bun"}}'

tasks:
  default:
    run: once
    desc: Show available Bun automation tasks
    summary: Display the list of available Bun-related tasks defined in this Taskfile.
    cmds:
      - task --list

  install:
    run: once
    desc: Install Bun via the Nix profile
    summary: |
      Install Bun into the user Nix profile when it is not already
      available on PATH. Override the BUN_NIX_INSTALLABLE
      variable to pin a different package or version.
    cmds:
      - task: nix:install:profile
        vars:
          NIX_INSTALLABLE: "{{.BUN_NIX_INSTALLABLE}}"

  version:
    run: once
    desc: Show the active Bun version
    summary: |
      Print the version reported by the bun binary resolved from PATH.
      Auto-installs Bun if missing.
    deps:
      - task: install
    cmds:
      - task: _version:unix
      - task: _version:windows

  _version:unix:
    run: once
    internal: true
    desc: Show Bun version on macOS or Linux
    summary: Print the bun version on macOS or Linux.
    platforms: [linux, darwin]
    cmds:
      - bash -lc '{{.NIX_LOAD}}; bun --version'

  _version:windows:
    run: once
    internal: true
    desc: Show Bun version on Windows
    summary: Print the bun version on Windows.
    platforms: [windows]
    preconditions:
      - sh: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "if (Get-Command bun -ErrorAction SilentlyContinue) {
          exit 0 } else { exit 1 }"
        msg: Bun is not installed or not available in PATH.
    cmds:
      - bun --version
