---
version: "3.5"

output:
  group:
    begin: "::group::{{.TASK}}"
    end: "::endgroup::"
    error_only: false

silent: true

includes:
  uv:
    taskfile: ../uv/Taskfile.yml

vars:
  SQLFLUFF_LINT_SKIP_PATTERN: ""
  SQLFLUFF_INTERNAL_SKIP_CONFIG: ".taskotter-sqlfluff-skip.cfg"
  UV_LOAD: 'export PATH="$HOME/.local/bin:$PATH"'
  SQLFLUFF_VERSION: "4.2.2"

tasks:
  default:
    run: once
    desc: Show available sqlfluff automation tasks
    summary: Display the list of available sqlfluff-related tasks defined in this Taskfile.
    cmds:
      - task --list

  install:
    run: once
    desc: Install sqlfluff on the current operating system
    summary: |
      Install sqlfluff via uv tool install for the current platform.
      uv manages an isolated environment so sqlfluff never conflicts with
      project dependencies. Installs uv first if it is not already present.
    cmds:
      - task: _install:unix
      - task: _install:windows

  install:undo:
    run: once
    desc: Remove sqlfluff from the current operating system
    summary: |
      Remove sqlfluff via uv tool uninstall on all platforms.
      The uv-managed isolated environment is cleaned up automatically.
    prompt:
      - This will remove sqlfluff from this machine.
      - Continue?
    cmds:
      - task: _install:undo:unix
      - task: _install:undo:windows

  upgrade:
    run: once
    desc: Upgrade sqlfluff to the latest release
    summary: |
      Upgrade sqlfluff via uv tool upgrade on all platforms.
      Auto-installs sqlfluff if missing.
    cmds:
      - task: _upgrade:unix
      - task: _upgrade:windows

  version:
    run: once
    desc: Show the installed sqlfluff version
    summary: Print the active sqlfluff version via sqlfluff --version. Auto-installs sqlfluff if missing.
    deps:
      - task: install
    cmds:
      - bash -c '{{.UV_LOAD}}; sqlfluff --version'

  lint:
    run: once
    desc: Lint SQL files with sqlfluff (TARGETS=path)
    summary: |
      Run sqlfluff lint against TARGETS (default .). Pass CONFIG to use a
      custom config file via --config, DIALECT to override the SQL dialect,
      and EXTRA_ARGS for additional flags. Auto-installs sqlfluff if missing.
    deps:
      - task: install
    dir: "{{.USER_WORKING_DIR}}"
    vars:
      TARGETS: "."
      CONFIG: ""
      DIALECT: ""
      EXTRA_ARGS: ""
    cmds:
      - task: config:skip
      - >-
        bash -c '{{.UV_LOAD}};
        _skip="{{if .SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE}}{{.SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE}}
        {{- else}}{{.SQLFLUFF_LINT_SKIP_PATTERN}}{{end}}";
        if [ -n "$_skip" ]; then
        _files="$(mktemp)"; trap "rm -f $_files" EXIT;
        _targets="{{if .TARGETS_OVERRIDE}}{{.TARGETS_OVERRIDE}}{{else}}{{.TARGETS}}{{end}}";
        if [ -n "{{.CLI_ARGS}}" ]; then _targets="";
        for _argument in {{.CLI_ARGS}}; do
        if [ -e "$_argument" ]; then _targets="$_targets $_argument"; fi; done;
        [ -n "$_targets" ] || _targets="{{if .TARGETS_OVERRIDE}}{{.TARGETS_OVERRIDE}}{{else}}{{.TARGETS}}{{end}}"; fi;
        for _target in $_targets; do
        if [ -d "$_target" ]; then find "$_target" -type f
        \( -name "*.sql" -o -name "*.sql.j2" -o -name "*.dml" -o -name "*.ddl" \) -print0;
        elif [ -f "$_target" ]; then printf "%s\000" "$_target"; fi; done
        | task --silent --taskfile "{{.TASKFILE_DIR}}/../internal/skipfiles/Taskfile.yml"
        filter "SKIPFILES_PATTERN=$_skip" >"$_files";
        if [ ! -s "$_files" ]; then echo "No matching SQL files found"; exit 0; fi;
        xargs -0 sqlfluff lint
        {{if .CLI_ARGS}}{{.CLI_ARGS}}
        {{else}}
        {{if .DIALECT_OVERRIDE}}--dialect {{.DIALECT_OVERRIDE}}
        {{else if .DIALECT}}--dialect {{.DIALECT}} {{end}}
        {{if .EXTRA_ARGS_OVERRIDE}}{{.EXTRA_ARGS_OVERRIDE}}
        {{- else}}{{.EXTRA_ARGS}}{{end}}{{end}}
        --config "{{.SQLFLUFF_INTERNAL_SKIP_CONFIG}}" <"$_files"
        && echo "✅ sqlfluff passed" || { echo "❌ sqlfluff failed"; exit 1; }; exit; fi;
        sqlfluff lint
        {{if .CLI_ARGS}}{{.CLI_ARGS}}
        {{else}}
        {{if .DIALECT_OVERRIDE}}--dialect {{.DIALECT_OVERRIDE}}
        {{else if .DIALECT}}--dialect {{.DIALECT}} {{end}}
        {{if .TARGETS_OVERRIDE}}{{.TARGETS_OVERRIDE}}{{else}}{{.TARGETS}}{{end}}
        {{if .EXTRA_ARGS_OVERRIDE}}{{.EXTRA_ARGS_OVERRIDE}}
        {{- else}}{{.EXTRA_ARGS}}{{end}}{{end}}
        {{if or .SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE .SQLFLUFF_LINT_SKIP_PATTERN}}
        --config "{{.SQLFLUFF_INTERNAL_SKIP_CONFIG}}"
        {{else if .CONFIG_OVERRIDE}}--config "{{.CONFIG_OVERRIDE}}"
        {{else if .CONFIG}}--config "{{.CONFIG}}"{{end}}
        && echo "✅ sqlfluff passed" || { echo "❌ sqlfluff failed"; exit 1; }'

  lint:fix:
    run: once
    desc: Auto-fix SQL files with sqlfluff (TARGETS=path)
    summary: |
      Run sqlfluff fix against TARGETS (default .) to automatically fix
      lint violations. Pass CONFIG, DIALECT, and EXTRA_ARGS as needed.
      Auto-installs sqlfluff if missing.
    deps:
      - task: install
    dir: "{{.USER_WORKING_DIR}}"
    vars:
      TARGETS: "."
      CONFIG: ""
      DIALECT: ""
      EXTRA_ARGS: ""
    cmds:
      - task: config:skip
      - >-
        bash -c '{{.UV_LOAD}};
        _skip="{{if .SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE}}{{.SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE}}
        {{- else}}{{.SQLFLUFF_LINT_SKIP_PATTERN}}{{end}}";
        if [ -n "$_skip" ]; then
        _files="$(mktemp)"; trap "rm -f $_files" EXIT;
        _targets="{{if .TARGETS_OVERRIDE}}{{.TARGETS_OVERRIDE}}{{else}}{{.TARGETS}}{{end}}";
        if [ -n "{{.CLI_ARGS}}" ]; then _targets="";
        for _argument in {{.CLI_ARGS}}; do
        if [ -e "$_argument" ]; then _targets="$_targets $_argument"; fi; done;
        [ -n "$_targets" ] || _targets="{{if .TARGETS_OVERRIDE}}{{.TARGETS_OVERRIDE}}{{else}}{{.TARGETS}}{{end}}"; fi;
        for _target in $_targets; do
        if [ -d "$_target" ]; then find "$_target" -type f
        \( -name "*.sql" -o -name "*.sql.j2" -o -name "*.dml" -o -name "*.ddl" \) -print0;
        elif [ -f "$_target" ]; then printf "%s\000" "$_target"; fi; done
        | task --silent --taskfile "{{.TASKFILE_DIR}}/../internal/skipfiles/Taskfile.yml"
        filter "SKIPFILES_PATTERN=$_skip" >"$_files";
        if [ ! -s "$_files" ]; then echo "No matching SQL files found"; exit 0; fi;
        xargs -0 sqlfluff fix
        {{if .CLI_ARGS}}{{.CLI_ARGS}}
        {{else}}
        {{if .DIALECT_OVERRIDE}}--dialect {{.DIALECT_OVERRIDE}}
        {{else if .DIALECT}}--dialect {{.DIALECT}} {{end}}
        {{if .EXTRA_ARGS_OVERRIDE}}{{.EXTRA_ARGS_OVERRIDE}}
        {{- else}}{{.EXTRA_ARGS}}{{end}}{{end}}
        --config "{{.SQLFLUFF_INTERNAL_SKIP_CONFIG}}" <"$_files"
        && echo "✅ sqlfluff fixes applied" || exit 1; exit; fi;
        sqlfluff fix
        {{if .CLI_ARGS}}{{.CLI_ARGS}}
        {{else}}
        {{if .DIALECT_OVERRIDE}}--dialect {{.DIALECT_OVERRIDE}}
        {{else if .DIALECT}}--dialect {{.DIALECT}} {{end}}
        {{if .TARGETS_OVERRIDE}}{{.TARGETS_OVERRIDE}}{{else}}{{.TARGETS}}{{end}}
        {{if .EXTRA_ARGS_OVERRIDE}}{{.EXTRA_ARGS_OVERRIDE}}
        {{- else}}{{.EXTRA_ARGS}}{{end}}{{end}}
        {{if or .SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE .SQLFLUFF_LINT_SKIP_PATTERN}}
        --config "{{.SQLFLUFF_INTERNAL_SKIP_CONFIG}}"
        {{else if .CONFIG_OVERRIDE}}--config "{{.CONFIG_OVERRIDE}}"
        {{else if .CONFIG}}--config "{{.CONFIG}}"{{end}}
        && echo "✅ sqlfluff fixes applied"'

  ci:fix:
    run: once
    desc: Run lint:fix for CI fixing
    summary: |
      Run lint:fix to apply automatic lint fixes. Suitable for CI
      autofix workflows.
    cmds:
      - task: lint:fix

  parse:
    run: once
    desc: Parse SQL files and print the parse tree (TARGETS=path)
    summary: |
      Run sqlfluff parse against TARGETS (default .) and print the parse
      tree. Useful for debugging dialect issues or understanding how sqlfluff
      interprets a query. Pass CONFIG, DIALECT, and EXTRA_ARGS as needed.
      Auto-installs sqlfluff if missing.
    deps:
      - task: install
    dir: "{{.USER_WORKING_DIR}}"
    vars:
      TARGETS: "."
      CONFIG: ""
      DIALECT: ""
      EXTRA_ARGS: ""
    cmds:
      - task: config:skip
      - >-
        bash -c '{{.UV_LOAD}};
        _skip="{{if .SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE}}{{.SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE}}
        {{- else}}{{.SQLFLUFF_LINT_SKIP_PATTERN}}{{end}}";
        if [ -n "$_skip" ]; then
        _files="$(mktemp)"; trap "rm -f $_files" EXIT;
        _targets="{{if .TARGETS_OVERRIDE}}{{.TARGETS_OVERRIDE}}{{else}}{{.TARGETS}}{{end}}";
        if [ -n "{{.CLI_ARGS}}" ]; then _targets="";
        for _argument in {{.CLI_ARGS}}; do
        if [ -e "$_argument" ]; then _targets="$_targets $_argument"; fi; done;
        [ -n "$_targets" ] || _targets="{{if .TARGETS_OVERRIDE}}{{.TARGETS_OVERRIDE}}{{else}}{{.TARGETS}}{{end}}"; fi;
        for _target in $_targets; do
        if [ -d "$_target" ]; then find "$_target" -type f
        \( -name "*.sql" -o -name "*.sql.j2" -o -name "*.dml" -o -name "*.ddl" \) -print0;
        elif [ -f "$_target" ]; then printf "%s\000" "$_target"; fi; done
        | task --silent --taskfile "{{.TASKFILE_DIR}}/../internal/skipfiles/Taskfile.yml"
        filter "SKIPFILES_PATTERN=$_skip" >"$_files";
        if [ ! -s "$_files" ]; then echo "No matching SQL files found"; exit 0; fi;
        xargs -0 sqlfluff parse
        {{if .CLI_ARGS}}{{.CLI_ARGS}}
        {{else}}
        {{if .DIALECT_OVERRIDE}}--dialect {{.DIALECT_OVERRIDE}}
        {{else if .DIALECT}}--dialect {{.DIALECT}} {{end}}
        {{if .EXTRA_ARGS_OVERRIDE}}{{.EXTRA_ARGS_OVERRIDE}}
        {{- else}}{{.EXTRA_ARGS}}{{end}}{{end}}
        --config "{{.SQLFLUFF_INTERNAL_SKIP_CONFIG}}" <"$_files"; exit; fi;
        sqlfluff parse
        {{if .CLI_ARGS}}{{.CLI_ARGS}}
        {{else}}
        {{if .DIALECT_OVERRIDE}}--dialect {{.DIALECT_OVERRIDE}}
        {{else if .DIALECT}}--dialect {{.DIALECT}} {{end}}
        {{if .TARGETS_OVERRIDE}}{{.TARGETS_OVERRIDE}}{{else}}{{.TARGETS}}{{end}}
        {{if .EXTRA_ARGS_OVERRIDE}}{{.EXTRA_ARGS_OVERRIDE}}
        {{- else}}{{.EXTRA_ARGS}}{{end}}{{end}}
        {{if or .SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE .SQLFLUFF_LINT_SKIP_PATTERN}}
        --config "{{.SQLFLUFF_INTERNAL_SKIP_CONFIG}}"
        {{else if .CONFIG_OVERRIDE}}--config "{{.CONFIG_OVERRIDE}}"
        {{else if .CONFIG}}--config "{{.CONFIG}}"{{end}}'

  config:init:
    run: once
    dir: "{{.USER_WORKING_DIR}}"
    desc: Create a default .sqlfluff configuration file
    summary: |
      Write a sensible default .sqlfluff file in the current directory.
      Sets the dialect to ansi, max line length to 120, and 4-space
      indentation. Skipped if .sqlfluff already exists.
    status:
      - test -f .sqlfluff
    cmds:
      - "printf '[sqlfluff]\\n' > .sqlfluff"
      - "printf 'dialect = ansi\\n' >> .sqlfluff"
      - "printf 'max_line_length = 120\\n' >> .sqlfluff"
      - "printf '\\n' >> .sqlfluff"
      - "printf '[sqlfluff:indentation]\\n' >> .sqlfluff"
      - "printf 'indent_unit = space\\n' >> .sqlfluff"
      - "printf 'tab_space_size = 4\\n' >> .sqlfluff"
      - echo 'Created .sqlfluff with default configuration.'

  config:skip:
    desc: Write a sqlfluff config overlay that ignores the skip pattern
    summary: |
      Generate a sqlfluff config that merges the project config with
      SQLFLUFF_LINT_SKIP_PATTERN appended to [sqlfluff] ignore_paths. The lint,
      lint:fix, and parse tasks run this first. The overlay is rewritten on every run
      and is not removed afterwards, so add it to .gitignore. Running with no
      skip pattern set deletes it.
    dir: "{{.USER_WORKING_DIR}}"
    set: [errexit, nounset, pipefail]
    env:
      SQLFLUFF_SKIP_PATTERN: >-
        {{if .SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE}}{{.SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE}}
        {{- else}}{{.SQLFLUFF_LINT_SKIP_PATTERN}}{{end}}
      SQLFLUFF_SKIP_SOURCE_CONFIG: >-
        {{if .CONFIG_OVERRIDE}}{{.CONFIG_OVERRIDE}}{{else}}{{default "" .CONFIG}}{{end}}
      SQLFLUFF_SKIP_OUTPUT: "{{.SQLFLUFF_INTERNAL_SKIP_CONFIG}}"
    cmds:
      - cmd: |
          pattern="$SQLFLUFF_SKIP_PATTERN"
          source_config="$SQLFLUFF_SKIP_SOURCE_CONFIG"
          output="$SQLFLUFF_SKIP_OUTPUT"

          # Nothing to skip: drop the overlay a previous run left behind so the
          # next task falls back to the project config instead of a stale one.
          if [[ -z "$pattern" ]]; then
            rm -f "$output"
            exit 0
          fi

          pattern="${pattern//\\//}"
          if [[ -n "$source_config" && -f "$source_config" ]]; then
            awk -v pattern="$pattern" '
              BEGIN { in_sqlfluff = 0; section_found = 0; key_found = 0 }
              /^\[[^]]+\][[:space:]]*$/ {
                if (in_sqlfluff && !key_found) { print "ignore_paths = " pattern; key_found = 1 }
                in_sqlfluff = ($0 ~ /^\[sqlfluff\][[:space:]]*$/)
                if (in_sqlfluff) { section_found = 1 }
              }
              in_sqlfluff && /^[[:space:]]*ignore_paths[[:space:]]*=/ {
                value = $0
                sub(/^[^=]*=[[:space:]]*/, "", value)
                if (value == "") print "ignore_paths = " pattern
                else print "ignore_paths = " value "," pattern
                key_found = 1
                next
              }
              { print }
              END {
                if (in_sqlfluff && !key_found) print "ignore_paths = " pattern
                else if (!section_found) {
                  print ""
                  print "[sqlfluff]"
                  print "ignore_paths = " pattern
                }
              }
            ' "$source_config" >"$output"
          else
            printf '[sqlfluff]\nignore_paths = %s\n' "$pattern" >"$output"
          fi
        platforms: [linux, darwin]
      - cmd: |
          powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
          $ErrorActionPreference = 'Stop'
          $pattern = $env:SQLFLUFF_SKIP_PATTERN
          $sourceConfig = $env:SQLFLUFF_SKIP_SOURCE_CONFIG
          $output = $env:SQLFLUFF_SKIP_OUTPUT

          if ([string]::IsNullOrWhiteSpace($pattern)) {
              Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
              exit 0
          }

          $normalizedPattern = $pattern.Replace('\', '/')
          if (-not [string]::IsNullOrWhiteSpace($sourceConfig) -and
              (Test-Path -LiteralPath $sourceConfig -PathType Leaf)) {
              $lines = @(Get-Content -LiteralPath $sourceConfig)
              $result = [System.Collections.Generic.List[string]]::new()
              $inSqlfluff = $false
              $sectionFound = $false
              $keyFound = $false
              foreach ($line in $lines) {
                  if ($line -match '^\[[^]]+\]\s*$') {
                      if ($inSqlfluff -and -not $keyFound) {
                          $result.Add("ignore_paths = $normalizedPattern")
                          $keyFound = $true
                      }
                      $inSqlfluff = $line -match '^\[sqlfluff\]\s*$'
                      if ($inSqlfluff) { $sectionFound = $true }
                  }
                  if ($inSqlfluff -and $line -match '^\s*ignore_paths\s*=\s*(.*)$') {
                      $existing = $Matches[1]
                      $separator = if ([string]::IsNullOrWhiteSpace($existing)) { '' } else { ',' }
                      $result.Add("ignore_paths = $existing$separator$normalizedPattern")
                      $keyFound = $true
                  } else {
                      $result.Add($line)
                  }
              }
              if ($inSqlfluff -and -not $keyFound) {
                  $result.Add("ignore_paths = $normalizedPattern")
              } elseif (-not $sectionFound) {
                  $result.Add('')
                  $result.Add('[sqlfluff]')
                  $result.Add("ignore_paths = $normalizedPattern")
              }
              Set-Content -LiteralPath $output -Encoding UTF8 -Value $result
          } else {
              Set-Content -LiteralPath $output -Encoding UTF8 -Value "[sqlfluff]`nignore_paths = $normalizedPattern"
          }
          POWERSHELL
        platforms: [windows]

  _install:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    status:
      - >-
        bash -c '{{.UV_LOAD}};
        sqlfluff --version | grep -q "version {{.SQLFLUFF_VERSION}}$"'
    deps:
      - task: uv:install
    cmds:
      - echo 'Installing sqlfluff via uv...'
      - bash -c '{{.UV_LOAD}}; uv tool install --force "sqlfluff=={{.SQLFLUFF_VERSION}}"'
      - echo 'sqlfluff installed.'

  _install:windows:
    run: once
    internal: true
    platforms: [windows]
    status:
      - >-
        powershell -NoProfile -ExecutionPolicy Bypass -Command
        "if ((Get-Command sqlfluff -ErrorAction SilentlyContinue)
        -and ((sqlfluff --version) -match 'version {{.SQLFLUFF_VERSION}}$')) { exit 0 } else { exit 1 }"
    deps:
      - task: uv:install
    cmds:
      - echo Installing sqlfluff via uv...
      - uv tool install --force sqlfluff=={{.SQLFLUFF_VERSION}}
      - echo sqlfluff installed.

  _install:undo:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    status:
      - bash -c '{{.UV_LOAD}}; ! command -v sqlfluff >/dev/null 2>&1'
    cmds:
      - echo 'Removing sqlfluff...'
      - bash -c '{{.UV_LOAD}}; uv tool uninstall sqlfluff'
      - echo 'sqlfluff removed.'

  _install:undo:windows:
    run: once
    internal: true
    platforms: [windows]
    status:
      - >-
        powershell -NoProfile -ExecutionPolicy Bypass -Command
        "if (Get-Command sqlfluff -ErrorAction SilentlyContinue) { exit 1 } else { exit 0 }"
    cmds:
      - echo Removing sqlfluff via uv...
      - uv tool uninstall sqlfluff
      - echo sqlfluff removed.

  _upgrade:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    preconditions:
      - sh: bash -c '{{.UV_LOAD}}; command -v sqlfluff >/dev/null 2>&1'
        msg: sqlfluff is not installed. Run task install first.
    cmds:
      - echo 'Upgrading sqlfluff via uv...'
      - bash -c '{{.UV_LOAD}}; uv tool install --force "sqlfluff=={{.SQLFLUFF_VERSION}}"'
      - echo 'sqlfluff upgraded.'

  _upgrade:windows:
    run: once
    internal: true
    platforms: [windows]
    preconditions:
      - sh: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command
          "if (Get-Command sqlfluff -ErrorAction SilentlyContinue) { exit 0 } else { exit 1 }"
        msg: sqlfluff is not installed. Run task install first.
    cmds:
      - echo Upgrading sqlfluff via uv...
      - uv tool upgrade sqlfluff
      - echo sqlfluff upgraded.
