---
version: "3.5"

output:
  group:
    begin: "::group::{{.TASK}}"
    end: "::endgroup::"
    error_only: false

silent: true

includes:
  uv:
    taskfile: ../uv/Taskfile.yml

  skipfiles:
    taskfile: ../internal/skipfiles/Taskfile.yml
    internal: true

vars:
  ANSIBLE_LINT_SKIP_PATTERN: ""
  ANSIBLE_CONFIG: "ansible/ansible.cfg"
  UV_LOAD: 'export PATH="$HOME/.local/bin:$PATH"; export ANSIBLE_CONFIG="ansible/ansible.cfg"'
  ANSIBLE_PLAYBOOK: ""
  ANSIBLE_INVENTORY: ""
  ANSIBLE_PATTERN: "all"
  ANSIBLE_TARGETS: "."
  ANSIBLE_FILE: ""
  ANSIBLE_REQUIREMENTS: "requirements.yml"
  ANSIBLE_EXTRA_ARGS: ""
  ANSIBLE_VERSION: ""
  ANSIBLE_LINT_VERSION: ""

tasks:
  default:
    run: once
    desc: Show available Ansible automation tasks
    summary: Display the list of available Ansible-related tasks defined in this Taskfile.
    cmds:
      - task --list

  install:
    run: once
    desc: Install Ansible and ansible-lint on the current operating system
    summary: |
      Install ansible and ansible-lint via uv tool install on macOS and Linux.
      Pass ANSIBLE_VERSION and/or ANSIBLE_LINT_VERSION to pin specific
      releases instead of the latest. Ansible does not support Windows as a
      control node.
    deps:
      - task: uv:install
    cmds:
      - task: _install:unix

  install:undo:
    run: once
    desc: Remove Ansible and ansible-lint from the current operating system
    summary: |
      Uninstall ansible and ansible-lint via uv tool uninstall on macOS and Linux.
    prompt:
      - This will remove Ansible and ansible-lint from this machine.
      - Continue?
    cmds:
      - task: _install:undo:unix

  upgrade:
    run: once
    desc: Upgrade Ansible and ansible-lint to the latest release
    summary: |
      Upgrade ansible and ansible-lint via uv tool upgrade on macOS and Linux.
      Auto-installs if missing.
    deps:
      - task: install
    cmds:
      - task: _upgrade:unix

  version:
    run: once
    desc: Show the installed Ansible and ansible-lint versions
    summary: Print ansible --version and ansible-lint --version. Auto-installs if missing.
    deps:
      - task: install
    cmds:
      - task: _version:unix

  lint:
    run: once
    desc: Lint Ansible YAML files with ansible-lint (ANSIBLE_TARGETS=path)
    summary: |
      Run ansible-lint against ANSIBLE_TARGETS (default .). ansible-lint enforces
      Ansible best practices and YAML syntax checks.
      Pass ANSIBLE_EXTRA_ARGS for additional flags. Auto-installs if missing.
    deps:
      - task: install
    vars:
      ANSIBLE_TARGETS: "."
      ANSIBLE_EXTRA_ARGS: ""
    cmds:
      - task: _lint:unix
      - echo "✅ ansible-lint passed"

  lint:fix:
    run: once
    desc: Auto-fix Ansible YAML files with ansible-lint --fix (ANSIBLE_TARGETS=path)
    summary: |
      Run ansible-lint --fix against ANSIBLE_TARGETS (default .) to automatically
      resolve fixable lint violations. Pass ANSIBLE_EXTRA_ARGS for additional flags.
      Auto-installs if missing.
    deps:
      - task: install
    vars:
      ANSIBLE_TARGETS: "."
      ANSIBLE_EXTRA_ARGS: ""
    cmds:
      - task: _lint:fix:unix
      - echo "✅ ansible-lint fixes applied"

  ci:
    run: once
    desc: Run ansible-lint and ansible-playbook --syntax-check (ANSIBLE_PLAYBOOK=path)
    summary: |
      Run the lint task followed by syntax:check. Requires ANSIBLE_PLAYBOOK to
      be set so ansible-playbook --syntax-check can validate the playbook.
    cmds:
      - task: lint
      - task: syntax:check

  ci:fix:
    run: once
    desc: Run lint:fix for CI fixing
    summary: |
      Run lint:fix to apply automatic lint fixes. Suitable for CI
      autofix workflows.
    cmds:
      - task: lint:fix

  syntax:check:
    run: once
    desc: Check Ansible playbook syntax (ANSIBLE_PLAYBOOK=path)
    summary: |
      Run ansible-playbook --syntax-check to validate the playbook structure
      without executing any tasks. Pass INVENTORY to specify the inventory file.
      Auto-installs if missing.
    deps:
      - task: install
    requires:
      vars:
        - ANSIBLE_PLAYBOOK
    preconditions:
      - sh: test -n "{{.ANSIBLE_PLAYBOOK_OVERRIDE}}{{.ANSIBLE_PLAYBOOK}}"
        msg: PLAYBOOK is required. Pass ANSIBLE_PLAYBOOK=path/to/playbook.yml when running this task.
    cmds:
      - task: _syntax:check:unix

  run:
    run: once
    desc: Run an Ansible playbook (ANSIBLE_PLAYBOOK=path)
    summary: |
      Execute PLAYBOOK via ansible-playbook. Pass INVENTORY to specify the
      inventory file and ANSIBLE_EXTRA_ARGS for additional flags such as --check,
      --diff, or --tags. Auto-installs if missing.
    deps:
      - task: install
    requires:
      vars:
        - ANSIBLE_PLAYBOOK
    preconditions:
      - sh: test -n "{{.ANSIBLE_PLAYBOOK_OVERRIDE}}{{.ANSIBLE_PLAYBOOK}}"
        msg: PLAYBOOK is required. Pass ANSIBLE_PLAYBOOK=path/to/playbook.yml when running this task.
    cmds:
      - task: _run:unix

  ping:
    run: once
    desc: Test Ansible host connectivity (ANSIBLE_INVENTORY=path)
    summary: |
      Run the Ansible ping module against PATTERN (default all) using the
      specified INVENTORY. Confirms that Ansible can reach and authenticate
      to the target hosts. Auto-installs if missing.
    deps:
      - task: install
    requires:
      vars:
        - ANSIBLE_INVENTORY
    preconditions:
      - sh: test -n "{{.ANSIBLE_INVENTORY_OVERRIDE}}{{.ANSIBLE_INVENTORY}}"
        msg: INVENTORY is required. Pass ANSIBLE_INVENTORY=path/to/inventory when running this task.
    cmds:
      - task: _ping:unix

  list:hosts:
    run: once
    desc: List Ansible inventory hosts (ANSIBLE_INVENTORY=path)
    summary: |
      Print the hosts matching PATTERN (default all) from INVENTORY without
      executing any tasks. Useful for verifying inventory content.
      Auto-installs if missing.
    deps:
      - task: install
    requires:
      vars:
        - ANSIBLE_INVENTORY
    preconditions:
      - sh: test -n "{{.ANSIBLE_INVENTORY_OVERRIDE}}{{.ANSIBLE_INVENTORY}}"
        msg: INVENTORY is required. Pass ANSIBLE_INVENTORY=path/to/inventory when running this task.
    cmds:
      - task: _list:hosts:unix

  galaxy:install:
    run: once
    desc: Install Ansible roles and collections (ANSIBLE_REQUIREMENTS=path)
    summary: |
      Run ansible-galaxy install -r ANSIBLE_REQUIREMENTS (default requirements.yml)
      to install all declared roles and collections. Pass ANSIBLE_EXTRA_ARGS for
      additional flags. Auto-installs if missing.
    deps:
      - task: install
    cmds:
      - task: _galaxy:install:unix

  vault:encrypt:
    run: once
    desc: Encrypt a file with Ansible Vault (ANSIBLE_FILE=path)
    summary: |
      Run ansible-vault encrypt on ANSIBLE_FILE. You will be prompted for the vault
      password. Pass ANSIBLE_EXTRA_ARGS for additional flags such as --vault-id.
      Auto-installs if missing.
    deps:
      - task: install
    requires:
      vars:
        - ANSIBLE_FILE
    preconditions:
      - sh: test -n "{{.ANSIBLE_FILE}}"
        msg: ANSIBLE_FILE is required. Pass ANSIBLE_FILE=path/to/file when running this task.
    cmds:
      - task: _vault:unix
        vars:
          ARGS: 'encrypt "{{.ANSIBLE_FILE}}" {{.ANSIBLE_EXTRA_ARGS}}'

  vault:decrypt:
    run: once
    desc: Decrypt a file with Ansible Vault (ANSIBLE_FILE=path)
    summary: |
      Run ansible-vault decrypt on ANSIBLE_FILE. You will be prompted for the vault
      password. Pass ANSIBLE_EXTRA_ARGS for additional flags such as --vault-id.
      Auto-installs if missing.
    prompt:
      - This will decrypt and overwrite ANSIBLE_FILE with plaintext.
      - Continue?
    deps:
      - task: install
    requires:
      vars:
        - ANSIBLE_FILE
    preconditions:
      - sh: test -n "{{.ANSIBLE_FILE}}"
        msg: ANSIBLE_FILE is required. Pass ANSIBLE_FILE=path/to/file when running this task.
    cmds:
      - task: _vault:unix
        vars:
          ARGS: 'decrypt "{{.ANSIBLE_FILE}}" {{.ANSIBLE_EXTRA_ARGS}}'

  _install:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    status:
      - >-
        bash -lc '{{.UV_LOAD}}; command -v ansible >/dev/null 2>&1
        && { [ -z "{{.ANSIBLE_VERSION}}" ]
        || uv tool list | grep -Eq "^ansible v?{{.ANSIBLE_VERSION}}( |$)"; }'
      - >-
        bash -lc '{{.UV_LOAD}}; command -v ansible-lint >/dev/null 2>&1
        && { [ -z "{{.ANSIBLE_LINT_VERSION}}" ]
        || uv tool list | grep -Eq "^ansible-lint v?{{.ANSIBLE_LINT_VERSION}}( |$)"; }'
    cmds:
      - echo 'Installing Ansible via uv...'
      - >-
        bash -lc '{{.UV_LOAD}}; uv tool install --force
        "ansible{{if .ANSIBLE_VERSION}}=={{.ANSIBLE_VERSION}}{{end}}"'
      - >-
        bash -lc '{{.UV_LOAD}}; uv tool install --force
        "ansible-lint{{if .ANSIBLE_LINT_VERSION}}=={{.ANSIBLE_LINT_VERSION}}{{end}}"'
      - echo 'Ansible and ansible-lint installed.'

  _install:undo:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    status:
      - bash -lc '{{.UV_LOAD}}; ! command -v ansible >/dev/null 2>&1'
      - bash -lc '{{.UV_LOAD}}; ! command -v ansible-lint >/dev/null 2>&1'
    cmds:
      - echo 'Removing Ansible...'
      - bash -lc '{{.UV_LOAD}}; uv tool uninstall ansible'
      - bash -lc '{{.UV_LOAD}}; uv tool uninstall ansible-lint'
      - echo 'Ansible and ansible-lint removed.'

  _upgrade:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    preconditions:
      - sh: bash -lc '{{.UV_LOAD}}; command -v ansible >/dev/null 2>&1'
        msg: Ansible is not installed. Run task install first.
    cmds:
      - echo 'Upgrading Ansible...'
      - >-
        bash -lc '{{.UV_LOAD}}; uv tool install --force
        "ansible{{if .ANSIBLE_VERSION}}=={{.ANSIBLE_VERSION}}{{end}}"'
      - >-
        bash -lc '{{.UV_LOAD}}; uv tool install --force
        "ansible-lint{{if .ANSIBLE_LINT_VERSION}}=={{.ANSIBLE_LINT_VERSION}}{{end}}"'
      - echo 'Ansible and ansible-lint upgraded.'

  _version:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    preconditions:
      - sh: bash -lc '{{.UV_LOAD}}; command -v ansible >/dev/null 2>&1'
        msg: Ansible is not installed. Run task install first.
    cmds:
      - bash -lc '{{.UV_LOAD}}; ansible --version'
      - bash -lc '{{.UV_LOAD}}; ansible-lint --version'

  _lint:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    cmds:
      - task: _lint:run:unix
        vars:
          ANSIBLE_LINT_FIX_FLAG: ""

  _lint:fix:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    cmds:
      - task: _lint:run:unix
        vars:
          ANSIBLE_LINT_FIX_FLAG: "--fix"

  _lint:run:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    cmds:
      - |
        {{.UV_LOAD}}
        targets='{{if .ANSIBLE_TARGETS_OVERRIDE}}{{.ANSIBLE_TARGETS_OVERRIDE}}{{else if
        .ANSIBLE_TARGETS}}{{.ANSIBLE_TARGETS}}{{else}}.{{end}}'
        skip_pattern='{{if .ANSIBLE_LINT_SKIP_PATTERN_OVERRIDE}}{{.ANSIBLE_LINT_SKIP_PATTERN_OVERRIDE}}
        {{- else}}{{.ANSIBLE_LINT_SKIP_PATTERN}}{{end}}'
        if [ -z "$skip_pattern" ]; then
          ansible-lint {{.ANSIBLE_LINT_FIX_FLAG}} $targets \
            {{if .ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{.ANSIBLE_EXTRA_ARGS_OVERRIDE}}
            {{- else}}{{.ANSIBLE_EXTRA_ARGS}}{{end}}
          exit
        fi
        matched_files="$(mktemp)"
        trap 'rm -f "$matched_files"' EXIT
        for target in $targets; do
          if [ -d "$target" ]; then
            find "$target" -type f \( -name '*.yml' -o -name '*.yaml' \) -print0
          elif [ -f "$target" ]; then
            printf '%s\000' "$target"
          fi
        done | task --silent --taskfile "{{.TASKFILE_DIR}}/../internal/skipfiles/Taskfile.yml" \
          filter "SKIPFILES_PATTERN=$skip_pattern" SKIPFILES_MATCHING=true |
          tr '\0' '\n' >"$matched_files"
        set --
        while IFS= read -r path; do
          set -- "$@" --exclude "$path"
        done <"$matched_files"
        ansible-lint {{.ANSIBLE_LINT_FIX_FLAG}} "$@" $targets \
          {{if .ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{.ANSIBLE_EXTRA_ARGS_OVERRIDE}}
          {{- else}}{{.ANSIBLE_EXTRA_ARGS}}{{end}}

  _syntax:check:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    preconditions:
      - sh: bash -lc '{{.UV_LOAD}}; command -v ansible-playbook >/dev/null 2>&1'
        msg: ansible-playbook is not installed. Run task install first.
    cmds:
      - |
        {{.UV_LOAD}}
        playbook='{{if .ANSIBLE_PLAYBOOK_OVERRIDE}}{{.ANSIBLE_PLAYBOOK_OVERRIDE}}{{else}}{{.ANSIBLE_PLAYBOOK}}{{end}}'
        skip_pattern='{{if .ANSIBLE_LINT_SKIP_PATTERN_OVERRIDE}}{{.ANSIBLE_LINT_SKIP_PATTERN_OVERRIDE}}
        {{- else}}{{.ANSIBLE_LINT_SKIP_PATTERN}}{{end}}'
        if [ -n "$skip_pattern" ] && [ -z "$(
          printf '%s\000' "$playbook" |
            task --silent --taskfile "{{.TASKFILE_DIR}}/../internal/skipfiles/Taskfile.yml" \
              filter "SKIPFILES_PATTERN=$skip_pattern" |
            tr -d '\0'
        )" ]; then
          echo 'Ansible syntax check skipped'
          exit 0
        fi
        ansible-playbook --syntax-check "$playbook" \
          {{if .ANSIBLE_INVENTORY_OVERRIDE}}-i "{{.ANSIBLE_INVENTORY_OVERRIDE}}"
          {{- else if .ANSIBLE_INVENTORY}}-i "{{.ANSIBLE_INVENTORY}}"{{end}} \
          {{if .ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{.ANSIBLE_EXTRA_ARGS_OVERRIDE}}
          {{- else}}{{.ANSIBLE_EXTRA_ARGS}}{{end}} {{.CLI_ARGS}}

  _run:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    preconditions:
      - sh: bash -lc '{{.UV_LOAD}}; command -v ansible-playbook >/dev/null 2>&1'
        msg: ansible-playbook is not installed. Run task install first.
    cmds:
      - >-
        bash -lc '{{.UV_LOAD}};
        ansible-playbook
        "{{if .ANSIBLE_PLAYBOOK_OVERRIDE}}{{.ANSIBLE_PLAYBOOK_OVERRIDE}}{{else}}{{.ANSIBLE_PLAYBOOK}}{{end}}"
        {{if .ANSIBLE_INVENTORY_OVERRIDE}}-i "{{.ANSIBLE_INVENTORY_OVERRIDE}}"
        {{else if .ANSIBLE_INVENTORY}}-i "{{.ANSIBLE_INVENTORY}}" {{end}}
        {{if .ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{.ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{else}}{{.ANSIBLE_EXTRA_ARGS}}{{end}}'

  _ping:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    preconditions:
      - sh: bash -lc '{{.UV_LOAD}}; command -v ansible >/dev/null 2>&1'
        msg: Ansible is not installed. Run task install first.
    cmds:
      - >-
        bash -lc '{{.UV_LOAD}};
        ansible "{{.ANSIBLE_PATTERN}}" -m ping
        {{if .ANSIBLE_INVENTORY_OVERRIDE}}-i "{{.ANSIBLE_INVENTORY_OVERRIDE}}"
        {{else if .ANSIBLE_INVENTORY}}-i "{{.ANSIBLE_INVENTORY}}" {{end}}
        {{if .ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{.ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{else}}{{.ANSIBLE_EXTRA_ARGS}}{{end}}'

  _list:hosts:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    preconditions:
      - sh: bash -lc '{{.UV_LOAD}}; command -v ansible >/dev/null 2>&1'
        msg: Ansible is not installed. Run task install first.
    cmds:
      - >-
        bash -lc '{{.UV_LOAD}};
        ansible "{{.ANSIBLE_PATTERN}}" --list-hosts
        {{if .ANSIBLE_INVENTORY_OVERRIDE}}-i "{{.ANSIBLE_INVENTORY_OVERRIDE}}"
        {{else if .ANSIBLE_INVENTORY}}-i "{{.ANSIBLE_INVENTORY}}" {{end}}
        {{if .ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{.ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{else}}{{.ANSIBLE_EXTRA_ARGS}}{{end}}'

  _galaxy:install:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    preconditions:
      - sh: bash -lc '{{.UV_LOAD}}; command -v ansible-galaxy >/dev/null 2>&1'
        msg: ansible-galaxy is not installed. Run task install first.
    cmds:
      - >-
        bash -lc '{{.UV_LOAD}};
        ansible-galaxy install -r "{{.ANSIBLE_REQUIREMENTS}}"
        {{if .ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{.ANSIBLE_EXTRA_ARGS_OVERRIDE}}{{else}}{{.ANSIBLE_EXTRA_ARGS}}{{end}}'

  _vault:unix:
    run: once
    internal: true
    platforms: [linux, darwin]
    set: [errexit, nounset, pipefail]
    requires:
      vars: [ARGS]
    preconditions:
      - sh: bash -lc '{{.UV_LOAD}}; command -v ansible-vault >/dev/null 2>&1'
        msg: ansible-vault is not installed. Run task install first.
    cmds:
      - bash -lc '{{.UV_LOAD}}; ansible-vault {{.ARGS}}'
