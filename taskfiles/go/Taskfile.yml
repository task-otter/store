---
version: "3.5"
output:
  group:
    begin: "::group::{{.TASK}}"
    end: "::endgroup::"
    error_only: false
silent: true
includes:
  skipfiles:
    taskfile: ../internal/skipfiles/Taskfile.yml
    internal: true
vars:
  INSTALL_DIR_UNIX: "/usr/local"
  GO_ROOT_UNIX: "{{.INSTALL_DIR_UNIX}}/go"
  GO_BIN_UNIX: "{{.GO_ROOT_UNIX}}/bin"
  GO_CMD_UNIX: "{{.GO_BIN_UNIX}}/go"
  GO_VERSION_URL: https://go.dev/VERSION?m=text
  GO_DOWNLOAD_BASE_URL: https://go.dev/dl
  GO_VERSION: ""
  GOLANGCI_LINT_VERSION: ""
  GOVULNCHECK_VERSION: ""
  GOSEC_VERSION: ""
  GO_FMT_SKIP_PATTERN: ""
  GO_LINT_SKIP_PATTERN: ""
  GO_JUNIT_REPORT: ""
  # Written next to the project's golangci-lint config so relative paths inside
  # the merged copy keep resolving. Overwritten on every run, never deleted.
  GO_INTERNAL_SKIP_CONFIG: ".golangci-taskotter-skip.yml"
  GO_COVER_PROFILE: ""
  GO_FUZZTIME: ""
  # Formatter selection shared by every golangci-lint fmt invocation (both the
  # skip-pattern and plain paths, on Unix and Windows). Kept in one place so the
  # four call sites cannot drift apart.
  GO_FMT_FORMATTER_FLAGS: >-
    -E gci -E gofmt -E gofumpt -E goimports -E golines -E swaggo
  GLOBAL_GO_BIN:
    sh: |
      gobin="${GOBIN:-}"
      if [ -z "$gobin" ] && command -v go >/dev/null 2>&1; then
        gobin="$(go env GOBIN)"
      fi
      if [ -z "$gobin" ]; then
        gopath="${GOPATH:-}"
        if [ -z "$gopath" ] && command -v go >/dev/null 2>&1; then
          gopath="$(go env GOPATH)"
        fi
        gobin="${gopath:-$HOME/go}/bin"
      fi
      printf '%s' "$gobin"
tasks:
  default:
    run: once
    desc: Show available Go automation tasks
    summary: Display the list of available Go-related tasks defined in this Taskfile.
    cmds:
      - task --list
  install:
    run: once
    desc: Install Go on the current operating system
    summary: |
      Install GO_VERSION for the current platform, defaulting to the latest
      stable release. macOS uses Homebrew for latest or the official package
      for an explicit version. Linux uses the official tarball and Windows
      uses the official MSI.
    cmds:
      - task: _install:macos:brew
      - task: _install:macos:pkg
      - task: _install:linux:tarball
      - task: _install:windows:msi
      - task: _shell:setup:linux
  install:undo:
    run: once
    desc: Remove Go from the current operating system
    summary: |
      Remove the Go toolchain for the current platform.
      macOS uses Homebrew if Go was installed that way, otherwise removes
      /usr/local/go directly (the official package installer's location).
      Linux removes INSTALL_DIR_UNIX/go (default /usr/local/go).
      Windows uses the Programs and Features uninstaller via PowerShell.
    prompt:
      - This will remove Go from this machine.
      - Continue?
    cmds:
      - task: _install:undo:macos
      - task: _install:undo:linux
      - task: _install:undo:windows
  install:golangci-lint:
    run: once
    desc: Install golangci-lint globally
    summary: |
      Install GOLANGCI_LINT_VERSION, defaulting to latest, into the global Go
      bin.
    deps:
      - task: install
    cmds:
      - cmd: |
          mkdir -p "{{.GLOBAL_GO_BIN}}"
          GOBIN="{{.GLOBAL_GO_BIN}}" go install \
            github.com/golangci/golangci-lint/v2/cmd/golangci-lint@{{default "latest" .GOLANGCI_LINT_VERSION}}
        platforms: [linux, darwin]
      - cmd: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "$ErrorActionPreference='Stop'; $globalGoBin =
          '{{.GLOBAL_GO_BIN}}'; New-Item -ItemType Directory -Force -Path $globalGoBin | Out-Null; $env:GOBIN =
          $globalGoBin; $installVersion = '{{default "latest" .GOLANGCI_LINT_VERSION}}'; go install
          github.com/golangci/golangci-lint/v2/cmd/golangci-lint@$installVersion"
        platforms: [windows]
    status:
      - test -x "{{.GLOBAL_GO_BIN}}/golangci-lint"
      - >-
        { test -z "{{.GOLANGCI_LINT_VERSION}}" || test "{{.GOLANGCI_LINT_VERSION}}" = "latest" ||
        "{{.GLOBAL_GO_BIN}}/golangci-lint" version --short | awk -v want="{{.GOLANGCI_LINT_VERSION}}" '{ got = $1;
        sub(/^v/, "", got); sub(/^v/, "", want); sub(/[-+].*$/, "", got); if (got == want) { found = 1 } } END { exit
        !found }'; }
  install:govulncheck:
    run: once
    desc: Install govulncheck globally
    summary: Install GOVULNCHECK_VERSION, defaulting to latest, into the global Go bin.
    deps:
      - task: install
    cmds:
      - >-
        GOBIN="{{.GLOBAL_GO_BIN}}" go install golang.org/x/vuln/cmd/govulncheck@{{default "latest"
        .GOVULNCHECK_VERSION}}
    status:
      - >-
        test -x "{{.GLOBAL_GO_BIN}}/govulncheck" && { test -z "{{.GOVULNCHECK_VERSION}}" || test
        "{{.GOVULNCHECK_VERSION}}" = "latest" || go version -m "{{.GLOBAL_GO_BIN}}/govulncheck" | awk -v
        want="{{.GOVULNCHECK_VERSION}}" '$1 == "mod" && $3 == want { found = 1 } END { exit !found }'; }
  install:gosec:
    run: once
    desc: Install gosec globally
    summary: Install GOSEC_VERSION, defaulting to latest, into the global Go bin.
    deps:
      - task: install
    cmds:
      - >-
        GOBIN="{{.GLOBAL_GO_BIN}}" go install github.com/securego/gosec/v2/cmd/gosec@{{default "latest" .GOSEC_VERSION}}
    status:
      - >-
        test -x "{{.GLOBAL_GO_BIN}}/gosec" && { test -z "{{.GOSEC_VERSION}}" || test "{{.GOSEC_VERSION}}" = "latest" ||
        go version -m "{{.GLOBAL_GO_BIN}}/gosec" | awk -v want="{{.GOSEC_VERSION}}" '$1 == "mod" && $3 == want { found =
        1 } END { exit !found }'; }
  install:go-junit-report:
    run: once
    desc: Install go-junit-report globally
    summary: Install go-junit-report v2.1.0 into the global Go bin.
    deps:
      - task: install
    cmds:
      - GOBIN="{{.GLOBAL_GO_BIN}}" go install github.com/jstemmer/go-junit-report/v2@v2.1.0
    status:
      - >-
        test -x "{{.GLOBAL_GO_BIN}}/go-junit-report" && go version -m "{{.GLOBAL_GO_BIN}}/go-junit-report" | awk -v
        want="v2.1.0" '$1 == "mod" && $3 == want { found = 1 } END { exit !found }'
  fmt:
    run: once
    desc: Format Go files with golangci-lint formatters
    summary: |
      Rewrite Go files under the current directory with golangci-lint's Go
      formatters. Set GO_FMT_SKIP_PATTERN to a shell-style path glob for files
      to skip. Pass CLI arguments after -- to override the default target.
    cmds:
      - task: golangci-lint:fmt
  fmt:check:
    run: once
    desc: Check Go file formatting with golangci-lint formatters
    summary: |
      Check Go files under the current directory with golangci-lint's Go
      formatters without modifying them. Pass CLI arguments after -- to
      override the default target. Set GO_FMT_SKIP_PATTERN to a shell-style
      path glob for files to skip. Use fmt to apply fixes.
    deps:
      - task: golangci-lint:fmt:check
  lint:
    run: once
    desc: Run all Go lint and security checks
    summary: |
      Run golangci-lint, golangci-lint formatters, govulncheck, and gosec.
      Set GO_LINT_SKIP_PATTERN to a shell-style path glob for files that lint
      and security analysis should skip.
    deps:
      - task: golangci-lint:lint
      - task: golangci-lint:fmt:check
      - task: govulncheck:lint
      - task: gosec:lint
  lint:fix:
    run: once
    desc: Auto-fix Go lint and formatting issues
    summary: |
      Run golangci-lint with --fix, then format Go files with golangci-lint
      formatters. Set GO_LINT_SKIP_PATTERN to a shell-style path glob for files
      that golangci-lint must not modify. Pass CLI arguments after -- to
      override the default target.
    cmds:
      - task: golangci-lint:lint:fix
      - task: fmt
  golangci-lint:lint:
    run: once
    desc: Lint Go packages with golangci-lint
    summary: |
      Run golangci-lint against all Go packages by default. Set
      GO_LINT_SKIP_PATTERN to a shell-style path glob for files to skip. Pass
      CLI arguments after -- to override the default ./... target.
      Auto-installs golangci-lint if missing. When .custom-gcl.yml exists in the
      working directory, builds or reuses its custom binary instead.
    deps:
      - task: install:golangci-lint
    cmds:
      - task: _golangci-lint:lint
        vars:
          GO_LINT_FIX: ""
          GO_LINT_ARGS: "{{if .CLI_ARGS}}{{.CLI_ARGS}}{{else}}./...{{end}}"
  golangci-lint:lint:fix:
    run: once
    desc: Auto-fix Go lint issues with golangci-lint
    summary: |
      Run golangci-lint with --fix against all Go packages by default. Set
      GO_LINT_SKIP_PATTERN to a shell-style path glob for files that must not
      be modified. Pass CLI arguments after -- to override the default ./...
      target. Auto-installs golangci-lint if missing. When .custom-gcl.yml
      exists in the working directory, builds or reuses its custom binary
      instead.
    deps:
      - task: install:golangci-lint
    cmds:
      - task: _golangci-lint:lint
        vars:
          GO_LINT_FIX: "--fix"
          GO_LINT_ARGS: "{{if .CLI_ARGS}}{{.CLI_ARGS}}{{else}}./...{{end}}"
  config:skip:
    desc: Write a golangci-lint config overlay that excludes the skip pattern
    summary: |
      Generate a golangci-lint config that merges the project config with
      GO_LINT_SKIP_PATTERN translated into a linters.exclusions.paths regex. The
      overlay is written next to the project's golangci-lint config so relative
      paths inside it keep resolving; golangci-lint:lint and lint:fix run this
      first. It is rewritten on every run and is not removed afterwards, so add
      it to .gitignore. Running with no skip pattern set deletes it.
    dir: "{{.USER_WORKING_DIR}}"
    set: [errexit, nounset, pipefail]
    env:
      GO_SKIP_PATTERN: >-
        {{if .GO_LINT_SKIP_PATTERN_OVERRIDE}}{{.GO_LINT_SKIP_PATTERN_OVERRIDE}}
        {{- else}}{{.GO_LINT_SKIP_PATTERN}}{{end}}
    cmds:
      - cmd: |
          pattern="$GO_SKIP_PATTERN"
          source_config="$("{{.GLOBAL_GO_BIN}}/golangci-lint" config path 2>/dev/null || true)"
          config_dir=.
          if [[ -n "$source_config" ]]; then
            config_dir="$(dirname "$source_config")"
          fi
          output="$config_dir/{{.GO_INTERNAL_SKIP_CONFIG}}"

          # Nothing to skip: drop the overlay a previous run left behind so the
          # next task falls back to the project config instead of a stale one.
          if [[ -z "$pattern" ]]; then
            rm -f "$output"
            exit 0
          fi

          path_regex="$(printf '%s' "$pattern" | awk '
            {
              glob = $0
              gsub(/\\/, "/", glob)
              regex = "^"
              for (position = 1; position <= length(glob); position++) {
                character = substr(glob, position, 1)
                following = substr(glob, position + 1, 1)
                if (character == "*") {
                  if (following == "*") {
                    if (substr(glob, position + 2, 1) == "/") {
                      regex = regex "(?:.*/)?"
                      position += 2
                    } else {
                      regex = regex ".*"
                      position += 1
                    }
                  } else {
                    regex = regex "[^/]*"
                  }
                } else if (character == "?") {
                  regex = regex "[^/]"
                } else if (index(".+()[]{}^$|\\", character) > 0) {
                  regex = regex "\\" character
                } else {
                  regex = regex character
                }
              }
              printf "%s$", regex
            }
          ')"
          export GO_SKIP_REGEX="$path_regex"
          if [[ -z "$source_config" || ! -f "$source_config" ]]; then
            printf "version: \"2\"\nlinters:\n  exclusions:\n    paths:\n      - |-\n        %s\n" \
              "$path_regex" >"$output"
            exit 0
          fi

          first_content="$(awk '
            /^[[:space:]]*(#.*)?$/ { next }
            { sub(/^[[:space:]]*/, ""); print substr($0, 1, 1); exit }
          ' "$source_config")"
          if [[ "$first_content" == '{' ]]; then
            printf '%s\n' \
              'GO_LINT_SKIP_PATTERN requires a YAML golangci-lint config on macOS and Linux.' \
              'Convert the JSON config to YAML or add the exclusion to linters.exclusions.paths.' >&2
            exit 2
          fi

          awk '
            function indentation(value) {
              match(value, /^ */)
              return RLENGTH
            }
            function ignorable(value) {
              return value ~ /^ *($|#)/
            }
            function trimmed(value, result) {
              result = value
              sub(/^ */, "", result)
              sub(/ *$/, "", result)
              return result
            }
            function mapping_key_kind(value, key, result) {
              result = trimmed(value)
              if (result ~ ("^" key ":[ ]*(#.*)?$")) return 1
              if (result ~ ("^" key ":[ ]*\\{\\}[ ]*(#.*)?$")) return 2
              if (result ~ ("^" key ":")) return 3
              return 0
            }
            function block_end(start, base_indent, total, cursor) {
              for (cursor = start + 1; cursor <= total; cursor++) {
                if (!ignorable(lines[cursor]) && indentation(lines[cursor]) <= base_indent) {
                  return cursor
                }
              }
              return total + 1
            }
            function child_indent(start, finish, base_indent, cursor, current, minimum) {
              minimum = 0
              for (cursor = start + 1; cursor < finish; cursor++) {
                if (ignorable(lines[cursor])) continue
                current = indentation(lines[cursor])
                if (current > base_indent && (minimum == 0 || current < minimum)) minimum = current
              }
              return minimum == 0 ? base_indent + 2 : minimum
            }
            function spaces(count) {
              return sprintf("%*s", count, "")
            }
            function fail(message) {
              print "prepare golangci-lint overlay: " message > "/dev/stderr"
              failed = 1
            }
            {
              lines[NR] = $0
            }
            END {
              regex = ENVIRON["GO_SKIP_REGEX"]
              gsub(/\047/, "\047\047", regex)
              quoted_regex = "\047" regex "\047"
              total = NR

              linters = 0
              for (cursor = 1; cursor <= total; cursor++) {
                if (indentation(lines[cursor]) != 0) continue
                kind = mapping_key_kind(lines[cursor], "linters")
                if (kind == 0) continue
                if (kind == 3) {
                  fail("linters must be a YAML mapping")
                  exit 2
                }
                linters = cursor
                if (kind == 2) lines[cursor] = "linters:"
                break
              }

              if (linters == 0) {
                for (cursor = 1; cursor <= total; cursor++) print lines[cursor]
                if (total > 0 && lines[total] !~ /^ *$/) print ""
                print "linters:"
                print "  exclusions:"
                print "    paths:"
                print "      - " quoted_regex
                exit
              }

              linters_end = block_end(linters, 0, total)
              linters_child = child_indent(linters, linters_end, 0)
              exclusions = 0
              for (cursor = linters + 1; cursor < linters_end; cursor++) {
                if (indentation(lines[cursor]) != linters_child) continue
                kind = mapping_key_kind(lines[cursor], "exclusions")
                if (kind == 0) continue
                if (kind == 3) {
                  fail("linters.exclusions must be a YAML mapping")
                  exit 2
                }
                exclusions = cursor
                if (kind == 2) lines[cursor] = spaces(linters_child) "exclusions:"
                break
              }

              if (exclusions == 0) {
                for (cursor = 1; cursor <= total + 1; cursor++) {
                  if (cursor == linters_end) {
                    print spaces(linters_child) "exclusions:"
                    print spaces(linters_child + 2) "paths:"
                    print spaces(linters_child + 4) "- " quoted_regex
                  }
                  if (cursor <= total) print lines[cursor]
                }
                exit
              }

              exclusions_end = block_end(exclusions, linters_child, total)
              exclusions_child = child_indent(exclusions, exclusions_end, linters_child)
              paths = 0
              empty_paths = 0
              for (cursor = exclusions + 1; cursor < exclusions_end; cursor++) {
                if (indentation(lines[cursor]) != exclusions_child) continue
                value = trimmed(lines[cursor])
                if (value ~ /^paths:[ ]*(#.*)?$/) {
                  paths = cursor
                  break
                }
                if (value ~ /^paths:[ ]*\[\][ ]*(#.*)?$/) {
                  paths = cursor
                  empty_paths = 1
                  break
                }
                if (value ~ /^paths:/) {
                  fail("linters.exclusions.paths must be a block YAML sequence")
                  exit 2
                }
              }

              if (paths == 0) {
                for (cursor = 1; cursor <= total + 1; cursor++) {
                  if (cursor == exclusions_end) {
                    print spaces(exclusions_child) "paths:"
                    print spaces(exclusions_child + 2) "- " quoted_regex
                  }
                  if (cursor <= total) print lines[cursor]
                }
                exit
              }

              if (empty_paths) lines[paths] = spaces(exclusions_child) "paths:"
              for (cursor = 1; cursor <= total; cursor++) {
                print lines[cursor]
                if (cursor == paths) print spaces(exclusions_child + 2) "- " quoted_regex
              }
            }
          ' "$source_config" >"$output"
        platforms: [linux, darwin]
      - cmd: |
          powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
          $ErrorActionPreference = 'Stop'
          $pattern = $env:GO_SKIP_PATTERN
          $sourceConfig = (& '{{.GLOBAL_GO_BIN}}/golangci-lint' config path 2>$null | Select-Object -First 1)
          if ($LASTEXITCODE -ne 0) { $sourceConfig = '' }
          if ($sourceConfig) {
              $sourceConfig = (Resolve-Path -LiteralPath $sourceConfig).Path
              $configDir = Split-Path -Parent $sourceConfig
          } else {
              $configDir = (Get-Location).Path
          }
          $output = Join-Path $configDir '{{.GO_INTERNAL_SKIP_CONFIG}}'

          if ([string]::IsNullOrWhiteSpace($pattern)) {
              Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
              exit 0
          }

          $normalizedPattern = $pattern.Replace('\', '/')
          $builder = [System.Text.StringBuilder]::new('^')
          for ($index = 0; $index -lt $normalizedPattern.Length; $index++) {
              $character = $normalizedPattern[$index]
              if ($character -eq '*') {
                  if (($index + 1) -lt $normalizedPattern.Length -and
                      $normalizedPattern[$index + 1] -eq '*') {
                      if (($index + 2) -lt $normalizedPattern.Length -and
                          $normalizedPattern[$index + 2] -eq '/') {
                          [void]$builder.Append('(?:.*/)?')
                          $index += 2
                      } else {
                          [void]$builder.Append('.*')
                          $index += 1
                      }
                  } else {
                      [void]$builder.Append('[^/]*')
                  }
              } elseif ($character -eq '?') {
                  [void]$builder.Append('[^/]')
              } else {
                  [void]$builder.Append([regex]::Escape([string]$character))
              }
          }
          [void]$builder.Append('$')
          $pathRegex = $builder.ToString()
          $quotedRegex = "'" + $pathRegex.Replace("'", "''") + "'"

          if ([string]::IsNullOrWhiteSpace($sourceConfig) -or
              -not (Test-Path -LiteralPath $sourceConfig -PathType Leaf)) {
              @(
                  'version: "2"',
                  'linters:',
                  '  exclusions:',
                  '    paths:',
                  "      - $quotedRegex"
              ) | Set-Content -LiteralPath $output -Encoding UTF8
              exit 0
          }

          $firstContent = Get-Content -LiteralPath $sourceConfig |
              Where-Object { $_ -notmatch '^\s*(?:#.*)?$' } |
              Select-Object -First 1
          if ($firstContent.TrimStart().StartsWith('{')) {
              $config = Get-Content -LiteralPath $sourceConfig -Raw | ConvertFrom-Json

              function Get-Or-AddObjectProperty([object]$parent, [string]$name) {
                  $property = $parent.PSObject.Properties[$name]
                  if ($null -eq $property) {
                      $value = [pscustomobject]@{}
                      $parent | Add-Member -MemberType NoteProperty -Name $name -Value $value
                      return $value
                  }
                  if ($property.Value -isnot [pscustomobject]) {
                      throw "$name must be an object"
                  }
                  return $property.Value
              }

              $linters = Get-Or-AddObjectProperty $config 'linters'
              $exclusions = Get-Or-AddObjectProperty $linters 'exclusions'
              $pathsProperty = $exclusions.PSObject.Properties['paths']
              if ($null -eq $pathsProperty) {
                  $exclusions | Add-Member -MemberType NoteProperty -Name 'paths' -Value @($pathRegex)
              } elseif ($pathsProperty.Value -is [string]) {
                  throw 'linters.exclusions.paths must be an array'
              } else {
                  $paths = @($pathsProperty.Value)
                  if ($pathRegex -notin $paths) {
                      $pathsProperty.Value = @($paths + $pathRegex)
                  }
              }
              $config | ConvertTo-Json -Depth 100 |
                  Set-Content -LiteralPath $output -Encoding UTF8
              exit 0
          }

          function Get-YamlIndent([string]$line) {
              return $line.Length - $line.TrimStart([char]' ').Length
          }
          function Test-YamlIgnorable([string]$line) {
              return $line -match '^\s*(?:#.*)?$'
          }
          function Get-YamlMappingKeyKind([string]$line, [string]$key) {
              $value = $line.Trim()
              $escapedKey = [regex]::Escape($key)
              if ($value -match "^${escapedKey}:\s*(?:#.*)?$") { return 1 }
              if ($value -match "^${escapedKey}:\s*\{\}\s*(?:#.*)?$") { return 2 }
              if ($value -match "^${escapedKey}:") { return 3 }
              return 0
          }
          function Get-YamlBlockEnd(
              [System.Collections.Generic.List[string]]$items,
              [int]$start,
              [int]$baseIndent
          ) {
              for ($index = $start + 1; $index -lt $items.Count; $index++) {
                  if (-not (Test-YamlIgnorable $items[$index]) -and
                      (Get-YamlIndent $items[$index]) -le $baseIndent) {
                      return $index
                  }
              }
              return $items.Count
          }
          function Get-YamlChildIndent(
              [System.Collections.Generic.List[string]]$items,
              [int]$start,
              [int]$finish,
              [int]$baseIndent
          ) {
              $minimum = 0
              for ($index = $start + 1; $index -lt $finish; $index++) {
                  if (Test-YamlIgnorable $items[$index]) { continue }
                  $current = Get-YamlIndent $items[$index]
                  if ($current -gt $baseIndent -and
                      ($minimum -eq 0 -or $current -lt $minimum)) {
                      $minimum = $current
                  }
              }
              if ($minimum -eq 0) { return $baseIndent + 2 }
              return $minimum
          }

          $lines = [System.Collections.Generic.List[string]]::new()
          foreach ($line in (Get-Content -LiteralPath $sourceConfig)) { $lines.Add($line) }
          $lintersIndex = -1
          for ($index = 0; $index -lt $lines.Count; $index++) {
              if ((Get-YamlIndent $lines[$index]) -ne 0) { continue }
              $kind = Get-YamlMappingKeyKind $lines[$index] 'linters'
              if ($kind -eq 0) { continue }
              if ($kind -eq 3) { throw 'linters must be a YAML mapping' }
              $lintersIndex = $index
              if ($kind -eq 2) { $lines[$index] = 'linters:' }
              break
          }

          if ($lintersIndex -lt 0) {
              if ($lines.Count -gt 0 -and -not [string]::IsNullOrWhiteSpace($lines[$lines.Count - 1])) {
                  $lines.Add('')
              }
              $lines.Add('linters:')
              $lines.Add('  exclusions:')
              $lines.Add('    paths:')
              $lines.Add("      - $quotedRegex")
              $lines | Set-Content -LiteralPath $output -Encoding UTF8
              exit 0
          }

          $lintersEnd = Get-YamlBlockEnd $lines $lintersIndex 0
          $lintersIndent = Get-YamlChildIndent $lines $lintersIndex $lintersEnd 0
          $exclusionsIndex = -1
          for ($index = $lintersIndex + 1; $index -lt $lintersEnd; $index++) {
              if ((Get-YamlIndent $lines[$index]) -ne $lintersIndent) { continue }
              $kind = Get-YamlMappingKeyKind $lines[$index] 'exclusions'
              if ($kind -eq 0) { continue }
              if ($kind -eq 3) { throw 'linters.exclusions must be a YAML mapping' }
              $exclusionsIndex = $index
              if ($kind -eq 2) { $lines[$index] = (' ' * $lintersIndent) + 'exclusions:' }
              break
          }

          if ($exclusionsIndex -lt 0) {
              $lines.InsertRange($lintersEnd, [string[]]@(
                  (' ' * $lintersIndent) + 'exclusions:',
                  (' ' * ($lintersIndent + 2)) + 'paths:',
                  (' ' * ($lintersIndent + 4)) + "- $quotedRegex"
              ))
              $lines | Set-Content -LiteralPath $output -Encoding UTF8
              exit 0
          }

          $exclusionsEnd = Get-YamlBlockEnd $lines $exclusionsIndex $lintersIndent
          $pathsIndent = Get-YamlChildIndent $lines $exclusionsIndex $exclusionsEnd $lintersIndent
          $pathsIndex = -1
          $emptyPaths = $false
          for ($index = $exclusionsIndex + 1; $index -lt $exclusionsEnd; $index++) {
              if ((Get-YamlIndent $lines[$index]) -ne $pathsIndent) { continue }
              $value = $lines[$index].Trim()
              if ($value -match '^paths:\s*(?:#.*)?$') {
                  $pathsIndex = $index
                  break
              }
              if ($value -match '^paths:\s*\[\]\s*(?:#.*)?$') {
                  $pathsIndex = $index
                  $emptyPaths = $true
                  break
              }
              if ($value -match '^paths:') {
                  throw 'linters.exclusions.paths must be a block YAML sequence'
              }
          }

          if ($pathsIndex -lt 0) {
              $lines.InsertRange($exclusionsEnd, [string[]]@(
                  (' ' * $pathsIndent) + 'paths:',
                  (' ' * ($pathsIndent + 2)) + "- $quotedRegex"
              ))
          } else {
              if ($emptyPaths) { $lines[$pathsIndex] = (' ' * $pathsIndent) + 'paths:' }
              $lines.Insert($pathsIndex + 1, (' ' * ($pathsIndent + 2)) + "- $quotedRegex")
          }
          $lines | Set-Content -LiteralPath $output -Encoding UTF8
          POWERSHELL
        platforms: [windows]
  _golangci-lint:custom:
    run: once
    internal: true
    dir: "{{.USER_WORKING_DIR}}"
    if: test -f .custom-gcl.yml
    method: checksum
    vars:
      CUSTOM_GCL_YAML:
        sh: if [ -f .custom-gcl.yml ]; then cat .custom-gcl.yml; else printf '{}'; fi
      CUSTOM_GCL:
        ref: fromYaml .CUSTOM_GCL_YAML
      CUSTOM_GCL_NAME: '{{default "custom-gcl" .CUSTOM_GCL.name}}'
      CUSTOM_GCL_DESTINATION: '{{default "." .CUSTOM_GCL.destination}}'
      CUSTOM_GCL_BINARY_NAME: "{{.CUSTOM_GCL_NAME}}{{exeExt}}"
      CUSTOM_GCL_BINARY: "{{joinPath .CUSTOM_GCL_DESTINATION .CUSTOM_GCL_BINARY_NAME}}"
    sources:
      - .custom-gcl.yml
    generates:
      - "{{.CUSTOM_GCL_BINARY}}"
    cmds:
      - '"{{.GLOBAL_GO_BIN}}/golangci-lint" custom -v'
  _golangci-lint:lint:
    run: once
    internal: true
    dir: "{{.USER_WORKING_DIR}}"
    deps:
      - task: _golangci-lint:custom
    vars:
      CUSTOM_GCL_ENABLED:
        sh: if [ -f .custom-gcl.yml ]; then printf true; else printf false; fi
      CUSTOM_GCL_YAML:
        sh: if [ -f .custom-gcl.yml ]; then cat .custom-gcl.yml; else printf '{}'; fi
      CUSTOM_GCL:
        ref: fromYaml .CUSTOM_GCL_YAML
      CUSTOM_GCL_NAME: '{{default "custom-gcl" .CUSTOM_GCL.name}}'
      CUSTOM_GCL_DESTINATION: '{{default "." .CUSTOM_GCL.destination}}'
      CUSTOM_GCL_BINARY_NAME: "{{.CUSTOM_GCL_NAME}}{{exeExt}}"
      CUSTOM_GCL_BINARY: "{{joinPath .CUSTOM_GCL_DESTINATION .CUSTOM_GCL_BINARY_NAME}}"
    cmds:
      - cmd: |
          lint_binary="{{.GLOBAL_GO_BIN}}/golangci-lint"
          if [ "{{.CUSTOM_GCL_ENABLED}}" = "true" ]; then
            case "{{.CUSTOM_GCL_BINARY}}" in
              /*) lint_binary="{{.CUSTOM_GCL_BINARY}}" ;;
              *) lint_binary="./{{.CUSTOM_GCL_BINARY}}" ;;
            esac
          fi

          skip_pattern='{{if .GO_LINT_SKIP_PATTERN_OVERRIDE}}{{.GO_LINT_SKIP_PATTERN_OVERRIDE}}
          {{- else}}{{.GO_LINT_SKIP_PATTERN}}{{end}}'
          if [ -z "$skip_pattern" ]; then
            "$lint_binary" run {{.GO_LINT_FIX}} {{.GO_LINT_ARGS}}
            exit
          fi

          config_path="$("$lint_binary" config path 2>/dev/null || true)"
          config_dir=.
          if [ -n "$config_path" ]; then
            config_dir="$(dirname "$config_path")"
          fi

          # config:skip resolves the config directory the same way, so both sides
          # agree on where the overlay lands. It is left in place afterwards and
          # overwritten by the next run.
          overlay_config="$config_dir/{{.GO_INTERNAL_SKIP_CONFIG}}"
          if ! task --silent --taskfile "{{.TASKFILE_DIR}}/Taskfile.yml" config:skip \
            "GO_LINT_SKIP_PATTERN_OVERRIDE=$skip_pattern"; then
            exit 1
          fi

          lint_status=0
          "$lint_binary" run \
            {{.GO_LINT_FIX}} \
            {{.GO_LINT_ARGS}} \
            --config "$overlay_config" || lint_status=$?
          exit "$lint_status"
        platforms: [linux, darwin]
      - cmd: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "$ErrorActionPreference='Stop'; $lintBinary =
          '{{.GLOBAL_GO_BIN}}/golangci-lint'; if ('{{.CUSTOM_GCL_ENABLED}}' -eq 'true') {

            $lintBinary = '{{.CUSTOM_GCL_BINARY}}'; if (-not [System.IO.Path]::IsPathRooted($lintBinary)) {

              $lintBinary = Join-Path (Get-Location) $lintBinary;
            }
          }; $skipPattern = '{{if
          .GO_LINT_SKIP_PATTERN_OVERRIDE}}{{.GO_LINT_SKIP_PATTERN_OVERRIDE}} {{-
          else}}{{.GO_LINT_SKIP_PATTERN}}{{end}}'; if (-not $skipPattern) {

            & $lintBinary run
              {{.GO_LINT_FIX}}
              {{.GO_LINT_ARGS}};
            exit $LASTEXITCODE;
          }; $configPath = (& $lintBinary config path 2>$null | Select-Object -First 1); if
          ($LASTEXITCODE -ne 0) {

            $configPath = '';
          }; if ($configPath) {

            $configPath = (Resolve-Path -LiteralPath $configPath).Path;
            $configDir = Split-Path -Parent $configPath;
          } else {

            $configDir = (Get-Location).Path;
          }; $overlayConfig = Join-Path $configDir '{{.GO_INTERNAL_SKIP_CONFIG}}';
          & task --silent --taskfile '{{.TASKFILE_DIR}}/Taskfile.yml' config:skip
            "GO_LINT_SKIP_PATTERN_OVERRIDE=$skipPattern"; if ($LASTEXITCODE -ne 0) {

            exit $LASTEXITCODE;
          }; & $lintBinary run
            {{.GO_LINT_FIX}}
            {{.GO_LINT_ARGS}}
            --config $overlayConfig; exit $LASTEXITCODE"
        platforms: [windows]
  golangci-lint:fmt:
    run: once
    desc: Format Go files with golangci-lint formatters
    summary: |
      Rewrite Go files with golangci-lint's gci, gofmt, gofumpt, goimports,
      golines, and swaggo formatters. Set GO_FMT_SKIP_PATTERN to a shell-style
      path glob for files to skip. Pass CLI arguments after -- to override the
      default target. Auto-installs golangci-lint if missing.
    deps:
      - task: install:golangci-lint
    cmds:
      - task: _golangci-lint:fmt
        vars:
          GO_FMT_DIFF: ""
          GO_FMT_TARGETS: "{{if .CLI_ARGS}}{{.CLI_ARGS}}{{else}}.{{end}}"
  golangci-lint:fmt:check:
    run: once
    desc: Check Go formatting with golangci-lint formatters
    summary: |
      Check Go files with golangci-lint's gci, gofmt, gofumpt, goimports,
      golines, and swaggo formatters by printing diffs instead of rewriting
      files. Set GO_FMT_SKIP_PATTERN to a shell-style path glob for files to
      skip. Pass CLI arguments after -- to override the default target.
      Auto-installs golangci-lint if missing.
    deps:
      - task: install:golangci-lint
    cmds:
      - task: _golangci-lint:fmt
        vars:
          GO_FMT_DIFF: "--diff"
          GO_FMT_TARGETS: "{{if .CLI_ARGS}}{{.CLI_ARGS}}{{else}}.{{end}}"
  _golangci-lint:fmt:
    run: once
    internal: true
    dir: "{{.USER_WORKING_DIR}}"
    cmds:
      - cmd: |
          skip_pattern='{{if .GO_FMT_SKIP_PATTERN_OVERRIDE}}{{.GO_FMT_SKIP_PATTERN_OVERRIDE}}
          {{- else}}{{.GO_FMT_SKIP_PATTERN}}{{end}}'
          if [ -z "$skip_pattern" ]; then
            "{{.GLOBAL_GO_BIN}}/golangci-lint" fmt \
              {{.GO_FMT_DIFF}} \
              {{.GO_FMT_FORMATTER_FLAGS}} \
              {{.GO_FMT_TARGETS}}
            exit
          fi

          file_list="$(mktemp)"
          trap 'rm -f "$file_list"' EXIT
          set --
          for target in {{.GO_FMT_TARGETS}}; do
            target="${target%...}"
            target="${target%/}"
            if [ -z "$target" ]; then
              target=.
            fi
            set -- "$@" "$target"
          done

          for target in "$@"; do
            if [ -d "$target" ]; then
              find "$target" \
                -type f \
                -name '*.go' \
                ! -path '*/node_modules/*' \
                ! -path '*/testdata/*' \
                ! -path '*/vendor/*' \
                -print0
            elif [ -f "$target" ]; then
              printf '%s\000' "$target"
            fi
          done | task --silent --taskfile "{{.TASKFILE_DIR}}/../internal/skipfiles/Taskfile.yml" \
            filter "SKIPFILES_PATTERN=$skip_pattern" >"$file_list"

          if [ ! -s "$file_list" ]; then
            echo 'No matching Go files found'
            exit 0
          fi

          xargs -0 "{{.GLOBAL_GO_BIN}}/golangci-lint" fmt \
              {{.GO_FMT_DIFF}} \
              {{.GO_FMT_FORMATTER_FLAGS}} <"$file_list"
        platforms: [linux, darwin]
      - cmd: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "$ErrorActionPreference='Stop'; $skipPattern = '{{if
          .GO_FMT_SKIP_PATTERN_OVERRIDE}}{{.GO_FMT_SKIP_PATTERN_OVERRIDE}} {{- else}}{{.GO_FMT_SKIP_PATTERN}}{{end}}';
          if (-not $skipPattern) {

            & '{{.GLOBAL_GO_BIN}}/golangci-lint' fmt
              {{.GO_FMT_DIFF}}
              {{.GO_FMT_FORMATTER_FLAGS}}
              {{.GO_FMT_TARGETS}};
            exit $LASTEXITCODE;
          }; $targets = '{{.GO_FMT_TARGETS}}' -split '\s+'; $files = @($targets | ForEach-Object {

            $target = $_ -replace '\.\.\.$', '';
            $target = $target.TrimEnd('/').TrimEnd('\');
            if (-not $target) { $target = '.' };
            if (Test-Path -LiteralPath $target -PathType Leaf) {
              Get-Item -LiteralPath $target;
            } else {
              Get-ChildItem -LiteralPath $target -Recurse -Filter '*.go' -File;
            };
          } | Where-Object {

            $path = [string](Resolve-Path -LiteralPath $_.FullName -Relative);
            $path = $path.Replace('\', '/');
            $path -notlike '*/node_modules/*' -and
              $path -notlike '*/testdata/*' -and
              $path -notlike '*/vendor/*';
          } | ForEach-Object FullName |

            task --silent --taskfile '{{.TASKFILE_DIR}}/../internal/skipfiles/Taskfile.yml'
              filter ('SKIPFILES_PATTERN=' + $skipPattern));
          if ($files.Count -gt 0) {

            & '{{.GLOBAL_GO_BIN}}/golangci-lint' fmt
              {{.GO_FMT_DIFF}}
              {{.GO_FMT_FORMATTER_FLAGS}}
              @files;
            exit $LASTEXITCODE;
          }"
        platforms: [windows]
  govulncheck:lint:
    run: once
    desc: Scan Go packages with govulncheck
    summary: |
      Scan all Go packages for known vulnerabilities by default. Pass CLI
      arguments after -- to override the default ./... target. Auto-installs
      govulncheck if missing. Packages containing files matched by
      GO_LINT_SKIP_PATTERN are omitted.
    deps:
      - task: install:govulncheck
    cmds:
      - task: _go:package-analysis
        vars:
          GO_ANALYSIS_BINARY: "{{.GLOBAL_GO_BIN}}/govulncheck"
          GO_ANALYSIS_PREFIX_ARGS: ""
  gosec:lint:
    run: once
    desc: Scan Go packages with gosec
    summary: |
      Scan all Go packages for security issues by default. Pass CLI arguments
      after -- to override the default ./... target. Auto-installs gosec if
      missing. Packages containing files matched by GO_LINT_SKIP_PATTERN are
      omitted.
    deps:
      - task: install:gosec
    cmds:
      - task: _go:package-analysis
        vars:
          GO_ANALYSIS_BINARY: "{{.GLOBAL_GO_BIN}}/gosec"
          GO_ANALYSIS_PREFIX_ARGS: "-exclude-generated"
  _go:package-analysis:
    internal: true
    dir: "{{.USER_WORKING_DIR}}"
    cmds:
      - cmd: |
          skip_pattern='{{if .GO_LINT_SKIP_PATTERN_OVERRIDE}}{{.GO_LINT_SKIP_PATTERN_OVERRIDE}}
          {{- else}}{{.GO_LINT_SKIP_PATTERN}}{{end}}'
          if [ -z "$skip_pattern" ]; then
            "{{.GO_ANALYSIS_BINARY}}" {{.GO_ANALYSIS_PREFIX_ARGS}} \
              {{if .CLI_ARGS}}{{.CLI_ARGS}}{{else}}./...{{end}}
            exit
          fi
          targets=''
          options=''
          for argument in {{.CLI_ARGS}}; do
            case "$argument" in
              .|*/*|*\\*|*...) targets="$targets $argument" ;;
              *) options="$options $argument" ;;
            esac
          done
          [ -n "$targets" ] || targets='./...'
          package_list="$(mktemp)"
          trap 'rm -f "$package_list"' EXIT
          export SKIPFILES_PATTERN="$skip_pattern"
          export SKIPFILES_GO_TARGETS="$targets"
          # Propagate an enumeration failure instead of letting it look like an
          # empty package set: `go list` exits non-zero when a package does not
          # compile, and reporting success there would silently skip the scan.
          enumerate_status=0
          task --silent --taskfile "{{.TASKFILE_DIR}}/../internal/skipfiles/Taskfile.yml" \
            go-packages >"$package_list" || enumerate_status=$?
          if [ "$enumerate_status" -ne 0 ]; then
            echo 'Failed to enumerate Go packages' >&2
            exit "$enumerate_status"
          fi
          if [ ! -s "$package_list" ]; then
            echo 'No matching Go packages found'
            exit 0
          fi
          xargs -0 "{{.GO_ANALYSIS_BINARY}}" {{.GO_ANALYSIS_PREFIX_ARGS}} $options <"$package_list"
        platforms: [linux, darwin]
      - cmd: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "$skipPattern='{{if
          .GO_LINT_SKIP_PATTERN_OVERRIDE}}{{.GO_LINT_SKIP_PATTERN_OVERRIDE}} {{-
          else}}{{.GO_LINT_SKIP_PATTERN}}{{end}}'; if (-not $skipPattern) {

            & '{{.GO_ANALYSIS_BINARY}}' {{.GO_ANALYSIS_PREFIX_ARGS}}
              {{if .CLI_ARGS}}{{.CLI_ARGS}}{{else}}./...{{end}};
            exit $LASTEXITCODE
          }; $targets=@(); $options=@(); foreach ($argument in '{{.CLI_ARGS}}'.Split(' ',
          [System.StringSplitOptions]::RemoveEmptyEntries)) {

            if ($argument -eq '.' -or $argument.Contains('/') -or
                $argument.Contains('\') -or $argument.EndsWith('...')) {
              $targets += $argument
            } else { $options += $argument }
          }; if (-not $targets) { $targets=@('./...') }; $env:SKIPFILES_PATTERN = $skipPattern;
          $env:SKIPFILES_GO_TARGETS = $targets -join ' '; $packages=@(& task --silent

            --taskfile '{{.TASKFILE_DIR}}/../internal/skipfiles/Taskfile.yml'
            go-packages);
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if (-not $packages) { Write-Host 'No matching Go packages
          found'; exit 0 }; & '{{.GO_ANALYSIS_BINARY}}' {{.GO_ANALYSIS_PREFIX_ARGS}} @options @packages; exit
          $LASTEXITCODE"
        platforms: [windows]
  upgrade:
    run: once
    desc: Upgrade Go to the selected or latest stable release
    summary: |
      Upgrade Go to GO_VERSION, defaulting to the latest stable release.
      macOS uses Homebrew for latest or the official package for an explicit
      version. Linux uses the official tarball and Windows uses the official
      MSI.
    cmds:
      - task: _upgrade:macos:brew
      - task: _install:macos:pkg
      - task: _install:linux:tarball
        vars:
          FORCE: "true"
      - task: _install:windows:msi
        vars:
          FORCE: "true"
      - task: _shell:setup:linux
        vars:
          FORCE: "true"
  version:
    run: once
    desc: Show the installed Go version
    summary: Print the active Go toolchain version via `go version`. Auto-installs Go if missing.
    deps:
      - task: install
    cmds:
      - task: _version:unix
      - task: _version:windows
  which:
    run: once
    desc: Show the path to the Go binary
    summary: Print the Go binary resolved from PATH, falling back to GO_CMD_UNIX on Linux. Auto-installs Go if missing.
    deps:
      - task: install
    cmds:
      - task: _which:unix
      - task: _which:windows
  verify:
    run: once
    desc: Verify the Go installation and environment
    summary: |
      Check that Go is installed, print the active version, and show the
      resolved GOROOT and GOPATH values for quick installation diagnostics.
      Auto-installs Go if missing.
    deps:
      - task: install
    cmds:
      - task: _verify:unix
      - task: _verify:windows
  test:
    run: once
    dir: "{{.USER_WORKING_DIR}}"
    desc: Run Go unit tests and write JUnit XML and coverage reports
    summary: |
      Run `go test -v` with coverage against all Go packages by default, stream
      test output to stdout, write a JUnit XML report to GO_JUNIT_REPORT
      (default junit.xml), and write a coverage profile to GO_COVER_PROFILE
      (default coverage.out). Pass CLI arguments after -- to override the
      default ./... target or add flags.
    deps:
      - task: install:go-junit-report
    vars:
      GO_JUNIT_REPORT_OUT: '{{if .GO_JUNIT_REPORT}}{{.GO_JUNIT_REPORT}}{{else}}junit.xml{{end}}'
      GO_COVER_OUT: '{{if .GO_COVER_PROFILE}}{{.GO_COVER_PROFILE}}{{else}}coverage.out{{end}}'
    cmds:
      - >-
        go test -v -covermode atomic -coverprofile "{{.GO_COVER_OUT}}"
        {{if .CLI_ARGS}}{{.CLI_ARGS}}{{else}}./...{{end}} 2>&1 | "{{.GLOBAL_GO_BIN}}/go-junit-report"
        -set-exit-code -iocopy -out "{{.GO_JUNIT_REPORT_OUT}}"
  bench:
    run: once
    dir: "{{.USER_WORKING_DIR}}"
    desc: Run Go benchmarks
    summary: |
      Run `go test` benchmarks (-bench .) with memory statistics and no
      unit-test execution (-run '^$'). Pass CLI arguments after -- to override
      the default ./... target or the -bench pattern, for example:
      `task go:bench -- -bench BenchmarkName ./internal/parser`.
    cmds:
      - go test -run '^$' -bench . -benchmem {{if .CLI_ARGS}}{{.CLI_ARGS}}{{else}}./...{{end}}
  fuzz:
    run: once
    dir: "{{.USER_WORKING_DIR}}"
    desc: Run a Go fuzz target
    summary: |
      Run a Go fuzz target for a bounded duration (GO_FUZZTIME, default 30s). Go
      fuzzes a single target in one package per run, so pass the -fuzz pattern
      and package after --, for example:
      `task go:fuzz -- -fuzz FuzzName ./internal/parser`.
    vars:
      GO_FUZZ_TIME: '{{if .GO_FUZZTIME}}{{.GO_FUZZTIME}}{{else}}30s{{end}}'
    cmds:
      - go test -run '^$' -fuzztime '{{.GO_FUZZ_TIME}}' {{if .CLI_ARGS}}{{.CLI_ARGS}}{{else}}-fuzz . .{{end}}
  _install:macos:brew:
    run: once
    internal: true
    desc: Install Go on macOS using Homebrew
    summary: Run `brew install go` on macOS when GO_VERSION is not set.
    platforms: [darwin]
    set: [errexit, nounset, pipefail]
    status:
      - '[ -n "{{.GO_VERSION}}" ] || command -v go >/dev/null 2>&1'
    preconditions:
      - sh: command -v brew >/dev/null 2>&1
        msg: Homebrew is required to install Go on macOS. Install it from https://brew.sh and re-run this task.
    cmds:
      - echo 'Installing Go via Homebrew...'
      - brew update
      - brew install go
      - echo 'Go installed successfully.'
  _upgrade:macos:brew:
    run: once
    internal: true
    desc: Upgrade Go on macOS using Homebrew
    summary: Upgrade Go through Homebrew when GO_VERSION is not set.
    platforms: [darwin]
    set: [errexit, nounset, pipefail]
    status:
      - test -n "{{.GO_VERSION}}"
    preconditions:
      - sh: command -v brew >/dev/null 2>&1
        msg: Homebrew is required to upgrade Go on macOS. Install it from https://brew.sh and re-run this task.
    cmds:
      - echo 'Upgrading Go via Homebrew...'
      - brew update
      - |
        if brew list go >/dev/null 2>&1; then
          brew upgrade go
        else
          brew install go
        fi
  _install:macos:pkg:
    run: once
    internal: true
    desc: Install an explicit Go version on macOS
    summary: Download and install the official GO_VERSION package on macOS.
    platforms: [darwin]
    set: [errexit, nounset, pipefail]
    vars:
      GO_DARWIN_UNAME_ARCH:
        sh: uname -m
      GO_DARWIN_ARCH: >-
        {{if or (eq .GO_DARWIN_UNAME_ARCH "arm64") (eq .GO_DARWIN_UNAME_ARCH "aarch64")}}arm64 {{- else if or (eq
        .GO_DARWIN_UNAME_ARCH "x86_64") (eq .GO_DARWIN_UNAME_ARCH "amd64")}}amd64 {{-
        else}}{{.GO_DARWIN_UNAME_ARCH}}{{end}}
      GO_DARWIN_ARCH_SUPPORTED: >-
        {{if or (eq .GO_DARWIN_ARCH "arm64") (eq .GO_DARWIN_ARCH "amd64")}}true {{- else}}false{{end}}
    status:
      - >-
        [ -z "{{.GO_VERSION}}" ] || { [ "{{default "false" .FORCE}}" != "true" ] && test -x "{{.GO_CMD_UNIX}}" && [
        "$("{{.GO_CMD_UNIX}}" version | awk '{print $3}')" = "{{.GO_VERSION}}" ]; }
    preconditions:
      - sh: 'case "{{.GO_VERSION}}" in ""|go*) exit 0 ;; *) exit 1 ;; esac'
        msg: GO_VERSION must use the official Go release format, for example go1.26.2.
      - sh: '[ "{{.GO_DARWIN_ARCH_SUPPORTED}}" = "true" ]'
        msg: Unsupported macOS architecture {{.GO_DARWIN_UNAME_ARCH}}.
      - sh: command -v curl >/dev/null 2>&1
        msg: curl is required to download Go.
      - sh: command -v installer >/dev/null 2>&1
        msg: The macOS installer command is required to install Go.
      - sh: command -v shasum >/dev/null 2>&1
        msg: shasum is required to verify the Go download.
      - sh: 'test "$(id -u)" -eq 0 || command -v sudo >/dev/null 2>&1'
        msg: sudo is required to install the Go package when not running as root.
    cmds:
      - cmd: |
          # This task only handles an explicitly requested version; Homebrew
          # covers the "latest" path. The guard lives here and not only in
          # status: because `task --force` disables status checks, and a forced
          # run with an empty GO_VERSION would request
          # https://go.dev/dl/.darwin-arm64.pkg, which 404s.
          if [ -z "{{.GO_VERSION}}" ]; then
            exit 0
          fi

          work_dir="$(mktemp -d)"
          trap 'rm -rf "$work_dir"' EXIT INT TERM

          package="{{.GO_VERSION}}.darwin-{{.GO_DARWIN_ARCH}}.pkg"
          echo "Downloading $package..."
          curl -fL "{{.GO_DOWNLOAD_BASE_URL}}/$package" -o "$work_dir/$package"
          curl -fsSL "{{.GO_DOWNLOAD_BASE_URL}}/$package.sha256" -o "$work_dir/$package.sha256"

          expected="$(tr -d '[:space:]' < "$work_dir/$package.sha256")"
          actual="$(shasum -a 256 "$work_dir/$package" | awk '{print $1}')"
          if [ "$expected" != "$actual" ]; then
            echo "Checksum mismatch for $package: expected $expected, got $actual" >&2
            exit 1
          fi

          if [ "$(id -u)" -eq 0 ]; then
            installer -pkg "$work_dir/$package" -target /
          else
            sudo installer -pkg "$work_dir/$package" -target /
          fi
  _install:linux:tarball:
    run: once
    internal: true
    desc: Install Go on Linux using the official tarball
    summary: |
      Download GO_VERSION, defaulting to the latest stable Go Linux tarball,
      replace INSTALL_DIR_UNIX/go, and extract the new toolchain. Requires sudo
      when not running as root.
    platforms: [linux]
    set: [errexit, nounset, pipefail]
    vars:
      GO_UNAME_ARCH:
        sh: uname -m
      GO_LINUX_ARCH: >-
        {{if or (eq .GO_UNAME_ARCH "x86_64") (eq .GO_UNAME_ARCH "amd64")}}amd64 {{- else if or (eq .GO_UNAME_ARCH
        "aarch64") (eq .GO_UNAME_ARCH "arm64")}}arm64 {{- else if eq .GO_UNAME_ARCH "armv6l"}}armv6l {{- else if or (eq
        .GO_UNAME_ARCH "i386") (eq .GO_UNAME_ARCH "i686")}}386 {{- else if eq .GO_UNAME_ARCH "ppc64le"}}ppc64le {{- else
        if eq .GO_UNAME_ARCH "s390x"}}s390x {{- else}}{{.GO_UNAME_ARCH}}{{end}}
      GO_LINUX_ARCH_SUPPORTED: >-
        {{if or (eq .GO_LINUX_ARCH "amd64") (eq .GO_LINUX_ARCH "arm64") (eq .GO_LINUX_ARCH "armv6l") (eq .GO_LINUX_ARCH
        "386") (eq .GO_LINUX_ARCH "ppc64le") (eq .GO_LINUX_ARCH "s390x")}}true {{- else}}false{{end}}
    status:
      - '[ "{{default "false" .FORCE}}" != "true" ]'
      - >-
        [ -z "{{.GO_VERSION}}" ] || { command -v go >/dev/null 2>&1 && [ "$(go version | awk '{print $3}')" =
        "{{.GO_VERSION}}" ]; }
      - command -v go >/dev/null 2>&1
    preconditions:
      - sh: 'case "{{.GO_VERSION}}" in ""|go*) exit 0 ;; *) exit 1 ;; esac'
        msg: GO_VERSION must use the official Go release format, for example go1.26.2.
      - sh: command -v curl >/dev/null 2>&1
        msg: curl is required to download Go.
      - sh: command -v tar >/dev/null 2>&1
        msg: tar is required to extract Go.
      - sh: command -v uname >/dev/null 2>&1
        msg: uname is required to detect the system architecture.
      - sh: command -v sha256sum >/dev/null 2>&1 || command -v shasum >/dev/null 2>&1
        msg: sha256sum or shasum is required to verify the Go download.
      - sh: '[ "{{.GO_LINUX_ARCH_SUPPORTED}}" = "true" ]'
        msg: Unsupported Linux architecture {{.GO_UNAME_ARCH}}.
      - sh: 'test "$(id -u)" -eq 0 || command -v sudo >/dev/null 2>&1'
        msg: sudo is required to install Go under INSTALL_DIR_UNIX when not running as root.
    cmds:
      - cmd: |
          # Everything happens in a private temp directory removed by the trap,
          # so an interrupted or failed run leaves neither a partial download nor
          # a predictable /tmp path another user could have pre-created.
          work_dir="$(mktemp -d)"
          trap 'rm -rf "$work_dir" 2>/dev/null || sudo -n rm -rf "$work_dir" 2>/dev/null || true' \
            EXIT INT TERM

          privileged() {
            if [ "$(id -u)" -eq 0 ]; then
              "$@"
            else
              sudo "$@"
            fi
          }

          if [ -n "{{.GO_VERSION}}" ]; then
            archive="{{.GO_VERSION}}.linux-{{.GO_LINUX_ARCH}}.tar.gz"
          else
            archive="$(curl -fsSL "{{.GO_VERSION_URL}}" | sed -n '1p').linux-{{.GO_LINUX_ARCH}}.tar.gz"
          fi

          echo "Downloading $archive..."
          curl -fL "{{.GO_DOWNLOAD_BASE_URL}}/$archive" -o "$work_dir/$archive"
          curl -fsSL "{{.GO_DOWNLOAD_BASE_URL}}/$archive.sha256" -o "$work_dir/$archive.sha256"

          # go.dev serves the bare hex digest, not a checksum-file line.
          expected="$(tr -d '[:space:]' < "$work_dir/$archive.sha256")"
          if command -v sha256sum >/dev/null 2>&1; then
            actual="$(sha256sum "$work_dir/$archive" | awk '{print $1}')"
          else
            actual="$(shasum -a 256 "$work_dir/$archive" | awk '{print $1}')"
          fi
          if [ "$expected" != "$actual" ]; then
            echo "Checksum mismatch for $archive: expected $expected, got $actual" >&2
            exit 1
          fi

          # Extract and smoke-test the new toolchain *before* touching the
          # installed one. The previous flow removed GO_ROOT_UNIX first, so a
          # truncated archive left the machine with no Go at all and no rollback.
          # Extraction is privileged so GO_ROOT_UNIX stays root-owned.
          if [ "$(id -u)" -eq 0 ]; then
            tar -C "$work_dir" -xzf "$work_dir/$archive"
          else
            sudo tar -C "$work_dir" -xzf "$work_dir/$archive"
          fi
          "$work_dir/go/bin/go" version >/dev/null

          echo "Installing Go to {{.GO_ROOT_UNIX}}..."
          privileged rm -rf "{{.GO_ROOT_UNIX}}.old"
          if [ -d "{{.GO_ROOT_UNIX}}" ]; then
            privileged mv "{{.GO_ROOT_UNIX}}" "{{.GO_ROOT_UNIX}}.old"
          fi
          if ! privileged mv "$work_dir/go" "{{.GO_ROOT_UNIX}}"; then
            echo "Install failed; restoring the previous toolchain." >&2
            if [ -d "{{.GO_ROOT_UNIX}}.old" ]; then
              privileged mv "{{.GO_ROOT_UNIX}}.old" "{{.GO_ROOT_UNIX}}"
            fi
            exit 1
          fi
          privileged rm -rf "{{.GO_ROOT_UNIX}}.old"
          echo "Go installed to {{.GO_ROOT_UNIX}}."
  _install:windows:msi:
    run: once
    internal: true
    desc: Install Go on Windows using the official MSI
    summary: |
      Download GO_VERSION, defaulting to the latest stable Go Windows MSI, and
      install it quietly.
    platforms: [windows]
    status:
      - >-
        powershell -NoProfile -ExecutionPolicy Bypass -Command "if ('{{default "false" .FORCE}}' -eq 'true') { exit 1 };
        $requested = '{{.GO_VERSION}}'; if (Get-Command go -ErrorAction SilentlyContinue) {

          if ([string]::IsNullOrWhiteSpace($requested)
          -or ((& go version).Split()[2] -eq $requested)) { exit 0 }
        }; exit 1"
    preconditions:
      - sh: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "$version = '{{.GO_VERSION}}'; if
          ([string]::IsNullOrWhiteSpace($version) -or $version.StartsWith('go')) { exit 0 } else { exit 1 }"
        msg: GO_VERSION must use the official Go release format, for example go1.26.2.
      - sh: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "if ($env:PROCESSOR_ARCHITECTURE -eq 'AMD64' -or
          $env:PROCESSOR_ARCHITECTURE -eq 'ARM64') { exit 0 } else { exit 1 }"
        msg: Go MSI install supports AMD64 and ARM64 Windows architectures.
    cmds:
      # Resolve the version, pick the architecture, download, verify and install
      # in one PowerShell session. Splitting these across commands previously
      # required staging the version in a shared TEMP file and duplicating the
      # download for AMD64 and ARM64.
      - >-
        powershell -NoProfile -ExecutionPolicy Bypass -Command "$ErrorActionPreference='Stop'; $version =
        '{{.GO_VERSION}}'; if ([string]::IsNullOrWhiteSpace($version)) {

          $version = ((Invoke-RestMethod -Uri '{{.GO_VERSION_URL}}') -split \"`n\")[0].Trim()
        }; $arch = if ($env:PROCESSOR_ARCHITECTURE -eq 'ARM64') { 'arm64' } else { 'amd64' }; $package =
        \"$version.windows-$arch.msi\"; $workDir = Join-Path $env:TEMP ('taskotter-go-' +
        [Guid]::NewGuid().ToString('N')); New-Item -ItemType Directory -Path $workDir | Out-Null; try {

          $installer = Join-Path $workDir $package;
          Write-Host \"Downloading $package...\";
          Invoke-WebRequest -Uri \"{{.GO_DOWNLOAD_BASE_URL}}/$package\" -OutFile $installer;
          $expected = (Invoke-RestMethod
            -Uri \"{{.GO_DOWNLOAD_BASE_URL}}/$package.sha256\").Trim();
          $actual = (Get-FileHash -LiteralPath $installer -Algorithm SHA256).Hash;
          if ($expected -ne $actual) {
            Write-Error \"Checksum mismatch for $package: expected $expected, got $actual\";
          };
          Start-Process msiexec.exe -Wait
            -ArgumentList @('/i', $installer, '/quiet', '/norestart');
        } finally {

          Remove-Item -LiteralPath $workDir -Recurse -Force -ErrorAction SilentlyContinue;
        }"
  _install:undo:macos:
    run: once
    internal: true
    platforms: [darwin]
    set: [errexit, nounset, pipefail]
    status:
      - '! command -v go >/dev/null 2>&1 && ! test -d "{{.GO_ROOT_UNIX}}"'
    cmds:
      - |
        if command -v brew >/dev/null 2>&1 && brew list go >/dev/null 2>&1; then
          echo 'Removing Go via Homebrew...'
          brew uninstall go
        elif [ -d "{{.GO_ROOT_UNIX}}" ]; then
          echo 'Removing Go from {{.GO_ROOT_UNIX}}...'
          rm -rf "{{.GO_ROOT_UNIX}}"
        fi
      - echo 'Go removed.'
  _install:undo:linux:
    run: once
    internal: true
    platforms: [linux]
    set: [errexit, nounset, pipefail]
    status:
      - '! test -d "{{.GO_ROOT_UNIX}}"'
    preconditions:
      - sh: 'test "$(id -u)" -eq 0 || command -v sudo >/dev/null 2>&1'
        msg: sudo is required to remove Go under INSTALL_DIR_UNIX when not running as root.
    cmds:
      - echo 'Removing Go from {{.GO_ROOT_UNIX}}...'
      - cmd: rm -rf "{{.GO_ROOT_UNIX}}"
        if: '[ "$(id -u)" -eq 0 ]'
      - cmd: sudo rm -rf "{{.GO_ROOT_UNIX}}"
        if: '[ "$(id -u)" -ne 0 ]'
      - echo 'Go removed.'
  _install:undo:windows:
    run: once
    internal: true
    platforms: [windows]
    status:
      - >-
        powershell -NoProfile -ExecutionPolicy Bypass -Command "if (Get-Command go -ErrorAction SilentlyContinue) { exit
        1 } else { exit 0 }"
    cmds:
      - echo Removing Go...
      - >-
        powershell -NoProfile -ExecutionPolicy Bypass -Command "$pkg = Get-Package -Name 'Go Programming Language*'
        -ErrorAction SilentlyContinue; if ($pkg) { $pkg | Uninstall-Package -Force } else { Write-Warning 'Go package
        not found via Get-Package. Remove it manually from Apps & Features.' }"
      - echo Go removed. Restart your terminal.
  _shell:setup:linux:
    run: once
    internal: true
    desc: Configure Go PATH on Linux
    summary: Add GO_BIN_UNIX to the current user's shell profile if it is not already available.
    platforms: [linux]
    set: [errexit, nounset, pipefail]
    status:
      - '[ "{{default "false" .FORCE}}" != "true" ] && command -v go >/dev/null 2>&1'
    cmds:
      - task: _shell:setup:linux:profile
        vars:
          FORCE: "{{.FORCE}}"
          GO_PROFILE: "$HOME/.zshrc"
        if: '[ "$(basename "${SHELL:-}")" = "zsh" ]'
      - task: _shell:setup:linux:profile
        vars:
          FORCE: "{{.FORCE}}"
          GO_PROFILE: "$HOME/.bashrc"
        if: '[ "$(basename "${SHELL:-}")" = "bash" ]'
      - task: _shell:setup:linux:profile
        vars:
          FORCE: "{{.FORCE}}"
          GO_PROFILE: "$HOME/.profile"
        if: '[ "$(basename "${SHELL:-}")" != "zsh" ] && [ "$(basename "${SHELL:-}")" != "bash" ]'
  _shell:setup:linux:profile:
    run: once
    internal: true
    desc: Configure Go PATH in a Linux shell profile
    summary: Add GO_BIN_UNIX to the selected Linux shell profile if it is not already available.
    platforms: [linux]
    set: [errexit, nounset, pipefail]
    requires:
      vars:
        - GO_PROFILE
    status:
      - >-
        [ "{{default "false" .FORCE}}" != "true" ] && command -v go >/dev/null 2>&1 || grep -qF "{{.GO_BIN_UNIX}}"
        "{{.GO_PROFILE}}" 2>/dev/null
    cmds:
      # The header and the export are appended by a single guarded block. As two
      # separately guarded commands, an interrupt between them left a "# Go"
      # header with no export, and the second guard then saw the marker and
      # skipped forever.
      - cmd: |
          touch "{{.GO_PROFILE}}"
          if ! grep -qF "{{.GO_BIN_UNIX}}" "{{.GO_PROFILE}}" 2>/dev/null; then
            printf '\n# Go\nexport PATH="$PATH:{{.GO_BIN_UNIX}}"\n' >> "{{.GO_PROFILE}}"
          fi
      - echo "Go PATH added to {{.GO_PROFILE}}. Restart your shell or run export PATH=\"\$PATH:{{.GO_BIN_UNIX}}\"."
  _version:unix:
    run: once
    internal: true
    desc: Show Go version on macOS or Linux
    summary: Print `go version` on macOS or Linux.
    platforms: [linux, darwin]
    preconditions:
      - sh: command -v go >/dev/null 2>&1 || test -x "{{.GO_CMD_UNIX}}"
        msg: Go is not installed or not available in PATH.
    cmds:
      # An explicit GO_VERSION pins the toolchain under GO_ROOT_UNIX; otherwise
      # prefer whatever PATH resolves, falling back to GO_ROOT_UNIX.
      - cmd: |
          if [ -n "{{.GO_VERSION}}" ] && [ -x "{{.GO_CMD_UNIX}}" ]; then
            "{{.GO_CMD_UNIX}}" version
          elif command -v go >/dev/null 2>&1; then
            go version
          else
            "{{.GO_CMD_UNIX}}" version
          fi
  _version:windows:
    run: once
    internal: true
    desc: Show Go version on Windows
    summary: Print `go version` on Windows.
    platforms: [windows]
    preconditions:
      - sh: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "if (Get-Command go -ErrorAction SilentlyContinue) {
          exit 0 } else { exit 1 }"
        msg: Go is not installed or not available in PATH.
    cmds:
      - go version
  _which:unix:
    run: once
    internal: true
    desc: Show Go binary path on macOS or Linux
    summary: Print the Go binary path resolved by the current PATH on macOS or Linux.
    platforms: [linux, darwin]
    preconditions:
      - sh: command -v go >/dev/null 2>&1 || test -x "{{.GO_CMD_UNIX}}"
        msg: Go is not installed or not available in PATH.
    cmds:
      - cmd: |
          if [ -n "{{.GO_VERSION}}" ] && [ -x "{{.GO_CMD_UNIX}}" ]; then
            printf '%s\n' '{{.GO_CMD_UNIX}}'
          elif command -v go >/dev/null 2>&1; then
            command -v go
          else
            printf '%s\n' '{{.GO_CMD_UNIX}}'
          fi
  _which:windows:
    run: once
    internal: true
    desc: Show Go binary path on Windows
    summary: Print the Go binary path resolved by PowerShell on Windows.
    platforms: [windows]
    preconditions:
      - sh: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "if (Get-Command go -ErrorAction SilentlyContinue) {
          exit 0 } else { exit 1 }"
        msg: Go is not installed or not available in PATH.
    cmds:
      - powershell -NoProfile -ExecutionPolicy Bypass -Command "(Get-Command go).Source"
  _verify:unix:
    run: once
    internal: true
    desc: Verify Go on macOS or Linux
    summary: Print Go version, GOROOT, and GOPATH on macOS or Linux.
    platforms: [linux, darwin]
    preconditions:
      - sh: command -v go >/dev/null 2>&1 || test -x "{{.GO_CMD_UNIX}}"
        msg: Go is not installed or not available in PATH.
    cmds:
      - cmd: |
          if [ -n "{{.GO_VERSION}}" ] && [ -x "{{.GO_CMD_UNIX}}" ]; then
            go_cmd='{{.GO_CMD_UNIX}}'
          elif command -v go >/dev/null 2>&1; then
            go_cmd=go
          else
            go_cmd='{{.GO_CMD_UNIX}}'
          fi
          "$go_cmd" version
          "$go_cmd" env GOROOT GOPATH
  _verify:windows:
    run: once
    internal: true
    desc: Verify Go on Windows
    summary: Print Go version, GOROOT, and GOPATH on Windows.
    platforms: [windows]
    preconditions:
      - sh: >-
          powershell -NoProfile -ExecutionPolicy Bypass -Command "if (Get-Command go -ErrorAction SilentlyContinue) {
          exit 0 } else { exit 1 }"
        msg: Go is not installed or not available in PATH.
    cmds:
      - go version
      - go env GOROOT GOPATH
