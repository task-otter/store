# module: prettier/node/nvm/npm
# case:   bare

=== task: ci ===
  echo \"--ignore-path .prettierignore\"
" result: ""
fi
task: "_exec:ignore" finished
task: "_exec:ignore" started
task: "_run" finished
task: "_run" started
task: "ci" finished
task: "ci" started
task: "fmt:check" finished
task: "fmt:check" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [fmt:check] echo "✅ prettier passed"
task: [npm:_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- prettier . --check  '
task: dynamic variable: "if [ -n \".prettierignore\" ] && [ -f \".prettierignore\" ]; then

=== task: config:init ===
' > ".prettierrc.json"
' >> ".prettierrc.json"
' >> ".prettierrc.json"
' >> ".prettierrc.json"
task: "config:init" finished
task: "config:init" started
task: [config:init] echo 'Created .prettierrc.json.'
task: [config:init] printf '  "semi": true
task: [config:init] printf '  "singleQuote": true,
task: [config:init] printf '{
task: [config:init] printf '}
task: status command test -f ".prettierrc.json" exited non-zero: exit status 1

=== task: fix ===
  echo \"--ignore-path .prettierignore\"
" result: ""
fi
task: "_exec:ignore" finished
task: "_exec:ignore" started
task: "_run" finished
task: "_run" started
task: "fix" finished
task: "fix" started
task: "fmt" finished
task: "fmt" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [fmt] echo "✅ prettier fixes applied"
task: [npm:_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- prettier . --write  '
task: dynamic variable: "if [ -n \".prettierignore\" ] && [ -f \".prettierignore\" ]; then

=== task: fmt ===
  echo \"--ignore-path .prettierignore\"
" result: ""
fi
task: "_exec:ignore" finished
task: "_exec:ignore" started
task: "_run" finished
task: "_run" started
task: "fmt" finished
task: "fmt" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [fmt] echo "✅ prettier fixes applied"
task: [npm:_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- prettier . --write  '
task: dynamic variable: "if [ -n \".prettierignore\" ] && [ -f \".prettierignore\" ]; then

=== task: fmt:check ===
  echo \"--ignore-path .prettierignore\"
" result: ""
fi
task: "_exec:ignore" finished
task: "_exec:ignore" started
task: "_run" finished
task: "_run" started
task: "fmt:check" finished
task: "fmt:check" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [fmt:check] echo "✅ prettier passed"
task: [npm:_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- prettier . --check  '
task: dynamic variable: "if [ -n \".prettierignore\" ] && [ -f \".prettierignore\" ]; then

=== task: help ===
task: "help" finished
task: "help" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [npm:_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- prettier --help '

=== task: install ===
task: "install" started
task: Task "install" is up to date
task: status command node -e "const installed = require('./node_modules/prettier/package.json'); const requested = ''; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install:undo ===
task: "install:undo" finished
task: "install:undo" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:remove" finished
task: "npm:remove" started
task: [npm:_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm uninstall -g prettier '

=== task: upgrade ===
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:add" finished
task: "npm:add" started
task: "upgrade" finished
task: "upgrade" started
task: [npm:_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm install -g prettier@latest '

=== task: version ===
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: "version" finished
task: "version" started
task: [npm:_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- prettier --version '
