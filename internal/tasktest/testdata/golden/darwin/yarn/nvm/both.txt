# module: yarn/nvm
# case:   both
# vars:
#   NODE_VERSION=BASE-NODE_VERSION
#   NODE_VERSION_OVERRIDE=OVR-NODE_VERSION

=== task: add ===
--- exit: exit status 206
task: Task "add" cancelled because it is missing required variables: PACKAGES

=== task: audit ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "audit" finished
task: "audit" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn npm audit'

=== task: audit:json ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "audit:json" finished
task: "audit:json" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn npm audit --json'

=== task: audit:report ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "audit:report" finished
task: "audit:report" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn npm audit'

=== task: build ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "build" finished
task: "build" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn run build'

=== task: cache:clean ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "cache:clean" finished
task: "cache:clean" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn cache clean'

=== task: ci ===
--- exit: exit status 201
task: "_ci:unix" started
task: "ci" started
task: Failed to run task "ci": task: Failed to run task "_ci:unix": task: precondition not met
task: yarn.lock not found. Yarn CI installs require a lockfile.

=== task: clean ===
task: "_clean:unix" started
task: "_clean:windows" not for current platform - ignored
task: "clean" finished
task: "clean" started
task: Task "_clean:unix" is up to date
task: status command test ! -d node_modules exited zero

=== task: clean:all ===
task: "_clean:all:unix" finished
task: "_clean:all:unix" started
task: "_clean:all:windows" not for current platform - ignored
task: "clean:all" finished
task: "clean:all" started
task: [_clean:all:unix] rm -f yarn.lock
task: [_clean:all:unix] rm -rf node_modules

=== task: dev ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "dev" finished
task: "dev" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn run dev'

=== task: exec ===
--- exit: exit status 206
task: Task "exec" cancelled because it is missing required variables: BINARY

=== task: format ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "format" finished
task: "format" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn run format'

=== task: install ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn install'

=== task: install:undo ===
task: "corepack:_corepack:unix" finished
task: "corepack:_corepack:unix" started
task: "corepack:_corepack:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [corepack:_corepack:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use "BASE-NODE_VERSION"; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run the install task for this Corepack Taskfile."; exit 1; }; corepack disable yarn'

=== task: lint ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "lint" finished
task: "lint" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn run lint'

=== task: manager:pin ===
--- exit: exit status 206
task: Task "manager:pin" cancelled because it is missing required variables: PACKAGE_MANAGER_VERSION

=== task: manager:setup ===
task: "corepack:_corepack:unix" finished
task: "corepack:_corepack:unix" started
task: "corepack:_corepack:windows" not for current platform - ignored
task: "corepack:_install:unix" finished
task: "corepack:_install:unix" started
task: "corepack:_install:windows" not for current platform - ignored
task: "corepack:enable" finished
task: "corepack:enable" started
task: "corepack:install" finished
task: "corepack:install" started
task: "corepack:setup" finished
task: "corepack:setup" started
task: "manager:setup" finished
task: "manager:setup" started
task: [corepack:_corepack:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use "BASE-NODE_VERSION"; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run the install task for this Corepack Taskfile."; exit 1; }; corepack enable'
task: [corepack:_install:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use "BASE-NODE_VERSION"; npm install --global "corepack@0.34.0"'
task: status command bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use "BASE-NODE_VERSION"; command -v corepack >/dev/null 2>&1 && corepack --version | grep -Fqx "0.34.0"' exited non-zero: exit status 1
task: status command test -s "$HOME/.nvm/nvm.sh" exited zero

=== task: node:setup ===

  task nvm:node:install
  task nvm:node:install VERSION="$NODE_VERSION"
else
fi
task: "_node:setup:unix" finished
task: "_node:setup:unix" started
task: "_node:setup:windows" not for current platform - ignored
task: "node:setup" finished
task: "node:setup" started
task: [_node:setup:unix] if [ -n "$NODE_VERSION" ]; then

=== task: remove ===
--- exit: exit status 206
task: Task "remove" cancelled because it is missing required variables: PACKAGES

=== task: run ===
--- exit: exit status 206
task: Task "run" cancelled because it is missing required variables: SCRIPT

=== task: test ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "test" finished
task: "test" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn test'

=== task: typecheck ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "typecheck" finished
task: "typecheck" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn run typecheck'

=== task: update ===
task: "_yarn:unix" finished
task: "_yarn:unix" started
task: "_yarn:windows" not for current platform - ignored
task: "update" finished
task: "update" started
task: [_yarn:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack yarn up '*''

=== task: upgrade ===
task: "corepack:_corepack:unix" finished
task: "corepack:_corepack:unix" started
task: "corepack:_corepack:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [corepack:_corepack:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use "BASE-NODE_VERSION"; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run the install task for this Corepack Taskfile."; exit 1; }; corepack prepare yarn@latest --activate'

=== task: version ===
task: "_version:unix" finished
task: "_version:unix" started
task: "_version:windows" not for current platform - ignored
task: "version" finished
task: "version" started
task: [_version:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; node --version; corepack yarn --version'
