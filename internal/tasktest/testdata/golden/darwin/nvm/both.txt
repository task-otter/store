# module: nvm
# case:   both
# vars:
#   DEFAULT_NODE_VERSION_UNIX=BASE-DEFAULT_NODE_VERSION_UNIX
#   DEFAULT_NODE_VERSION_WINDOWS=BASE-DEFAULT_NODE_VERSION_WINDOWS
#   NVM_SH_VERSION=BASE-NVM_SH_VERSION
#   NVM_WINDOWS_VERSION=BASE-NVM_WINDOWS_VERSION
#   DEFAULT_NODE_VERSION_UNIX_OVERRIDE=OVR-DEFAULT_NODE_VERSION_UNIX
#   DEFAULT_NODE_VERSION_WINDOWS_OVERRIDE=OVR-DEFAULT_NODE_VERSION_WINDOWS
#   NVM_SH_VERSION_OVERRIDE=OVR-NVM_SH_VERSION
#   NVM_WINDOWS_VERSION_OVERRIDE=OVR-NVM_WINDOWS_VERSION

=== task: install ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: [_install:unix] curl -o- "https://raw.githubusercontent.com/nvm-sh/nvm/BASE-NVM_SH_VERSION/install.sh" | bash
task: [_install:unix] echo 'nvm-sh installed. Restart your shell or run . "$HOME/.nvm/nvm.sh" to load nvm in the current session.'
task: [_install:unix] echo Installing nvm-sh BASE-NVM_SH_VERSION...
task: [_install:unix] if condition not met - skipped
task: status command test -s "$HOME/.nvm/nvm.sh" && { expected="BASE-NVM_SH_VERSION"; expected="${expected#v}"; bash -c '. "$HOME/.nvm/nvm.sh"; nvm --version' | grep -Fqx "$expected"; } exited non-zero: exit status 1

=== task: install:undo ===
task: "_install:undo:unix" finished
task: "_install:undo:unix" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:unix] bash -c 'found=$(grep -rl "NVM_DIR\|nvm\.sh" "$HOME/.bashrc" "$HOME/.bash_profile" "$HOME/.profile" "$HOME/.zshrc" 2>/dev/null || true); [ -n "$found" ] && echo "Manually remove NVM_DIR lines from:" && echo "$found" | sed "s/^/  /" || echo "No NVM_DIR references found in shell profile files."'
task: [_install:undo:unix] bash -lc 'export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"; [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" && nvm unload || true'
task: [_install:undo:unix] echo Removed "$HOME/.nvm".
task: [_install:undo:unix] echo Removing nvm-sh from "$HOME/.nvm"...
task: [_install:undo:unix] rm -rf "$HOME/.nvm"
task: status command test ! -d "$HOME/.nvm" exited non-zero: exit status 1

=== task: ls ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_ls:unix" finished
task: "_ls:unix" started
task: "_ls:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "ls" finished
task: "ls" started
task: [_install:unix] curl -o- "https://raw.githubusercontent.com/nvm-sh/nvm/BASE-NVM_SH_VERSION/install.sh" | bash
task: [_install:unix] echo 'nvm-sh installed. Restart your shell or run . "$HOME/.nvm/nvm.sh" to load nvm in the current session.'
task: [_install:unix] echo Installing nvm-sh BASE-NVM_SH_VERSION...
task: [_install:unix] if condition not met - skipped
task: [_ls:unix] bash -lc 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm ls'
task: status command test -s "$HOME/.nvm/nvm.sh" && { expected="BASE-NVM_SH_VERSION"; expected="${expected#v}"; bash -c '. "$HOME/.nvm/nvm.sh"; nvm --version' | grep -Fqx "$expected"; } exited non-zero: exit status 1

=== task: node:install ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_node:install:unix" finished
task: "_node:install:unix" started
task: "_node:install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "node:install" finished
task: "node:install" started
task: [_install:unix] curl -o- "https://raw.githubusercontent.com/nvm-sh/nvm/BASE-NVM_SH_VERSION/install.sh" | bash
task: [_install:unix] echo 'nvm-sh installed. Restart your shell or run . "$HOME/.nvm/nvm.sh" to load nvm in the current session.'
task: [_install:unix] echo Installing nvm-sh BASE-NVM_SH_VERSION...
task: [_install:unix] if condition not met - skipped
task: [_node:install:unix] bash -lc 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm install "$VERSION"'
task: [_node:install:unix] echo Installing Node.js BASE-DEFAULT_NODE_VERSION_UNIX with nvm-sh...
task: status command bash -lc 'if [ "BASE-DEFAULT_NODE_VERSION_UNIX" = "--lts" ] || [ "BASE-DEFAULT_NODE_VERSION_UNIX" = "node" ]; then exit 1; fi; test -d "$HOME/.nvm/versions/node/vBASE-DEFAULT_NODE_VERSION_UNIX"' exited non-zero: exit status 1
task: status command test -s "$HOME/.nvm/nvm.sh" && { expected="BASE-NVM_SH_VERSION"; expected="${expected#v}"; bash -c '. "$HOME/.nvm/nvm.sh"; nvm --version' | grep -Fqx "$expected"; } exited non-zero: exit status 1

=== task: node:uninstall ===
--- exit: exit status 206
task: Task "node:uninstall" cancelled because it is missing required variables: VERSION

=== task: node:use ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_node:install:unix" finished
task: "_node:install:unix" started
task: "_node:install:windows" not for current platform - ignored
task: "_node:use:unix" finished
task: "_node:use:unix" started
task: "_node:use:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "node:install" finished
task: "node:install" started
task: "node:use" finished
task: "node:use" started
task: [_install:unix] curl -o- "https://raw.githubusercontent.com/nvm-sh/nvm/BASE-NVM_SH_VERSION/install.sh" | bash
task: [_install:unix] echo 'nvm-sh installed. Restart your shell or run . "$HOME/.nvm/nvm.sh" to load nvm in the current session.'
task: [_install:unix] echo Installing nvm-sh BASE-NVM_SH_VERSION...
task: [_install:unix] if condition not met - skipped
task: [_node:install:unix] bash -lc 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm install "$VERSION"'
task: [_node:install:unix] echo Installing Node.js BASE-DEFAULT_NODE_VERSION_UNIX with nvm-sh...
task: [_node:use:unix] bash -lc 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use "$VERSION"; nvm alias default "$(nvm current)"'
task: [_node:use:unix] echo 'Node.js BASE-DEFAULT_NODE_VERSION_UNIX set as default for new shells. Run . "$HOME/.nvm/nvm.sh" && nvm use BASE-DEFAULT_NODE_VERSION_UNIX to activate in the current session.'
task: [_node:use:unix] echo Switching to Node.js BASE-DEFAULT_NODE_VERSION_UNIX...
task: status command bash -lc 'if [ "BASE-DEFAULT_NODE_VERSION_UNIX" = "--lts" ] || [ "BASE-DEFAULT_NODE_VERSION_UNIX" = "node" ]; then exit 1; fi; test -d "$HOME/.nvm/versions/node/vBASE-DEFAULT_NODE_VERSION_UNIX"' exited non-zero: exit status 1
task: status command test -s "$HOME/.nvm/nvm.sh" && { expected="BASE-NVM_SH_VERSION"; expected="${expected#v}"; bash -c '. "$HOME/.nvm/nvm.sh"; nvm --version' | grep -Fqx "$expected"; } exited non-zero: exit status 1

=== task: node:version ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_node:version:unix" finished
task: "_node:version:unix" started
task: "_node:version:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "node:version" finished
task: "node:version" started
task: [_install:unix] curl -o- "https://raw.githubusercontent.com/nvm-sh/nvm/BASE-NVM_SH_VERSION/install.sh" | bash
task: [_install:unix] echo 'nvm-sh installed. Restart your shell or run . "$HOME/.nvm/nvm.sh" to load nvm in the current session.'
task: [_install:unix] echo Installing nvm-sh BASE-NVM_SH_VERSION...
task: [_install:unix] if condition not met - skipped
task: [_node:version:unix] bash -lc 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm current; node --version; npm --version'
task: status command test -s "$HOME/.nvm/nvm.sh" && { expected="BASE-NVM_SH_VERSION"; expected="${expected#v}"; bash -c '. "$HOME/.nvm/nvm.sh"; nvm --version' | grep -Fqx "$expected"; } exited non-zero: exit status 1

=== task: version ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_version:unix" finished
task: "_version:unix" started
task: "_version:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "version" finished
task: "version" started
task: [_install:unix] curl -o- "https://raw.githubusercontent.com/nvm-sh/nvm/BASE-NVM_SH_VERSION/install.sh" | bash
task: [_install:unix] echo 'nvm-sh installed. Restart your shell or run . "$HOME/.nvm/nvm.sh" to load nvm in the current session.'
task: [_install:unix] echo Installing nvm-sh BASE-NVM_SH_VERSION...
task: [_install:unix] if condition not met - skipped
task: [_version:unix] bash -lc 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm --version'
task: status command test -s "$HOME/.nvm/nvm.sh" && { expected="BASE-NVM_SH_VERSION"; expected="${expected#v}"; bash -c '. "$HOME/.nvm/nvm.sh"; nvm --version' | grep -Fqx "$expected"; } exited non-zero: exit status 1
