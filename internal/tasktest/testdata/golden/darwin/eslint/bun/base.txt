# module: eslint/bun
# case:   base
# vars:
#   CACHE=false
#   CONFIG=BASE-CONFIG
#   ESLINT_DEFAULT_CACHE=false
#   ESLINT_DEFAULT_TARGETS=BASE-ESLINT_DEFAULT_TARGETS
#   ESLINT_LINT_SKIP_PATTERN=BASE-ESLINT_LINT_SKIP_PATTERN
#   ESLINT_TARGETS=BASE-ESLINT_TARGETS
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   TARGETS=BASE-TARGETS
#   VERSION=BASE-VERSION

=== task: cache:clean ===
task: "cache:clean" finished
task: "cache:clean" started
task: [cache:clean] if [ -d .cache/eslint ]; then rm -rf .cache/eslint; fi

=== task: ci ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "ci" finished
task: "ci" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x eslint --config "BASE-CONFIG"    --max-warnings=0 --ignore-pattern "BASE-ESLINT_LINT_SKIP_PATTERN"  BASE-TARGETS BASE-EXTRA_ARGS'
task: [ci] echo "✅ eslint passed"
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: config:init ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "config:init" finished
task: "config:init" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x eslint --init BASE-EXTRA_ARGS'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
task: status command test -f eslint.config.js || test -f eslint.config.mjs || test -f eslint.config.cjs || test -f .eslintrc.js || test -f .eslintrc.cjs || test -f .eslintrc.json || test -f .eslintrc.yml || test -f .eslintrc.yaml exited non-zero: exit status 1

=== task: help ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "help" finished
task: "help" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x eslint --help BASE-EXTRA_ARGS'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: init ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "config:init" finished
task: "config:init" started
task: "init" finished
task: "init" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x eslint --init BASE-EXTRA_ARGS'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
task: status command test -f eslint.config.js || test -f eslint.config.mjs || test -f eslint.config.cjs || test -f .eslintrc.js || test -f .eslintrc.cjs || test -f .eslintrc.json || test -f .eslintrc.yml || test -f .eslintrc.yaml exited non-zero: exit status 1

=== task: install ===
task: "install" started
task: Task "install" is up to date
task: status command node -e "const installed = require('./node_modules/eslint/package.json'); const requested = 'BASE-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install:undo ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:install" finished
task: "bun:install" started
task: "bun:remove" finished
task: "bun:remove" started
task: "install:undo" finished
task: "install:undo" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun remove -g eslint BASE-EXTRA_ARGS'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: lint ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "lint" finished
task: "lint" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x eslint --config "BASE-CONFIG"    --ignore-pattern "BASE-ESLINT_LINT_SKIP_PATTERN"  BASE-TARGETS BASE-EXTRA_ARGS'
task: [lint] echo "✅ eslint passed"
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: lint:fix ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "lint:fix" finished
task: "lint:fix" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x eslint --config "BASE-CONFIG"    --fix --ignore-pattern "BASE-ESLINT_LINT_SKIP_PATTERN"  BASE-TARGETS BASE-EXTRA_ARGS'
task: [lint:fix] echo "✅ eslint fixes applied"
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: upgrade ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:add" finished
task: "bun:add" started
task: "bun:install" finished
task: "bun:install" started
task: "upgrade" finished
task: "upgrade" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun add -g eslint@latest BASE-EXTRA_ARGS'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: version ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "version" finished
task: "version" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x eslint --version '
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
