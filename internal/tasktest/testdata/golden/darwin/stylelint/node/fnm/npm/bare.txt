# module: stylelint/node/fnm/npm
# case:   bare

=== task: cache:clean ===
task: "cache:clean" finished
task: "cache:clean" started
task: [cache:clean] if [ -d .cache/stylelint ]; then rm -rf .cache/stylelint; fi

=== task: ci ===
task: "_run" finished
task: "_run" started
task: "ci" finished
task: "ci" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [ci] echo "✅ stylelint passed"
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- stylelint **/*.{css,scss,sass,less,vue,svelte,astro} --max-warnings=0 --cache --cache-location .cache/stylelint/ --allow-empty-input '

=== task: config:init ===

  config_file=".stylelintrc.json"
  echo "$config_file already exists."
  echo "Created $config_file."
  printf '%s
' '{' '  "extends": "stylelint-config-standard"' '}' > "$config_file"
else
fi
fi
if [ -e "$config_file" ]; then
if [ -z "$config_file" ]; then
task: "config:init" finished
task: "config:init" started
task: [config:init] config_file=""

=== task: help ===
task: "help" finished
task: "help" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- stylelint --help '

=== task: install ===
task: "install" started
task: Task "install" is up to date
task: status command node -e "const installed = require('./node_modules/stylelint/package.json'); const requested = ''; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install:undo ===
task: "install:undo" finished
task: "install:undo" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:remove" finished
task: "npm:remove" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm uninstall -g stylelint stylelint-config-standard '

=== task: lint ===
task: "_run" finished
task: "_run" started
task: "lint" finished
task: "lint" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [lint] echo "✅ stylelint passed"
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- stylelint **/*.{css,scss,sass,less,vue,svelte,astro} --cache --cache-location .cache/stylelint/ --allow-empty-input '

=== task: lint:fix ===
task: "_run" finished
task: "_run" started
task: "lint:fix" finished
task: "lint:fix" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [lint:fix] echo "✅ stylelint fixes applied"
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- stylelint **/*.{css,scss,sass,less,vue,svelte,astro} --fix --cache --cache-location .cache/stylelint/ --allow-empty-input '

=== task: upgrade ===
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:add" finished
task: "npm:add" started
task: "upgrade" finished
task: "upgrade" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm install -g stylelint@latest stylelint-config-standard@latest '

=== task: version ===
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: "version" finished
task: "version" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- stylelint --version '
