# module: sqlfluff
# case:   both
# vars:
#   CONFIG=BASE-CONFIG
#   DIALECT=BASE-DIALECT
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   SQLFLUFF_INTERNAL_SKIP_CONFIG=BASE-SQLFLUFF_INTERNAL_SKIP_CONFIG
#   SQLFLUFF_LINT_SKIP_PATTERN=BASE-SQLFLUFF_LINT_SKIP_PATTERN
#   SQLFLUFF_VERSION=BASE-SQLFLUFF_VERSION
#   TARGETS=BASE-TARGETS
#   UV_LOAD=BASE-UV_LOAD
#   CONFIG_OVERRIDE=OVR-CONFIG
#   DIALECT_OVERRIDE=OVR-DIALECT
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   SQLFLUFF_INTERNAL_SKIP_CONFIG_OVERRIDE=OVR-SQLFLUFF_INTERNAL_SKIP_CONFIG
#   SQLFLUFF_LINT_SKIP_PATTERN_OVERRIDE=OVR-SQLFLUFF_LINT_SKIP_PATTERN
#   SQLFLUFF_VERSION_OVERRIDE=OVR-SQLFLUFF_VERSION
#   TARGETS_OVERRIDE=OVR-TARGETS
#   UV_LOAD_OVERRIDE=OVR-UV_LOAD

=== task: config:init ===
' > .sqlfluff
' >> .sqlfluff
' >> .sqlfluff
' >> .sqlfluff
' >> .sqlfluff
' >> .sqlfluff
' >> .sqlfluff
task: "config:init" finished
task: "config:init" started
task: [config:init] echo 'Created .sqlfluff with default configuration.'
task: [config:init] printf '
task: [config:init] printf '[sqlfluff:indentation]
task: [config:init] printf '[sqlfluff]
task: [config:init] printf 'dialect = ansi
task: [config:init] printf 'indent_unit = space
task: [config:init] printf 'max_line_length = 120
task: [config:init] printf 'tab_space_size = 4
task: status command test -f .sqlfluff exited non-zero: exit status 1

=== task: config:skip ===





                $keyFound = $true
                $result.Add("ignore_paths = $normalizedPattern")
            $existing = $Matches[1]
            $inSqlfluff = $line -match '^\[sqlfluff\]\s*$'
            $keyFound = $true
            $result.Add("ignore_paths = $existing$separator$normalizedPattern")
            $result.Add($line)
            $separator = if ([string]::IsNullOrWhiteSpace($existing)) { '' } else { ',' }
            if ($inSqlfluff -and -not $keyFound) {
            if ($inSqlfluff) { $sectionFound = $true }
            }
        $result.Add("ignore_paths = $normalizedPattern")
        $result.Add("ignore_paths = $normalizedPattern")
        $result.Add('')
        $result.Add('[sqlfluff]')
        if ($inSqlfluff -and $line -match '^\s*ignore_paths\s*=\s*(.*)$') {
        if ($line -match '^\[[^]]+\]\s*$') {
        print ""
        print "[sqlfluff]"
        print "ignore_paths = " pattern
        }
        }
        } else {
      else if (!section_found) {
      else print "ignore_paths = " value "," pattern
      if (in_sqlfluff && !key_found) print "ignore_paths = " pattern
      if (in_sqlfluff && !key_found) { print "ignore_paths = " pattern; key_found = 1 }
      if (in_sqlfluff) { section_found = 1 }
      if (value == "") print "ignore_paths = " pattern
      in_sqlfluff = ($0 ~ /^\[sqlfluff\][[:space:]]*$/)
      key_found = 1
      next
      sub(/^[^=]*=[[:space:]]*/, "", value)
      value = $0
      }
    $inSqlfluff = $false
    $keyFound = $false
    $lines = @(Get-Content -LiteralPath $sourceConfig)
    $result = [System.Collections.Generic.List[string]]::new()
    $sectionFound = $false
    (Test-Path -LiteralPath $sourceConfig -PathType Leaf)) {
    /^\[[^]]+\][[:space:]]*$/ {
    BEGIN { in_sqlfluff = 0; section_found = 0; key_found = 0 }
    END {
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Set-Content -LiteralPath $output -Encoding UTF8 -Value "[sqlfluff]`nignore_paths = $normalizedPattern"
    Set-Content -LiteralPath $output -Encoding UTF8 -Value $result
    exit 0
    foreach ($line in $lines) {
    if ($inSqlfluff -and -not $keyFound) {
    in_sqlfluff && /^[[:space:]]*ignore_paths[[:space:]]*=/ {
    { print }
    }
    }
    }
    }
    }
    } elseif (-not $sectionFound) {
  ' "$source_config" >"$output"
  awk -v pattern="$pattern" '
  exit 0
  printf '[sqlfluff]
  rm -f "$output"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$normalizedPattern = $pattern.Replace('\', '/')
$output = $env:SQLFLUFF_SKIP_OUTPUT
$pattern = $env:SQLFLUFF_SKIP_PATTERN
$sourceConfig = $env:SQLFLUFF_SKIP_SOURCE_CONFIG
' "$pattern" >"$output"
POWERSHELL
else
fi
fi
if (-not [string]::IsNullOrWhiteSpace($sourceConfig) -and
if ([string]::IsNullOrWhiteSpace($pattern)) {
if [[ -n "$source_config" && -f "$source_config" ]]; then
if [[ -z "$pattern" ]]; then
ignore_paths = %s
output="$SQLFLUFF_SKIP_OUTPUT"
pattern="${pattern//\\//}"
source_config="$SQLFLUFF_SKIP_SOURCE_CONFIG"
task: "config:skip" finished
task: "config:skip" started
task: [config:skip] pattern="$SQLFLUFF_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
}
}
} else {

=== task: fix ===





                $keyFound = $true
                $result.Add("ignore_paths = $normalizedPattern")
            $existing = $Matches[1]
            $inSqlfluff = $line -match '^\[sqlfluff\]\s*$'
            $keyFound = $true
            $result.Add("ignore_paths = $existing$separator$normalizedPattern")
            $result.Add($line)
            $separator = if ([string]::IsNullOrWhiteSpace($existing)) { '' } else { ',' }
            if ($inSqlfluff -and -not $keyFound) {
            if ($inSqlfluff) { $sectionFound = $true }
            }
        $result.Add("ignore_paths = $normalizedPattern")
        $result.Add("ignore_paths = $normalizedPattern")
        $result.Add('')
        $result.Add('[sqlfluff]')
        if ($inSqlfluff -and $line -match '^\s*ignore_paths\s*=\s*(.*)$') {
        if ($line -match '^\[[^]]+\]\s*$') {
        print ""
        print "[sqlfluff]"
        print "ignore_paths = " pattern
        }
        }
        } else {
      else if (!section_found) {
      else print "ignore_paths = " value "," pattern
      if (in_sqlfluff && !key_found) print "ignore_paths = " pattern
      if (in_sqlfluff && !key_found) { print "ignore_paths = " pattern; key_found = 1 }
      if (in_sqlfluff) { section_found = 1 }
      if (value == "") print "ignore_paths = " pattern
      in_sqlfluff = ($0 ~ /^\[sqlfluff\][[:space:]]*$/)
      key_found = 1
      next
      sub(/^[^=]*=[[:space:]]*/, "", value)
      value = $0
      }
    $inSqlfluff = $false
    $keyFound = $false
    $lines = @(Get-Content -LiteralPath $sourceConfig)
    $result = [System.Collections.Generic.List[string]]::new()
    $sectionFound = $false
    (Test-Path -LiteralPath $sourceConfig -PathType Leaf)) {
    /^\[[^]]+\][[:space:]]*$/ {
    BEGIN { in_sqlfluff = 0; section_found = 0; key_found = 0 }
    END {
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Set-Content -LiteralPath $output -Encoding UTF8 -Value "[sqlfluff]`nignore_paths = $normalizedPattern"
    Set-Content -LiteralPath $output -Encoding UTF8 -Value $result
    exit 0
    foreach ($line in $lines) {
    if ($inSqlfluff -and -not $keyFound) {
    in_sqlfluff && /^[[:space:]]*ignore_paths[[:space:]]*=/ {
    { print }
    }
    }
    }
    }
    }
    } elseif (-not $sectionFound) {
  ' "$source_config" >"$output"
  awk -v pattern="$pattern" '
  exit 0
  printf '[sqlfluff]
  rm -f "$output"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$normalizedPattern = $pattern.Replace('\', '/')
$output = $env:SQLFLUFF_SKIP_OUTPUT
$pattern = $env:SQLFLUFF_SKIP_PATTERN
$sourceConfig = $env:SQLFLUFF_SKIP_SOURCE_CONFIG
' "$pattern" >"$output"
POWERSHELL
else
fi
fi
if (-not [string]::IsNullOrWhiteSpace($sourceConfig) -and
if ([string]::IsNullOrWhiteSpace($pattern)) {
if [[ -n "$source_config" && -f "$source_config" ]]; then
if [[ -z "$pattern" ]]; then
ignore_paths = %s
output="$SQLFLUFF_SKIP_OUTPUT"
pattern="${pattern//\\//}"
source_config="$SQLFLUFF_SKIP_SOURCE_CONFIG"
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "config:skip" finished
task: "config:skip" started
task: "fix" finished
task: "fix" started
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -c 'BASE-UV_LOAD; uv tool install --force "sqlfluff==BASE-SQLFLUFF_VERSION"'
task: [_install:unix] echo 'Installing sqlfluff via uv...'
task: [_install:unix] echo 'sqlfluff installed.'
task: [config:skip] pattern="$SQLFLUFF_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [fix] bash -c 'BASE-UV_LOAD; _skip="OVR-SQLFLUFF_LINT_SKIP_PATTERN"; if [ -n "$_skip" ]; then _files="$(mktemp)"; trap "rm -f $_files" EXIT; _targets="OVR-TARGETS"; if [ -n "" ]; then _targets=""; for _argument in ; do if [ -e "$_argument" ]; then _targets="$_targets $_argument"; fi; done; [ -n "$_targets" ] || _targets="OVR-TARGETS"; fi; for _target in $_targets; do if [ -d "$_target" ]; then find "$_target" -type f \( -name "*.sql" -o -name "*.sql.j2" -o -name "*.dml" -o -name "*.ddl" \) -print0; elif [ -f "$_target" ]; then printf "%s\000" "$_target"; fi; done | task --silent --taskfile "$REPO/taskfiles/sqlfluff/../internal/skipfiles/Taskfile.yml" filter "SKIPFILES_PATTERN=$_skip" >"$_files"; if [ ! -s "$_files" ]; then echo "No matching SQL files found"; exit 0; fi; xargs -0 sqlfluff fix  --dialect OVR-DIALECT  OVR-EXTRA_ARGS --config "BASE-SQLFLUFF_INTERNAL_SKIP_CONFIG" <"$_files" && echo "✅ sqlfluff fixes applied" || exit 1; exit; fi; sqlfluff fix  --dialect OVR-DIALECT  OVR-TARGETS OVR-EXTRA_ARGS  --config "BASE-SQLFLUFF_INTERNAL_SKIP_CONFIG"  && echo "✅ sqlfluff fixes applied"'
task: status command bash -c 'BASE-UV_LOAD; sqlfluff --version | grep -q "version BASE-SQLFLUFF_VERSION$"' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero
}
}
} else {

=== task: install ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -c 'BASE-UV_LOAD; uv tool install --force "sqlfluff==BASE-SQLFLUFF_VERSION"'
task: [_install:unix] echo 'Installing sqlfluff via uv...'
task: [_install:unix] echo 'sqlfluff installed.'
task: status command bash -c 'BASE-UV_LOAD; sqlfluff --version | grep -q "version BASE-SQLFLUFF_VERSION$"' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: install:undo ===
task: "_install:undo:unix" finished
task: "_install:undo:unix" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:unix] bash -c 'BASE-UV_LOAD; uv tool uninstall sqlfluff'
task: [_install:undo:unix] echo 'Removing sqlfluff...'
task: [_install:undo:unix] echo 'sqlfluff removed.'
task: status command bash -c 'BASE-UV_LOAD; ! command -v sqlfluff >/dev/null 2>&1' exited non-zero: exit status 1

=== task: lint ===





                $keyFound = $true
                $result.Add("ignore_paths = $normalizedPattern")
            $existing = $Matches[1]
            $inSqlfluff = $line -match '^\[sqlfluff\]\s*$'
            $keyFound = $true
            $result.Add("ignore_paths = $existing$separator$normalizedPattern")
            $result.Add($line)
            $separator = if ([string]::IsNullOrWhiteSpace($existing)) { '' } else { ',' }
            if ($inSqlfluff -and -not $keyFound) {
            if ($inSqlfluff) { $sectionFound = $true }
            }
        $result.Add("ignore_paths = $normalizedPattern")
        $result.Add("ignore_paths = $normalizedPattern")
        $result.Add('')
        $result.Add('[sqlfluff]')
        if ($inSqlfluff -and $line -match '^\s*ignore_paths\s*=\s*(.*)$') {
        if ($line -match '^\[[^]]+\]\s*$') {
        print ""
        print "[sqlfluff]"
        print "ignore_paths = " pattern
        }
        }
        } else {
      else if (!section_found) {
      else print "ignore_paths = " value "," pattern
      if (in_sqlfluff && !key_found) print "ignore_paths = " pattern
      if (in_sqlfluff && !key_found) { print "ignore_paths = " pattern; key_found = 1 }
      if (in_sqlfluff) { section_found = 1 }
      if (value == "") print "ignore_paths = " pattern
      in_sqlfluff = ($0 ~ /^\[sqlfluff\][[:space:]]*$/)
      key_found = 1
      next
      sub(/^[^=]*=[[:space:]]*/, "", value)
      value = $0
      }
    $inSqlfluff = $false
    $keyFound = $false
    $lines = @(Get-Content -LiteralPath $sourceConfig)
    $result = [System.Collections.Generic.List[string]]::new()
    $sectionFound = $false
    (Test-Path -LiteralPath $sourceConfig -PathType Leaf)) {
    /^\[[^]]+\][[:space:]]*$/ {
    BEGIN { in_sqlfluff = 0; section_found = 0; key_found = 0 }
    END {
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Set-Content -LiteralPath $output -Encoding UTF8 -Value "[sqlfluff]`nignore_paths = $normalizedPattern"
    Set-Content -LiteralPath $output -Encoding UTF8 -Value $result
    exit 0
    foreach ($line in $lines) {
    if ($inSqlfluff -and -not $keyFound) {
    in_sqlfluff && /^[[:space:]]*ignore_paths[[:space:]]*=/ {
    { print }
    }
    }
    }
    }
    }
    } elseif (-not $sectionFound) {
  ' "$source_config" >"$output"
  awk -v pattern="$pattern" '
  exit 0
  printf '[sqlfluff]
  rm -f "$output"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$normalizedPattern = $pattern.Replace('\', '/')
$output = $env:SQLFLUFF_SKIP_OUTPUT
$pattern = $env:SQLFLUFF_SKIP_PATTERN
$sourceConfig = $env:SQLFLUFF_SKIP_SOURCE_CONFIG
' "$pattern" >"$output"
POWERSHELL
else
fi
fi
if (-not [string]::IsNullOrWhiteSpace($sourceConfig) -and
if ([string]::IsNullOrWhiteSpace($pattern)) {
if [[ -n "$source_config" && -f "$source_config" ]]; then
if [[ -z "$pattern" ]]; then
ignore_paths = %s
output="$SQLFLUFF_SKIP_OUTPUT"
pattern="${pattern//\\//}"
source_config="$SQLFLUFF_SKIP_SOURCE_CONFIG"
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "config:skip" finished
task: "config:skip" started
task: "install" finished
task: "install" started
task: "lint" finished
task: "lint" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -c 'BASE-UV_LOAD; uv tool install --force "sqlfluff==BASE-SQLFLUFF_VERSION"'
task: [_install:unix] echo 'Installing sqlfluff via uv...'
task: [_install:unix] echo 'sqlfluff installed.'
task: [config:skip] pattern="$SQLFLUFF_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [lint] bash -c 'BASE-UV_LOAD; _skip="OVR-SQLFLUFF_LINT_SKIP_PATTERN"; if [ -n "$_skip" ]; then _files="$(mktemp)"; trap "rm -f $_files" EXIT; _targets="OVR-TARGETS"; if [ -n "" ]; then _targets=""; for _argument in ; do if [ -e "$_argument" ]; then _targets="$_targets $_argument"; fi; done; [ -n "$_targets" ] || _targets="OVR-TARGETS"; fi; for _target in $_targets; do if [ -d "$_target" ]; then find "$_target" -type f \( -name "*.sql" -o -name "*.sql.j2" -o -name "*.dml" -o -name "*.ddl" \) -print0; elif [ -f "$_target" ]; then printf "%s\000" "$_target"; fi; done | task --silent --taskfile "$REPO/taskfiles/sqlfluff/../internal/skipfiles/Taskfile.yml" filter "SKIPFILES_PATTERN=$_skip" >"$_files"; if [ ! -s "$_files" ]; then echo "No matching SQL files found"; exit 0; fi; xargs -0 sqlfluff lint  --dialect OVR-DIALECT  OVR-EXTRA_ARGS --config "BASE-SQLFLUFF_INTERNAL_SKIP_CONFIG" <"$_files" && echo "✅ sqlfluff passed" || { echo "❌ sqlfluff failed"; exit 1; }; exit; fi; sqlfluff lint  --dialect OVR-DIALECT  OVR-TARGETS OVR-EXTRA_ARGS  --config "BASE-SQLFLUFF_INTERNAL_SKIP_CONFIG"  && echo "✅ sqlfluff passed" || { echo "❌ sqlfluff failed"; exit 1; }'
task: status command bash -c 'BASE-UV_LOAD; sqlfluff --version | grep -q "version BASE-SQLFLUFF_VERSION$"' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero
}
}
} else {

=== task: parse ===





                $keyFound = $true
                $result.Add("ignore_paths = $normalizedPattern")
            $existing = $Matches[1]
            $inSqlfluff = $line -match '^\[sqlfluff\]\s*$'
            $keyFound = $true
            $result.Add("ignore_paths = $existing$separator$normalizedPattern")
            $result.Add($line)
            $separator = if ([string]::IsNullOrWhiteSpace($existing)) { '' } else { ',' }
            if ($inSqlfluff -and -not $keyFound) {
            if ($inSqlfluff) { $sectionFound = $true }
            }
        $result.Add("ignore_paths = $normalizedPattern")
        $result.Add("ignore_paths = $normalizedPattern")
        $result.Add('')
        $result.Add('[sqlfluff]')
        if ($inSqlfluff -and $line -match '^\s*ignore_paths\s*=\s*(.*)$') {
        if ($line -match '^\[[^]]+\]\s*$') {
        print ""
        print "[sqlfluff]"
        print "ignore_paths = " pattern
        }
        }
        } else {
      else if (!section_found) {
      else print "ignore_paths = " value "," pattern
      if (in_sqlfluff && !key_found) print "ignore_paths = " pattern
      if (in_sqlfluff && !key_found) { print "ignore_paths = " pattern; key_found = 1 }
      if (in_sqlfluff) { section_found = 1 }
      if (value == "") print "ignore_paths = " pattern
      in_sqlfluff = ($0 ~ /^\[sqlfluff\][[:space:]]*$/)
      key_found = 1
      next
      sub(/^[^=]*=[[:space:]]*/, "", value)
      value = $0
      }
    $inSqlfluff = $false
    $keyFound = $false
    $lines = @(Get-Content -LiteralPath $sourceConfig)
    $result = [System.Collections.Generic.List[string]]::new()
    $sectionFound = $false
    (Test-Path -LiteralPath $sourceConfig -PathType Leaf)) {
    /^\[[^]]+\][[:space:]]*$/ {
    BEGIN { in_sqlfluff = 0; section_found = 0; key_found = 0 }
    END {
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Set-Content -LiteralPath $output -Encoding UTF8 -Value "[sqlfluff]`nignore_paths = $normalizedPattern"
    Set-Content -LiteralPath $output -Encoding UTF8 -Value $result
    exit 0
    foreach ($line in $lines) {
    if ($inSqlfluff -and -not $keyFound) {
    in_sqlfluff && /^[[:space:]]*ignore_paths[[:space:]]*=/ {
    { print }
    }
    }
    }
    }
    }
    } elseif (-not $sectionFound) {
  ' "$source_config" >"$output"
  awk -v pattern="$pattern" '
  exit 0
  printf '[sqlfluff]
  rm -f "$output"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$normalizedPattern = $pattern.Replace('\', '/')
$output = $env:SQLFLUFF_SKIP_OUTPUT
$pattern = $env:SQLFLUFF_SKIP_PATTERN
$sourceConfig = $env:SQLFLUFF_SKIP_SOURCE_CONFIG
' "$pattern" >"$output"
POWERSHELL
else
fi
fi
if (-not [string]::IsNullOrWhiteSpace($sourceConfig) -and
if ([string]::IsNullOrWhiteSpace($pattern)) {
if [[ -n "$source_config" && -f "$source_config" ]]; then
if [[ -z "$pattern" ]]; then
ignore_paths = %s
output="$SQLFLUFF_SKIP_OUTPUT"
pattern="${pattern//\\//}"
source_config="$SQLFLUFF_SKIP_SOURCE_CONFIG"
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "config:skip" finished
task: "config:skip" started
task: "install" finished
task: "install" started
task: "parse" finished
task: "parse" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -c 'BASE-UV_LOAD; uv tool install --force "sqlfluff==BASE-SQLFLUFF_VERSION"'
task: [_install:unix] echo 'Installing sqlfluff via uv...'
task: [_install:unix] echo 'sqlfluff installed.'
task: [config:skip] pattern="$SQLFLUFF_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [parse] bash -c 'BASE-UV_LOAD; _skip="OVR-SQLFLUFF_LINT_SKIP_PATTERN"; if [ -n "$_skip" ]; then _files="$(mktemp)"; trap "rm -f $_files" EXIT; _targets="OVR-TARGETS"; if [ -n "" ]; then _targets=""; for _argument in ; do if [ -e "$_argument" ]; then _targets="$_targets $_argument"; fi; done; [ -n "$_targets" ] || _targets="OVR-TARGETS"; fi; for _target in $_targets; do if [ -d "$_target" ]; then find "$_target" -type f \( -name "*.sql" -o -name "*.sql.j2" -o -name "*.dml" -o -name "*.ddl" \) -print0; elif [ -f "$_target" ]; then printf "%s\000" "$_target"; fi; done | task --silent --taskfile "$REPO/taskfiles/sqlfluff/../internal/skipfiles/Taskfile.yml" filter "SKIPFILES_PATTERN=$_skip" >"$_files"; if [ ! -s "$_files" ]; then echo "No matching SQL files found"; exit 0; fi; xargs -0 sqlfluff parse  --dialect OVR-DIALECT  OVR-EXTRA_ARGS --config "BASE-SQLFLUFF_INTERNAL_SKIP_CONFIG" <"$_files"; exit; fi; sqlfluff parse  --dialect OVR-DIALECT  OVR-TARGETS OVR-EXTRA_ARGS  --config "BASE-SQLFLUFF_INTERNAL_SKIP_CONFIG" '
task: status command bash -c 'BASE-UV_LOAD; sqlfluff --version | grep -q "version BASE-SQLFLUFF_VERSION$"' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero
}
}
} else {

=== task: upgrade ===
task: "_upgrade:unix" finished
task: "_upgrade:unix" started
task: "_upgrade:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [_upgrade:unix] bash -c 'BASE-UV_LOAD; uv tool install --force "sqlfluff==BASE-SQLFLUFF_VERSION"'
task: [_upgrade:unix] echo 'Upgrading sqlfluff via uv...'
task: [_upgrade:unix] echo 'sqlfluff upgraded.'

=== task: version ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: "version" finished
task: "version" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -c 'BASE-UV_LOAD; uv tool install --force "sqlfluff==BASE-SQLFLUFF_VERSION"'
task: [_install:unix] echo 'Installing sqlfluff via uv...'
task: [_install:unix] echo 'sqlfluff installed.'
task: [version] bash -c 'BASE-UV_LOAD; sqlfluff --version'
task: status command bash -c 'BASE-UV_LOAD; sqlfluff --version | grep -q "version BASE-SQLFLUFF_VERSION$"' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero
