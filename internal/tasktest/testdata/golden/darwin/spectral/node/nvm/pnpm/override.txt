# module: spectral/node/nvm/pnpm
# case:   override
# vars:
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   RULESET_OVERRIDE=OVR-RULESET
#   SPECTRAL_LINT_SKIP_PATTERN_OVERRIDE=OVR-SPECTRAL_LINT_SKIP_PATTERN
#   TARGETS_OVERRIDE=OVR-TARGETS
#   VERSION_OVERRIDE=OVR-VERSION

=== task: config:init ===
' > .spectral.yaml
task: "config:init" finished
task: "config:init" started
task: [config:init] echo 'Created .spectral.yaml extending spectral:oas.'
task: [config:init] printf 'extends: ["spectral:oas"]
task: status command test -f .spectral.yaml exited non-zero: exit status 1

=== task: help ===
task: "help" finished
task: "help" started
task: "install" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: Task "install" is up to date
task: [pnpm:_exec:unix] bash -c "spectral --help "
task: status command node -e "const installed = require('./node_modules/@stoplight/spectral-cli/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install ===
task: "install" started
task: Task "install" is up to date
task: status command node -e "const installed = require('./node_modules/@stoplight/spectral-cli/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install:undo ===
task: "install:undo" finished
task: "install:undo" started
task: "pnpm:_pnpm:unix" finished
task: "pnpm:_pnpm:unix" started
task: "pnpm:_pnpm:windows" not for current platform - ignored
task: "pnpm:remove" finished
task: "pnpm:remove" started
task: [pnpm:_pnpm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack pnpm remove -g @stoplight/spectral-cli OVR-EXTRA_ARGS'

=== task: lint ===
task: "install" started
task: "lint" finished
task: "lint" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: Task "install" is up to date
task: [lint] echo "✅ spectral passed"
task: [pnpm:_exec:unix] bash -c "spectral lint OVR-TARGETS "!OVR-SPECTRAL_LINT_SKIP_PATTERN" --ruleset "OVR-RULESET" OVR-EXTRA_ARGS"
task: status command node -e "const installed = require('./node_modules/@stoplight/spectral-cli/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: upgrade ===
task: "pnpm:_pnpm:unix" finished
task: "pnpm:_pnpm:unix" started
task: "pnpm:_pnpm:windows" not for current platform - ignored
task: "pnpm:add" finished
task: "pnpm:add" started
task: "upgrade" finished
task: "upgrade" started
task: [pnpm:_pnpm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack pnpm add -g @stoplight/spectral-cli@latest OVR-EXTRA_ARGS'

=== task: version ===
task: "install" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: "version" finished
task: "version" started
task: Task "install" is up to date
task: [pnpm:_exec:unix] bash -c "spectral --version "
task: status command node -e "const installed = require('./node_modules/@stoplight/spectral-cli/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero
