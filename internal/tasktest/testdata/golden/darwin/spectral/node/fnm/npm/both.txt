# module: spectral/node/fnm/npm
# case:   both
# vars:
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   RULESET=BASE-RULESET
#   SPECTRAL_LINT_SKIP_PATTERN=BASE-SPECTRAL_LINT_SKIP_PATTERN
#   TARGETS=BASE-TARGETS
#   VERSION=BASE-VERSION
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   RULESET_OVERRIDE=OVR-RULESET
#   SPECTRAL_LINT_SKIP_PATTERN_OVERRIDE=OVR-SPECTRAL_LINT_SKIP_PATTERN
#   TARGETS_OVERRIDE=OVR-TARGETS
#   VERSION_OVERRIDE=OVR-VERSION

=== task: config:init ===
' > .spectral.yaml
task: "config:init" finished
task: "config:init" started
task: [config:init] echo 'Created .spectral.yaml extending spectral:oas.'
task: [config:init] printf 'extends: ["spectral:oas"]
task: status command test -f .spectral.yaml exited non-zero: exit status 1

=== task: help ===
task: "help" finished
task: "help" started
task: "install" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: Task "install" is up to date
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- spectral --help '
task: status command node -e "const installed = require('./node_modules/@stoplight/spectral-cli/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install ===
task: "install" started
task: Task "install" is up to date
task: status command node -e "const installed = require('./node_modules/@stoplight/spectral-cli/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install:undo ===
task: "install:undo" finished
task: "install:undo" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:remove" finished
task: "npm:remove" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm uninstall -g @stoplight/spectral-cli OVR-EXTRA_ARGS'

=== task: lint ===
task: "install" started
task: "lint" finished
task: "lint" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: Task "install" is up to date
task: [lint] echo "✅ spectral passed"
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- spectral lint OVR-TARGETS "!OVR-SPECTRAL_LINT_SKIP_PATTERN" --ruleset "OVR-RULESET" OVR-EXTRA_ARGS'
task: status command node -e "const installed = require('./node_modules/@stoplight/spectral-cli/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: upgrade ===
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:add" finished
task: "npm:add" started
task: "upgrade" finished
task: "upgrade" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm install -g @stoplight/spectral-cli@latest OVR-EXTRA_ARGS'

=== task: version ===
task: "install" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: "version" finished
task: "version" started
task: Task "install" is up to date
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- spectral --version '
task: status command node -e "const installed = require('./node_modules/@stoplight/spectral-cli/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero
