# module: uv
# case:   base
# vars:
#   ARGS=BASE-ARGS
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   FILE=BASE-FILE
#   PYTHON_VERSION=BASE-PYTHON_VERSION
#   REQUIREMENTS=BASE-REQUIREMENTS
#   TOOL=BASE-TOOL
#   UV_INSTALL_URL=BASE-UV_INSTALL_URL
#   UV_INSTALL_URL_WINDOWS=BASE-UV_INSTALL_URL_WINDOWS
#   UV_LOAD=BASE-UV_LOAD
#   UV_VERSION=BASE-UV_VERSION
#   VENV=BASE-VENV

=== task: install ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: [_install:unix] curl -LsSf "https://astral.sh/uv/BASE-UV_VERSION/install.sh" | sh
task: [_install:unix] echo 'Installing uv via the official install script...'
task: [_install:unix] echo 'uv installed. Restart your shell or tasks will use UV_LOAD to locate the binary.'
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "BASE-UV_VERSION" ] || "$uv_bin" --version | grep -Fqx "uv BASE-UV_VERSION"; } exited non-zero: exit status 1

=== task: install:undo ===
task: "_install:undo:unix" finished
task: "_install:undo:unix" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:unix] bash -lc 'BASE-UV_LOAD; uv self uninstall'
task: [_install:undo:unix] echo 'Removing uv...'
task: [_install:undo:unix] echo 'uv removed.'
task: status command ! test -f "$HOME/.local/bin/uv" && ! command -v uv >/dev/null 2>&1 exited non-zero: exit status 1

=== task: pip:install ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_uv:unix" finished
task: "_uv:unix" started
task: "_uv:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "pip:install" finished
task: "pip:install" started
task: [_install:unix] curl -LsSf "https://astral.sh/uv/BASE-UV_VERSION/install.sh" | sh
task: [_install:unix] echo 'Installing uv via the official install script...'
task: [_install:unix] echo 'uv installed. Restart your shell or tasks will use UV_LOAD to locate the binary.'
task: [_uv:unix] bash -lc 'BASE-UV_LOAD; uv pip install -r "BASE-REQUIREMENTS" BASE-EXTRA_ARGS'
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "BASE-UV_VERSION" ] || "$uv_bin" --version | grep -Fqx "uv BASE-UV_VERSION"; } exited non-zero: exit status 1

=== task: python:install ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_uv:unix" finished
task: "_uv:unix" started
task: "_uv:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "python:install" finished
task: "python:install" started
task: [_install:unix] curl -LsSf "https://astral.sh/uv/BASE-UV_VERSION/install.sh" | sh
task: [_install:unix] echo 'Installing uv via the official install script...'
task: [_install:unix] echo 'uv installed. Restart your shell or tasks will use UV_LOAD to locate the binary.'
task: [_uv:unix] bash -lc 'BASE-UV_LOAD; uv python install BASE-PYTHON_VERSION'
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "BASE-UV_VERSION" ] || "$uv_bin" --version | grep -Fqx "uv BASE-UV_VERSION"; } exited non-zero: exit status 1

=== task: run ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_uv:unix" finished
task: "_uv:unix" started
task: "_uv:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "run" finished
task: "run" started
task: [_install:unix] curl -LsSf "https://astral.sh/uv/BASE-UV_VERSION/install.sh" | sh
task: [_install:unix] echo 'Installing uv via the official install script...'
task: [_install:unix] echo 'uv installed. Restart your shell or tasks will use UV_LOAD to locate the binary.'
task: [_uv:unix] bash -lc 'BASE-UV_LOAD; uv run "BASE-FILE" BASE-ARGS BASE-EXTRA_ARGS'
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "BASE-UV_VERSION" ] || "$uv_bin" --version | grep -Fqx "uv BASE-UV_VERSION"; } exited non-zero: exit status 1

=== task: tool:install ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_uv:unix" finished
task: "_uv:unix" started
task: "_uv:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "tool:install" finished
task: "tool:install" started
task: [_install:unix] curl -LsSf "https://astral.sh/uv/BASE-UV_VERSION/install.sh" | sh
task: [_install:unix] echo 'Installing uv via the official install script...'
task: [_install:unix] echo 'uv installed. Restart your shell or tasks will use UV_LOAD to locate the binary.'
task: [_uv:unix] bash -lc 'BASE-UV_LOAD; uv tool install "BASE-TOOL" BASE-EXTRA_ARGS'
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "BASE-UV_VERSION" ] || "$uv_bin" --version | grep -Fqx "uv BASE-UV_VERSION"; } exited non-zero: exit status 1

=== task: tool:upgrade ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_uv:unix" finished
task: "_uv:unix" started
task: "_uv:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "tool:upgrade" finished
task: "tool:upgrade" started
task: [_install:unix] curl -LsSf "https://astral.sh/uv/BASE-UV_VERSION/install.sh" | sh
task: [_install:unix] echo 'Installing uv via the official install script...'
task: [_install:unix] echo 'uv installed. Restart your shell or tasks will use UV_LOAD to locate the binary.'
task: [_uv:unix] bash -lc 'BASE-UV_LOAD; uv tool upgrade "BASE-TOOL" BASE-EXTRA_ARGS'
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "BASE-UV_VERSION" ] || "$uv_bin" --version | grep -Fqx "uv BASE-UV_VERSION"; } exited non-zero: exit status 1

=== task: upgrade ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_upgrade:unix" finished
task: "_upgrade:unix" started
task: "_upgrade:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "upgrade" finished
task: "upgrade" started
task: [_install:unix] curl -LsSf "https://astral.sh/uv/BASE-UV_VERSION/install.sh" | sh
task: [_install:unix] echo 'Installing uv via the official install script...'
task: [_install:unix] echo 'uv installed. Restart your shell or tasks will use UV_LOAD to locate the binary.'
task: [_upgrade:unix] bash -lc 'BASE-UV_LOAD; uv self update'
task: [_upgrade:unix] echo 'Upgrading uv...'
task: [_upgrade:unix] echo 'uv upgraded.'
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "BASE-UV_VERSION" ] || "$uv_bin" --version | grep -Fqx "uv BASE-UV_VERSION"; } exited non-zero: exit status 1

=== task: venv ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_uv:unix" finished
task: "_uv:unix" started
task: "_uv:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "venv" finished
task: "venv" started
task: [_install:unix] curl -LsSf "https://astral.sh/uv/BASE-UV_VERSION/install.sh" | sh
task: [_install:unix] echo 'Installing uv via the official install script...'
task: [_install:unix] echo 'uv installed. Restart your shell or tasks will use UV_LOAD to locate the binary.'
task: [_uv:unix] bash -lc 'BASE-UV_LOAD; uv venv "BASE-VENV" BASE-EXTRA_ARGS'
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "BASE-UV_VERSION" ] || "$uv_bin" --version | grep -Fqx "uv BASE-UV_VERSION"; } exited non-zero: exit status 1

=== task: version ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_version:unix" finished
task: "_version:unix" started
task: "_version:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "version" finished
task: "version" started
task: [_install:unix] curl -LsSf "https://astral.sh/uv/BASE-UV_VERSION/install.sh" | sh
task: [_install:unix] echo 'Installing uv via the official install script...'
task: [_install:unix] echo 'uv installed. Restart your shell or tasks will use UV_LOAD to locate the binary.'
task: [_version:unix] bash -lc 'BASE-UV_LOAD; uv --version'
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "BASE-UV_VERSION" ] || "$uv_bin" --version | grep -Fqx "uv BASE-UV_VERSION"; } exited non-zero: exit status 1
