# module: proto
# case:   base
# vars:
#   GO_MODULE=BASE-GO_MODULE
#   PROTOC_GEN_GO_GRPC_VERSION=BASE-PROTOC_GEN_GO_GRPC_VERSION
#   PROTOC_GEN_GO_VERSION=BASE-PROTOC_GEN_GO_VERSION
#   PROTOC_VERSION=BASE-PROTOC_VERSION
#   PROTO_PATH=BASE-PROTO_PATH
#   PROTO_PATTERN=BASE-PROTO_PATTERN

=== task: gen ===

    gopath=\"$(go env GOPATH)\"
  ! -path "*/.git/*" -print -quit)" ]; then
  --go-grpc_out=. ${module_opts} \
  --go_out=. ${module_opts} \
  -exec protoc --proto_path="BASE-PROTO_PATH" \
  -name "BASE-PROTO_PATTERN" \
  command -v go
  echo "No .proto files found under BASE-PROTO_PATH matching -name BASE-PROTO_PATTERN"
  exit 1
  fi
  gobin=\"$(go env GOBIN)\"
  gobin=\"${gopath:-$HOME/go}/bin\"
  gopath=\"${GOPATH:-}\"
  if [ -z \"$gopath\" ] && command -v go >/dev/null 2>&1; then
  module_opts="--go_opt=module=${GO_MODULE} --go-grpc_opt=module=${GO_MODULE}"
  printf '%s' \"/usr/local/go/bin/go\"
  printf '%s' \"go\"
  {} +
" result: "$HOME/go/bin"
" result: "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go"
elif [ \"$(uname -s 2>/dev/null || true)\" = \"Linux\" ]; then
else
fi
fi
fi
fi
fi
find "BASE-PROTO_PATH" -type f -name "BASE-PROTO_PATTERN" ! -path "*/.git/*" \
if [ -n "${GO_MODULE:-}" ]; then
if [ -z "$(find "BASE-PROTO_PATH" -type f \
if [ -z \"$gobin\" ] && command -v go >/dev/null 2>&1; then
if [ -z \"$gobin\" ]; then
module_opts=""
printf '%s' \"$gobin\"
task: "_gen:unix" finished
task: "_gen:unix" started
task: "_gen:windows" not for current platform - ignored
task: "_install:plugins" finished
task: "_install:plugins" started
task: "_install:protoc:linux" not for current platform - ignored
task: "_install:protoc:macos" finished
task: "_install:protoc:macos" started
task: "_install:protoc:windows" not for current platform - ignored
task: "gen" finished
task: "gen" started
task: "go:_install:linux:tarball" not for current platform - ignored
task: "go:_install:macos:brew" started
task: "go:_install:macos:pkg" started
task: "go:_install:windows:msi" not for current platform - ignored
task: "go:_shell:setup:linux" not for current platform - ignored
task: "go:install" finished
task: "go:install" started
task: "install" finished
task: "install" started
task: Task "go:_install:macos:brew" is up to date
task: Task "go:_install:macos:pkg" is up to date
task: [_gen:unix] export PATH="$HOME/go/bin:$PATH"
task: [_install:plugins] GOBIN="$HOME/go/bin" "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go" install google.golang.org/grpc/cmd/protoc-gen-go-grpc@BASE-PROTOC_GEN_GO_GRPC_VERSION
task: [_install:plugins] GOBIN="$HOME/go/bin" "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go" install google.golang.org/protobuf/cmd/protoc-gen-go@BASE-PROTOC_GEN_GO_VERSION
task: [_install:plugins] echo 'protoc Go plugins installed.'
task: [_install:plugins] mkdir -p "$HOME/go/bin"
task: [_install:protoc:macos] brew install protobuf
task: [_install:protoc:macos] echo 'Installing protoc via Homebrew...'
task: [_install:protoc:macos] echo 'protoc installed.'
task: dynamic variable: "gobin=\"${GOBIN:-}\"
task: dynamic variable: "if command -v go >/dev/null 2>&1; then
task: dynamic variable: "uname -m" result: "arm64"
task: status command [ -n "" ] || command -v go >/dev/null 2>&1 exited zero
task: status command [ -z "" ] || { [ "false" != "true" ] && test -x "/usr/local/go/bin/go" && [ "$("/usr/local/go/bin/go" version | awk '{print $3}')" = "" ]; } exited zero
task: status command command -v protoc >/dev/null 2>&1 && protoc --version | grep -Fqx "libprotoc BASE-PROTOC_VERSION" exited non-zero: exit status 1
task: status command test -x "$HOME/go/bin/protoc-gen-go" exited non-zero: exit status 1

=== task: install ===
    gopath=\"$(go env GOPATH)\"
  command -v go
  fi
  gobin=\"$(go env GOBIN)\"
  gobin=\"${gopath:-$HOME/go}/bin\"
  gopath=\"${GOPATH:-}\"
  if [ -z \"$gopath\" ] && command -v go >/dev/null 2>&1; then
  printf '%s' \"/usr/local/go/bin/go\"
  printf '%s' \"go\"
" result: "$HOME/go/bin"
" result: "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go"
elif [ \"$(uname -s 2>/dev/null || true)\" = \"Linux\" ]; then
else
fi
fi
fi
if [ -z \"$gobin\" ] && command -v go >/dev/null 2>&1; then
if [ -z \"$gobin\" ]; then
printf '%s' \"$gobin\"
task: "_install:plugins" finished
task: "_install:plugins" started
task: "_install:protoc:linux" not for current platform - ignored
task: "_install:protoc:macos" finished
task: "_install:protoc:macos" started
task: "_install:protoc:windows" not for current platform - ignored
task: "go:_install:linux:tarball" not for current platform - ignored
task: "go:_install:macos:brew" started
task: "go:_install:macos:pkg" started
task: "go:_install:windows:msi" not for current platform - ignored
task: "go:_shell:setup:linux" not for current platform - ignored
task: "go:install" finished
task: "go:install" started
task: "install" finished
task: "install" started
task: Task "go:_install:macos:brew" is up to date
task: Task "go:_install:macos:pkg" is up to date
task: [_install:plugins] GOBIN="$HOME/go/bin" "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go" install google.golang.org/grpc/cmd/protoc-gen-go-grpc@BASE-PROTOC_GEN_GO_GRPC_VERSION
task: [_install:plugins] GOBIN="$HOME/go/bin" "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go" install google.golang.org/protobuf/cmd/protoc-gen-go@BASE-PROTOC_GEN_GO_VERSION
task: [_install:plugins] echo 'protoc Go plugins installed.'
task: [_install:plugins] mkdir -p "$HOME/go/bin"
task: [_install:protoc:macos] brew install protobuf
task: [_install:protoc:macos] echo 'Installing protoc via Homebrew...'
task: [_install:protoc:macos] echo 'protoc installed.'
task: dynamic variable: "gobin=\"${GOBIN:-}\"
task: dynamic variable: "if command -v go >/dev/null 2>&1; then
task: dynamic variable: "uname -m" result: "arm64"
task: status command [ -n "" ] || command -v go >/dev/null 2>&1 exited zero
task: status command [ -z "" ] || { [ "false" != "true" ] && test -x "/usr/local/go/bin/go" && [ "$("/usr/local/go/bin/go" version | awk '{print $3}')" = "" ]; } exited zero
task: status command command -v protoc >/dev/null 2>&1 && protoc --version | grep -Fqx "libprotoc BASE-PROTOC_VERSION" exited non-zero: exit status 1
task: status command test -x "$HOME/go/bin/protoc-gen-go" exited non-zero: exit status 1

=== task: install:undo ===
    gopath=\"$(go env GOPATH)\"
  command -v go
  fi
  gobin=\"$(go env GOBIN)\"
  gobin=\"${gopath:-$HOME/go}/bin\"
  gopath=\"${GOPATH:-}\"
  if [ -z \"$gopath\" ] && command -v go >/dev/null 2>&1; then
  printf '%s' \"/usr/local/go/bin/go\"
  printf '%s' \"go\"
" result: "$HOME/go/bin"
" result: "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go"
elif [ \"$(uname -s 2>/dev/null || true)\" = \"Linux\" ]; then
else
fi
fi
fi
if [ -z \"$gobin\" ] && command -v go >/dev/null 2>&1; then
if [ -z \"$gobin\" ]; then
printf '%s' \"$gobin\"
task: "_install:undo:plugins" finished
task: "_install:undo:plugins" started
task: "_install:undo:plugins:unix" started
task: "_install:undo:plugins:windows" not for current platform - ignored
task: "_install:undo:protoc:linux" not for current platform - ignored
task: "_install:undo:protoc:macos" finished
task: "_install:undo:protoc:macos" started
task: "_install:undo:protoc:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: Task "_install:undo:plugins:unix" is up to date
task: [_install:undo:protoc:macos] brew uninstall protobuf
task: [_install:undo:protoc:macos] echo 'Removing protoc via Homebrew...'
task: [_install:undo:protoc:macos] echo 'protoc removed.'
task: dynamic variable: "gobin=\"${GOBIN:-}\"
task: dynamic variable: "if command -v go >/dev/null 2>&1; then
task: status command ! command -v protoc >/dev/null 2>&1 exited non-zero: exit status 1
task: status command ! test -x '$HOME/go/bin/protoc-gen-go' exited zero
task: status command ! test -x '$HOME/go/bin/protoc-gen-go-grpc' exited zero

=== task: ungen ===
    gopath=\"$(go env GOPATH)\"
  command -v go
  fi
  gobin=\"$(go env GOBIN)\"
  gobin=\"${gopath:-$HOME/go}/bin\"
  gopath=\"${GOPATH:-}\"
  if [ -z \"$gopath\" ] && command -v go >/dev/null 2>&1; then
  printf '%s' \"/usr/local/go/bin/go\"
  printf '%s' \"go\"
" result: "$HOME/go/bin"
" result: "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go"
elif [ \"$(uname -s 2>/dev/null || true)\" = \"Linux\" ]; then
else
fi
fi
fi
if [ -z \"$gobin\" ] && command -v go >/dev/null 2>&1; then
if [ -z \"$gobin\" ]; then
printf '%s' \"$gobin\"
task: "_ungen:unix" finished
task: "_ungen:unix" started
task: "_ungen:windows" not for current platform - ignored
task: "ungen" finished
task: "ungen" started
task: [_ungen:unix] echo 'Generated protobuf files removed.'
task: [_ungen:unix] find . -type f -name "*.pb.go" -delete
task: dynamic variable: "gobin=\"${GOBIN:-}\"
task: dynamic variable: "if command -v go >/dev/null 2>&1; then

=== task: upgrade ===
    gopath=\"$(go env GOPATH)\"
  command -v go
  fi
  gobin=\"$(go env GOBIN)\"
  gobin=\"${gopath:-$HOME/go}/bin\"
  gopath=\"${GOPATH:-}\"
  if [ -z \"$gopath\" ] && command -v go >/dev/null 2>&1; then
  printf '%s' \"/usr/local/go/bin/go\"
  printf '%s' \"go\"
" result: "$HOME/go/bin"
" result: "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go"
elif [ \"$(uname -s 2>/dev/null || true)\" = \"Linux\" ]; then
else
fi
fi
fi
if [ -z \"$gobin\" ] && command -v go >/dev/null 2>&1; then
if [ -z \"$gobin\" ]; then
printf '%s' \"$gobin\"
task: "_install:plugins" finished
task: "_install:plugins" started
task: "_upgrade:protoc:linux" not for current platform - ignored
task: "_upgrade:protoc:macos" finished
task: "_upgrade:protoc:macos" started
task: "_upgrade:protoc:windows" not for current platform - ignored
task: "go:_install:linux:tarball" not for current platform - ignored
task: "go:_install:macos:brew" started
task: "go:_install:macos:pkg" started
task: "go:_install:windows:msi" not for current platform - ignored
task: "go:_shell:setup:linux" not for current platform - ignored
task: "go:install" finished
task: "go:install" started
task: "upgrade" finished
task: "upgrade" started
task: Task "go:_install:macos:brew" is up to date
task: Task "go:_install:macos:pkg" is up to date
task: [_install:plugins] GOBIN="$HOME/go/bin" "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go" install google.golang.org/grpc/cmd/protoc-gen-go-grpc@BASE-PROTOC_GEN_GO_GRPC_VERSION
task: [_install:plugins] GOBIN="$HOME/go/bin" "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go" install google.golang.org/protobuf/cmd/protoc-gen-go@BASE-PROTOC_GEN_GO_VERSION
task: [_install:plugins] echo 'protoc Go plugins installed.'
task: [_install:plugins] mkdir -p "$HOME/go/bin"
task: [_upgrade:protoc:macos] brew upgrade protobuf
task: [_upgrade:protoc:macos] echo 'Upgrading protoc via Homebrew...'
task: [_upgrade:protoc:macos] echo 'protoc upgraded.'
task: [_upgrade:protoc:macos] if condition not met - skipped
task: dynamic variable: "gobin=\"${GOBIN:-}\"
task: dynamic variable: "if command -v go >/dev/null 2>&1; then
task: dynamic variable: "uname -m" result: "arm64"
task: status command [ -n "" ] || command -v go >/dev/null 2>&1 exited zero
task: status command [ -z "" ] || { [ "false" != "true" ] && test -x "/usr/local/go/bin/go" && [ "$("/usr/local/go/bin/go" version | awk '{print $3}')" = "" ]; } exited zero
task: status command test -x "$HOME/go/bin/protoc-gen-go" exited non-zero: exit status 1

=== task: version ===
    gopath=\"$(go env GOPATH)\"
  command -v go
  fi
  gobin=\"$(go env GOBIN)\"
  gobin=\"${gopath:-$HOME/go}/bin\"
  gopath=\"${GOPATH:-}\"
  if [ -z \"$gopath\" ] && command -v go >/dev/null 2>&1; then
  printf '%s' \"/usr/local/go/bin/go\"
  printf '%s' \"go\"
" result: "$HOME/go/bin"
" result: "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go"
elif [ \"$(uname -s 2>/dev/null || true)\" = \"Linux\" ]; then
else
fi
fi
fi
if [ -z \"$gobin\" ] && command -v go >/dev/null 2>&1; then
if [ -z \"$gobin\" ]; then
printf '%s' \"$gobin\"
task: "_install:plugins" finished
task: "_install:plugins" started
task: "_install:protoc:linux" not for current platform - ignored
task: "_install:protoc:macos" finished
task: "_install:protoc:macos" started
task: "_install:protoc:windows" not for current platform - ignored
task: "go:_install:linux:tarball" not for current platform - ignored
task: "go:_install:macos:brew" started
task: "go:_install:macos:pkg" started
task: "go:_install:windows:msi" not for current platform - ignored
task: "go:_shell:setup:linux" not for current platform - ignored
task: "go:install" finished
task: "go:install" started
task: "install" finished
task: "install" started
task: "version" finished
task: "version" started
task: Task "go:_install:macos:brew" is up to date
task: Task "go:_install:macos:pkg" is up to date
task: [_install:plugins] GOBIN="$HOME/go/bin" "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go" install google.golang.org/grpc/cmd/protoc-gen-go-grpc@BASE-PROTOC_GEN_GO_GRPC_VERSION
task: [_install:plugins] GOBIN="$HOME/go/bin" "/opt/homebrew/Cellar/go/1.26.5/libexec/bin/go" install google.golang.org/protobuf/cmd/protoc-gen-go@BASE-PROTOC_GEN_GO_VERSION
task: [_install:plugins] echo 'protoc Go plugins installed.'
task: [_install:plugins] mkdir -p "$HOME/go/bin"
task: [_install:protoc:macos] brew install protobuf
task: [_install:protoc:macos] echo 'Installing protoc via Homebrew...'
task: [_install:protoc:macos] echo 'protoc installed.'
task: [version] protoc --version
task: dynamic variable: "gobin=\"${GOBIN:-}\"
task: dynamic variable: "if command -v go >/dev/null 2>&1; then
task: dynamic variable: "uname -m" result: "arm64"
task: status command [ -n "" ] || command -v go >/dev/null 2>&1 exited zero
task: status command [ -z "" ] || { [ "false" != "true" ] && test -x "/usr/local/go/bin/go" && [ "$("/usr/local/go/bin/go" version | awk '{print $3}')" = "" ]; } exited zero
task: status command command -v protoc >/dev/null 2>&1 && protoc --version | grep -Fqx "libprotoc BASE-PROTOC_VERSION" exited non-zero: exit status 1
task: status command test -x "$HOME/go/bin/protoc-gen-go" exited non-zero: exit status 1
