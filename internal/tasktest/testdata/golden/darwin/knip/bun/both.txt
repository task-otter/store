# module: knip/bun
# case:   both
# vars:
#   CONFIG=BASE-CONFIG
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   KNIP_INTERNAL_JS_RUNTIME=BASE-KNIP_INTERNAL_JS_RUNTIME
#   KNIP_INTERNAL_SKIP_CONFIG=BASE-KNIP_INTERNAL_SKIP_CONFIG
#   KNIP_LINT_SKIP_PATTERN=BASE-KNIP_LINT_SKIP_PATTERN
#   PM=BASE-PM
#   VERSION=BASE-VERSION
#   CONFIG_OVERRIDE=OVR-CONFIG
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   KNIP_INTERNAL_JS_RUNTIME_OVERRIDE=OVR-KNIP_INTERNAL_JS_RUNTIME
#   KNIP_INTERNAL_SKIP_CONFIG_OVERRIDE=OVR-KNIP_INTERNAL_SKIP_CONFIG
#   KNIP_LINT_SKIP_PATTERN_OVERRIDE=OVR-KNIP_LINT_SKIP_PATTERN
#   PM_OVERRIDE=OVR-PM
#   VERSION_OVERRIDE=OVR-VERSION

=== task: ci ===







        'Add the skip pattern to the config ignore array or use JSON/JSONC.'
        'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config. ' +
        'knip.config.mjs', 'knip.config.cjs', 'package.json'
        'knip.json', 'knip.jsonc', '.knip.json', '.knip.jsonc',
        'knip.ts', 'knip.js', 'knip.config.ts', 'knip.config.js',
        continue;
        continue;
        else if (character === "\"") inString = false;
        else if (character === "\\") escaped = true;
        if (Test-Path -LiteralPath $candidate -PathType Leaf) { return $candidate }
        if (character === "
        if (character === "*" && next === "/") { blockComment = false; index += 1; }
        if (escaped) escaped = false;
      'Add the skip pattern to the config ignore array or use JSON/JSONC.' >&2
      'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config.' \
      blockComment = true; index += 1; continue;
      const character = source[index], next = source[index + 1];
      continue;
      continue;
      else if (character === "\"") inString = false;
      else if (character === "\\") escaped = true;
      if (!inString && character === "/" && next === "*") { blockComment = true; index += 1; continue; }
      if (!inString && character === "/" && next === "/") { lineComment = true; index += 1; continue; }
      if (blockComment) {
      if (character === "
      if (character === "*" && next === "/") { blockComment = false; index += 1; }
      if (escaped) escaped = false;
      if (inString) {
      if (lineComment) {
      lineComment = true; index += 1; continue;
      output += character;
      printf '%s' "$candidate"
      return
      }
      }
      } else if (character === "\"") inString = true;
    $sourceConfig = Find-Config @(
    )
    )
    ;;
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Write-Error (
    config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
    const character = source[index], next = source[index + 1];
    const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
    exit 0
    exit 2
    exit 2
    fi
    for (let index = 0; index < source.length; index += 1) {
    foreach ($candidate in $candidates) {
    if (!inString && character === "/" && next === "*") {
    if (!inString && character === "/" && next === "/") {
    if (blockComment) {
    if (inString) {
    if (lineComment) {
    if [[ -f "$candidate" ]]; then
    knip.config.mjs knip.config.cjs package.json || true)"
    knip.json knip.jsonc .knip.json .knip.jsonc \
    knip.ts knip.js knip.config.ts knip.config.js \
    let lineComment = false, blockComment = false;
    let output = "", inString = false, escaped = false;
    output += character;
    printf '%s
    return ''
    return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
    }
    }
    }
    }
    }
    }
    } else if (character === "\"") inString = true;
  config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
  config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
  const fs = require("node:fs");
  const parseJsonc = source => {
  const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
  const path = require("node:path");
  const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
  const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
  done
  exit 0
  for (let index = 0; index < source.length; index += 1) {
  for candidate in "$@"; do
  fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
  if (sourcePath) {
  js|mjs|cjs|ts)
  let config = {};
  let lineComment = false, blockComment = false;
  let output = "", inString = false, escaped = false;
  local candidate
  return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
  rm -f "$output"
  source_config="$(discover_config \
  }
  }
  };
 not for current platform - ignored
"$KNIP_SKIP_JS_RUNTIME" -e '
") { lineComment = false; output += character; }
") { lineComment = false; output += character; }
");
");
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$code = @'
$env:KNIP_SKIP_SOURCE_CONFIG = $sourceConfig
$output = $env:KNIP_SKIP_OUTPUT
$pattern = $env:KNIP_SKIP_PATTERN
$sourceConfig = $env:KNIP_SKIP_SOURCE_CONFIG
& $env:KNIP_SKIP_JS_RUNTIME -e $code
'
' \
'@
POWERSHELL
case "${source_config##*.}" in
config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
const fs = require("node:fs");
const parseJsonc = source => {
const path = require("node:path");
const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
discover_config() {
esac
exit $LASTEXITCODE
export KNIP_SKIP_SOURCE_CONFIG="$source_config"
fi
fi
fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
function Find-Config([string[]]$candidates) {
if ($sourceConfig -match '\.(?:[cm]?js|ts)$') {
if ([string]::IsNullOrWhiteSpace($pattern)) {
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if (sourcePath) {
if [[ -z "$pattern" ]]; then
if [[ -z "$source_config" ]]; then
let config = {};
output="$KNIP_SKIP_OUTPUT"
source_config="$KNIP_SKIP_SOURCE_CONFIG"
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "ci" finished
task: "ci" started
task: "config:skip" finished
task: "config:skip" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --production --config "BASE-KNIP_INTERNAL_SKIP_CONFIG" OVR-EXTRA_ARGS'
task: [ci] echo "✅ knip passed"
task: [config:skip] pattern="$KNIP_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
}
}
}
}
}
}
};

=== task: config:init ===
' '{' '  "$schema": "https://unpkg.com/knip@5/schema.json"' '}' > knip.json
task: "config:init" finished
task: "config:init" started
task: "init" finished
task: "init" started
task: [init] echo "✅ created knip.json"
task: [init] printf '%s
task: status command test -f knip.json || test -f knip.jsonc exited non-zero: exit status 1

=== task: config:skip ===







        'Add the skip pattern to the config ignore array or use JSON/JSONC.'
        'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config. ' +
        'knip.config.mjs', 'knip.config.cjs', 'package.json'
        'knip.json', 'knip.jsonc', '.knip.json', '.knip.jsonc',
        'knip.ts', 'knip.js', 'knip.config.ts', 'knip.config.js',
        continue;
        continue;
        else if (character === "\"") inString = false;
        else if (character === "\\") escaped = true;
        if (Test-Path -LiteralPath $candidate -PathType Leaf) { return $candidate }
        if (character === "
        if (character === "*" && next === "/") { blockComment = false; index += 1; }
        if (escaped) escaped = false;
      'Add the skip pattern to the config ignore array or use JSON/JSONC.' >&2
      'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config.' \
      blockComment = true; index += 1; continue;
      const character = source[index], next = source[index + 1];
      continue;
      continue;
      else if (character === "\"") inString = false;
      else if (character === "\\") escaped = true;
      if (!inString && character === "/" && next === "*") { blockComment = true; index += 1; continue; }
      if (!inString && character === "/" && next === "/") { lineComment = true; index += 1; continue; }
      if (blockComment) {
      if (character === "
      if (character === "*" && next === "/") { blockComment = false; index += 1; }
      if (escaped) escaped = false;
      if (inString) {
      if (lineComment) {
      lineComment = true; index += 1; continue;
      output += character;
      printf '%s' "$candidate"
      return
      }
      }
      } else if (character === "\"") inString = true;
    $sourceConfig = Find-Config @(
    )
    )
    ;;
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Write-Error (
    config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
    const character = source[index], next = source[index + 1];
    const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
    exit 0
    exit 2
    exit 2
    fi
    for (let index = 0; index < source.length; index += 1) {
    foreach ($candidate in $candidates) {
    if (!inString && character === "/" && next === "*") {
    if (!inString && character === "/" && next === "/") {
    if (blockComment) {
    if (inString) {
    if (lineComment) {
    if [[ -f "$candidate" ]]; then
    knip.config.mjs knip.config.cjs package.json || true)"
    knip.json knip.jsonc .knip.json .knip.jsonc \
    knip.ts knip.js knip.config.ts knip.config.js \
    let lineComment = false, blockComment = false;
    let output = "", inString = false, escaped = false;
    output += character;
    printf '%s
    return ''
    return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
    }
    }
    }
    }
    }
    }
    } else if (character === "\"") inString = true;
  config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
  config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
  const fs = require("node:fs");
  const parseJsonc = source => {
  const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
  const path = require("node:path");
  const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
  const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
  done
  exit 0
  for (let index = 0; index < source.length; index += 1) {
  for candidate in "$@"; do
  fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
  if (sourcePath) {
  js|mjs|cjs|ts)
  let config = {};
  let lineComment = false, blockComment = false;
  let output = "", inString = false, escaped = false;
  local candidate
  return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
  rm -f "$output"
  source_config="$(discover_config \
  }
  }
  };
 not for current platform - ignored
"$KNIP_SKIP_JS_RUNTIME" -e '
") { lineComment = false; output += character; }
") { lineComment = false; output += character; }
");
");
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$code = @'
$env:KNIP_SKIP_SOURCE_CONFIG = $sourceConfig
$output = $env:KNIP_SKIP_OUTPUT
$pattern = $env:KNIP_SKIP_PATTERN
$sourceConfig = $env:KNIP_SKIP_SOURCE_CONFIG
& $env:KNIP_SKIP_JS_RUNTIME -e $code
'
' \
'@
POWERSHELL
case "${source_config##*.}" in
config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
const fs = require("node:fs");
const parseJsonc = source => {
const path = require("node:path");
const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
discover_config() {
esac
exit $LASTEXITCODE
export KNIP_SKIP_SOURCE_CONFIG="$source_config"
fi
fi
fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
function Find-Config([string[]]$candidates) {
if ($sourceConfig -match '\.(?:[cm]?js|ts)$') {
if ([string]::IsNullOrWhiteSpace($pattern)) {
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if (sourcePath) {
if [[ -z "$pattern" ]]; then
if [[ -z "$source_config" ]]; then
let config = {};
output="$KNIP_SKIP_OUTPUT"
source_config="$KNIP_SKIP_SOURCE_CONFIG"
task: "config:skip" finished
task: "config:skip" started
task: [config:skip] pattern="$KNIP_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
}
}
}
}
}
}
};

=== task: dependencies ===







        'Add the skip pattern to the config ignore array or use JSON/JSONC.'
        'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config. ' +
        'knip.config.mjs', 'knip.config.cjs', 'package.json'
        'knip.json', 'knip.jsonc', '.knip.json', '.knip.jsonc',
        'knip.ts', 'knip.js', 'knip.config.ts', 'knip.config.js',
        continue;
        continue;
        else if (character === "\"") inString = false;
        else if (character === "\\") escaped = true;
        if (Test-Path -LiteralPath $candidate -PathType Leaf) { return $candidate }
        if (character === "
        if (character === "*" && next === "/") { blockComment = false; index += 1; }
        if (escaped) escaped = false;
      'Add the skip pattern to the config ignore array or use JSON/JSONC.' >&2
      'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config.' \
      blockComment = true; index += 1; continue;
      const character = source[index], next = source[index + 1];
      continue;
      continue;
      else if (character === "\"") inString = false;
      else if (character === "\\") escaped = true;
      if (!inString && character === "/" && next === "*") { blockComment = true; index += 1; continue; }
      if (!inString && character === "/" && next === "/") { lineComment = true; index += 1; continue; }
      if (blockComment) {
      if (character === "
      if (character === "*" && next === "/") { blockComment = false; index += 1; }
      if (escaped) escaped = false;
      if (inString) {
      if (lineComment) {
      lineComment = true; index += 1; continue;
      output += character;
      printf '%s' "$candidate"
      return
      }
      }
      } else if (character === "\"") inString = true;
    $sourceConfig = Find-Config @(
    )
    )
    ;;
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Write-Error (
    config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
    const character = source[index], next = source[index + 1];
    const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
    exit 0
    exit 2
    exit 2
    fi
    for (let index = 0; index < source.length; index += 1) {
    foreach ($candidate in $candidates) {
    if (!inString && character === "/" && next === "*") {
    if (!inString && character === "/" && next === "/") {
    if (blockComment) {
    if (inString) {
    if (lineComment) {
    if [[ -f "$candidate" ]]; then
    knip.config.mjs knip.config.cjs package.json || true)"
    knip.json knip.jsonc .knip.json .knip.jsonc \
    knip.ts knip.js knip.config.ts knip.config.js \
    let lineComment = false, blockComment = false;
    let output = "", inString = false, escaped = false;
    output += character;
    printf '%s
    return ''
    return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
    }
    }
    }
    }
    }
    }
    } else if (character === "\"") inString = true;
  config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
  config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
  const fs = require("node:fs");
  const parseJsonc = source => {
  const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
  const path = require("node:path");
  const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
  const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
  done
  exit 0
  for (let index = 0; index < source.length; index += 1) {
  for candidate in "$@"; do
  fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
  if (sourcePath) {
  js|mjs|cjs|ts)
  let config = {};
  let lineComment = false, blockComment = false;
  let output = "", inString = false, escaped = false;
  local candidate
  return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
  rm -f "$output"
  source_config="$(discover_config \
  }
  }
  };
 not for current platform - ignored
"$KNIP_SKIP_JS_RUNTIME" -e '
") { lineComment = false; output += character; }
") { lineComment = false; output += character; }
");
");
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$code = @'
$env:KNIP_SKIP_SOURCE_CONFIG = $sourceConfig
$output = $env:KNIP_SKIP_OUTPUT
$pattern = $env:KNIP_SKIP_PATTERN
$sourceConfig = $env:KNIP_SKIP_SOURCE_CONFIG
& $env:KNIP_SKIP_JS_RUNTIME -e $code
'
' \
'@
POWERSHELL
case "${source_config##*.}" in
config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
const fs = require("node:fs");
const parseJsonc = source => {
const path = require("node:path");
const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
discover_config() {
esac
exit $LASTEXITCODE
export KNIP_SKIP_SOURCE_CONFIG="$source_config"
fi
fi
fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
function Find-Config([string[]]$candidates) {
if ($sourceConfig -match '\.(?:[cm]?js|ts)$') {
if ([string]::IsNullOrWhiteSpace($pattern)) {
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if (sourcePath) {
if [[ -z "$pattern" ]]; then
if [[ -z "$source_config" ]]; then
let config = {};
output="$KNIP_SKIP_OUTPUT"
source_config="$KNIP_SKIP_SOURCE_CONFIG"
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "config:skip" finished
task: "config:skip" started
task: "dependencies" finished
task: "dependencies" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --dependencies --config "BASE-KNIP_INTERNAL_SKIP_CONFIG" OVR-EXTRA_ARGS'
task: [config:skip] pattern="$KNIP_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
}
}
}
}
}
}
};

=== task: dev-dependencies ===







        'Add the skip pattern to the config ignore array or use JSON/JSONC.'
        'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config. ' +
        'knip.config.mjs', 'knip.config.cjs', 'package.json'
        'knip.json', 'knip.jsonc', '.knip.json', '.knip.jsonc',
        'knip.ts', 'knip.js', 'knip.config.ts', 'knip.config.js',
        continue;
        continue;
        else if (character === "\"") inString = false;
        else if (character === "\\") escaped = true;
        if (Test-Path -LiteralPath $candidate -PathType Leaf) { return $candidate }
        if (character === "
        if (character === "*" && next === "/") { blockComment = false; index += 1; }
        if (escaped) escaped = false;
      'Add the skip pattern to the config ignore array or use JSON/JSONC.' >&2
      'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config.' \
      blockComment = true; index += 1; continue;
      const character = source[index], next = source[index + 1];
      continue;
      continue;
      else if (character === "\"") inString = false;
      else if (character === "\\") escaped = true;
      if (!inString && character === "/" && next === "*") { blockComment = true; index += 1; continue; }
      if (!inString && character === "/" && next === "/") { lineComment = true; index += 1; continue; }
      if (blockComment) {
      if (character === "
      if (character === "*" && next === "/") { blockComment = false; index += 1; }
      if (escaped) escaped = false;
      if (inString) {
      if (lineComment) {
      lineComment = true; index += 1; continue;
      output += character;
      printf '%s' "$candidate"
      return
      }
      }
      } else if (character === "\"") inString = true;
    $sourceConfig = Find-Config @(
    )
    )
    ;;
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Write-Error (
    config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
    const character = source[index], next = source[index + 1];
    const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
    exit 0
    exit 2
    exit 2
    fi
    for (let index = 0; index < source.length; index += 1) {
    foreach ($candidate in $candidates) {
    if (!inString && character === "/" && next === "*") {
    if (!inString && character === "/" && next === "/") {
    if (blockComment) {
    if (inString) {
    if (lineComment) {
    if [[ -f "$candidate" ]]; then
    knip.config.mjs knip.config.cjs package.json || true)"
    knip.json knip.jsonc .knip.json .knip.jsonc \
    knip.ts knip.js knip.config.ts knip.config.js \
    let lineComment = false, blockComment = false;
    let output = "", inString = false, escaped = false;
    output += character;
    printf '%s
    return ''
    return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
    }
    }
    }
    }
    }
    }
    } else if (character === "\"") inString = true;
  config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
  config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
  const fs = require("node:fs");
  const parseJsonc = source => {
  const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
  const path = require("node:path");
  const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
  const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
  done
  exit 0
  for (let index = 0; index < source.length; index += 1) {
  for candidate in "$@"; do
  fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
  if (sourcePath) {
  js|mjs|cjs|ts)
  let config = {};
  let lineComment = false, blockComment = false;
  let output = "", inString = false, escaped = false;
  local candidate
  return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
  rm -f "$output"
  source_config="$(discover_config \
  }
  }
  };
 not for current platform - ignored
"$KNIP_SKIP_JS_RUNTIME" -e '
") { lineComment = false; output += character; }
") { lineComment = false; output += character; }
");
");
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$code = @'
$env:KNIP_SKIP_SOURCE_CONFIG = $sourceConfig
$output = $env:KNIP_SKIP_OUTPUT
$pattern = $env:KNIP_SKIP_PATTERN
$sourceConfig = $env:KNIP_SKIP_SOURCE_CONFIG
& $env:KNIP_SKIP_JS_RUNTIME -e $code
'
' \
'@
POWERSHELL
case "${source_config##*.}" in
config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
const fs = require("node:fs");
const parseJsonc = source => {
const path = require("node:path");
const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
discover_config() {
esac
exit $LASTEXITCODE
export KNIP_SKIP_SOURCE_CONFIG="$source_config"
fi
fi
fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
function Find-Config([string[]]$candidates) {
if ($sourceConfig -match '\.(?:[cm]?js|ts)$') {
if ([string]::IsNullOrWhiteSpace($pattern)) {
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if (sourcePath) {
if [[ -z "$pattern" ]]; then
if [[ -z "$source_config" ]]; then
let config = {};
output="$KNIP_SKIP_OUTPUT"
source_config="$KNIP_SKIP_SOURCE_CONFIG"
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "config:skip" finished
task: "config:skip" started
task: "dev-dependencies" finished
task: "dev-dependencies" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --devDependencies --config "BASE-KNIP_INTERNAL_SKIP_CONFIG" OVR-EXTRA_ARGS'
task: [config:skip] pattern="$KNIP_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
}
}
}
}
}
}
};

=== task: exports ===







        'Add the skip pattern to the config ignore array or use JSON/JSONC.'
        'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config. ' +
        'knip.config.mjs', 'knip.config.cjs', 'package.json'
        'knip.json', 'knip.jsonc', '.knip.json', '.knip.jsonc',
        'knip.ts', 'knip.js', 'knip.config.ts', 'knip.config.js',
        continue;
        continue;
        else if (character === "\"") inString = false;
        else if (character === "\\") escaped = true;
        if (Test-Path -LiteralPath $candidate -PathType Leaf) { return $candidate }
        if (character === "
        if (character === "*" && next === "/") { blockComment = false; index += 1; }
        if (escaped) escaped = false;
      'Add the skip pattern to the config ignore array or use JSON/JSONC.' >&2
      'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config.' \
      blockComment = true; index += 1; continue;
      const character = source[index], next = source[index + 1];
      continue;
      continue;
      else if (character === "\"") inString = false;
      else if (character === "\\") escaped = true;
      if (!inString && character === "/" && next === "*") { blockComment = true; index += 1; continue; }
      if (!inString && character === "/" && next === "/") { lineComment = true; index += 1; continue; }
      if (blockComment) {
      if (character === "
      if (character === "*" && next === "/") { blockComment = false; index += 1; }
      if (escaped) escaped = false;
      if (inString) {
      if (lineComment) {
      lineComment = true; index += 1; continue;
      output += character;
      printf '%s' "$candidate"
      return
      }
      }
      } else if (character === "\"") inString = true;
    $sourceConfig = Find-Config @(
    )
    )
    ;;
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Write-Error (
    config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
    const character = source[index], next = source[index + 1];
    const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
    exit 0
    exit 2
    exit 2
    fi
    for (let index = 0; index < source.length; index += 1) {
    foreach ($candidate in $candidates) {
    if (!inString && character === "/" && next === "*") {
    if (!inString && character === "/" && next === "/") {
    if (blockComment) {
    if (inString) {
    if (lineComment) {
    if [[ -f "$candidate" ]]; then
    knip.config.mjs knip.config.cjs package.json || true)"
    knip.json knip.jsonc .knip.json .knip.jsonc \
    knip.ts knip.js knip.config.ts knip.config.js \
    let lineComment = false, blockComment = false;
    let output = "", inString = false, escaped = false;
    output += character;
    printf '%s
    return ''
    return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
    }
    }
    }
    }
    }
    }
    } else if (character === "\"") inString = true;
  config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
  config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
  const fs = require("node:fs");
  const parseJsonc = source => {
  const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
  const path = require("node:path");
  const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
  const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
  done
  exit 0
  for (let index = 0; index < source.length; index += 1) {
  for candidate in "$@"; do
  fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
  if (sourcePath) {
  js|mjs|cjs|ts)
  let config = {};
  let lineComment = false, blockComment = false;
  let output = "", inString = false, escaped = false;
  local candidate
  return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
  rm -f "$output"
  source_config="$(discover_config \
  }
  }
  };
 not for current platform - ignored
"$KNIP_SKIP_JS_RUNTIME" -e '
") { lineComment = false; output += character; }
") { lineComment = false; output += character; }
");
");
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$code = @'
$env:KNIP_SKIP_SOURCE_CONFIG = $sourceConfig
$output = $env:KNIP_SKIP_OUTPUT
$pattern = $env:KNIP_SKIP_PATTERN
$sourceConfig = $env:KNIP_SKIP_SOURCE_CONFIG
& $env:KNIP_SKIP_JS_RUNTIME -e $code
'
' \
'@
POWERSHELL
case "${source_config##*.}" in
config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
const fs = require("node:fs");
const parseJsonc = source => {
const path = require("node:path");
const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
discover_config() {
esac
exit $LASTEXITCODE
export KNIP_SKIP_SOURCE_CONFIG="$source_config"
fi
fi
fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
function Find-Config([string[]]$candidates) {
if ($sourceConfig -match '\.(?:[cm]?js|ts)$') {
if ([string]::IsNullOrWhiteSpace($pattern)) {
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if (sourcePath) {
if [[ -z "$pattern" ]]; then
if [[ -z "$source_config" ]]; then
let config = {};
output="$KNIP_SKIP_OUTPUT"
source_config="$KNIP_SKIP_SOURCE_CONFIG"
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "config:skip" finished
task: "config:skip" started
task: "exports" finished
task: "exports" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --exports --config "BASE-KNIP_INTERNAL_SKIP_CONFIG" OVR-EXTRA_ARGS'
task: [config:skip] pattern="$KNIP_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
}
}
}
}
}
}
};

=== task: files ===







        'Add the skip pattern to the config ignore array or use JSON/JSONC.'
        'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config. ' +
        'knip.config.mjs', 'knip.config.cjs', 'package.json'
        'knip.json', 'knip.jsonc', '.knip.json', '.knip.jsonc',
        'knip.ts', 'knip.js', 'knip.config.ts', 'knip.config.js',
        continue;
        continue;
        else if (character === "\"") inString = false;
        else if (character === "\\") escaped = true;
        if (Test-Path -LiteralPath $candidate -PathType Leaf) { return $candidate }
        if (character === "
        if (character === "*" && next === "/") { blockComment = false; index += 1; }
        if (escaped) escaped = false;
      'Add the skip pattern to the config ignore array or use JSON/JSONC.' >&2
      'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config.' \
      blockComment = true; index += 1; continue;
      const character = source[index], next = source[index + 1];
      continue;
      continue;
      else if (character === "\"") inString = false;
      else if (character === "\\") escaped = true;
      if (!inString && character === "/" && next === "*") { blockComment = true; index += 1; continue; }
      if (!inString && character === "/" && next === "/") { lineComment = true; index += 1; continue; }
      if (blockComment) {
      if (character === "
      if (character === "*" && next === "/") { blockComment = false; index += 1; }
      if (escaped) escaped = false;
      if (inString) {
      if (lineComment) {
      lineComment = true; index += 1; continue;
      output += character;
      printf '%s' "$candidate"
      return
      }
      }
      } else if (character === "\"") inString = true;
    $sourceConfig = Find-Config @(
    )
    )
    ;;
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Write-Error (
    config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
    const character = source[index], next = source[index + 1];
    const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
    exit 0
    exit 2
    exit 2
    fi
    for (let index = 0; index < source.length; index += 1) {
    foreach ($candidate in $candidates) {
    if (!inString && character === "/" && next === "*") {
    if (!inString && character === "/" && next === "/") {
    if (blockComment) {
    if (inString) {
    if (lineComment) {
    if [[ -f "$candidate" ]]; then
    knip.config.mjs knip.config.cjs package.json || true)"
    knip.json knip.jsonc .knip.json .knip.jsonc \
    knip.ts knip.js knip.config.ts knip.config.js \
    let lineComment = false, blockComment = false;
    let output = "", inString = false, escaped = false;
    output += character;
    printf '%s
    return ''
    return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
    }
    }
    }
    }
    }
    }
    } else if (character === "\"") inString = true;
  config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
  config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
  const fs = require("node:fs");
  const parseJsonc = source => {
  const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
  const path = require("node:path");
  const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
  const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
  done
  exit 0
  for (let index = 0; index < source.length; index += 1) {
  for candidate in "$@"; do
  fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
  if (sourcePath) {
  js|mjs|cjs|ts)
  let config = {};
  let lineComment = false, blockComment = false;
  let output = "", inString = false, escaped = false;
  local candidate
  return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
  rm -f "$output"
  source_config="$(discover_config \
  }
  }
  };
 not for current platform - ignored
"$KNIP_SKIP_JS_RUNTIME" -e '
") { lineComment = false; output += character; }
") { lineComment = false; output += character; }
");
");
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$code = @'
$env:KNIP_SKIP_SOURCE_CONFIG = $sourceConfig
$output = $env:KNIP_SKIP_OUTPUT
$pattern = $env:KNIP_SKIP_PATTERN
$sourceConfig = $env:KNIP_SKIP_SOURCE_CONFIG
& $env:KNIP_SKIP_JS_RUNTIME -e $code
'
' \
'@
POWERSHELL
case "${source_config##*.}" in
config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
const fs = require("node:fs");
const parseJsonc = source => {
const path = require("node:path");
const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
discover_config() {
esac
exit $LASTEXITCODE
export KNIP_SKIP_SOURCE_CONFIG="$source_config"
fi
fi
fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
function Find-Config([string[]]$candidates) {
if ($sourceConfig -match '\.(?:[cm]?js|ts)$') {
if ([string]::IsNullOrWhiteSpace($pattern)) {
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if (sourcePath) {
if [[ -z "$pattern" ]]; then
if [[ -z "$source_config" ]]; then
let config = {};
output="$KNIP_SKIP_OUTPUT"
source_config="$KNIP_SKIP_SOURCE_CONFIG"
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "config:skip" finished
task: "config:skip" started
task: "files" finished
task: "files" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --files --config "BASE-KNIP_INTERNAL_SKIP_CONFIG" OVR-EXTRA_ARGS'
task: [config:skip] pattern="$KNIP_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
}
}
}
}
}
}
};

=== task: help ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "help" finished
task: "help" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --help OVR-EXTRA_ARGS'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: init ===
' '{' '  "$schema": "https://unpkg.com/knip@5/schema.json"' '}' > knip.json
task: "init" finished
task: "init" started
task: [init] echo "✅ created knip.json"
task: [init] printf '%s
task: status command test -f knip.json || test -f knip.jsonc exited non-zero: exit status 1

=== task: install ===
task: "install" started
task: Task "install" is up to date
task: status command node -e "const installed = require('./node_modules/knip/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install:undo ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:install" finished
task: "bun:install" started
task: "bun:remove" finished
task: "bun:remove" started
task: "install:undo" finished
task: "install:undo" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun remove -g knip OVR-EXTRA_ARGS'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: lint ===







        'Add the skip pattern to the config ignore array or use JSON/JSONC.'
        'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config. ' +
        'knip.config.mjs', 'knip.config.cjs', 'package.json'
        'knip.json', 'knip.jsonc', '.knip.json', '.knip.jsonc',
        'knip.ts', 'knip.js', 'knip.config.ts', 'knip.config.js',
        continue;
        continue;
        else if (character === "\"") inString = false;
        else if (character === "\\") escaped = true;
        if (Test-Path -LiteralPath $candidate -PathType Leaf) { return $candidate }
        if (character === "
        if (character === "*" && next === "/") { blockComment = false; index += 1; }
        if (escaped) escaped = false;
      'Add the skip pattern to the config ignore array or use JSON/JSONC.' >&2
      'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config.' \
      blockComment = true; index += 1; continue;
      const character = source[index], next = source[index + 1];
      continue;
      continue;
      else if (character === "\"") inString = false;
      else if (character === "\\") escaped = true;
      if (!inString && character === "/" && next === "*") { blockComment = true; index += 1; continue; }
      if (!inString && character === "/" && next === "/") { lineComment = true; index += 1; continue; }
      if (blockComment) {
      if (character === "
      if (character === "*" && next === "/") { blockComment = false; index += 1; }
      if (escaped) escaped = false;
      if (inString) {
      if (lineComment) {
      lineComment = true; index += 1; continue;
      output += character;
      printf '%s' "$candidate"
      return
      }
      }
      } else if (character === "\"") inString = true;
    $sourceConfig = Find-Config @(
    )
    )
    ;;
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Write-Error (
    config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
    const character = source[index], next = source[index + 1];
    const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
    exit 0
    exit 2
    exit 2
    fi
    for (let index = 0; index < source.length; index += 1) {
    foreach ($candidate in $candidates) {
    if (!inString && character === "/" && next === "*") {
    if (!inString && character === "/" && next === "/") {
    if (blockComment) {
    if (inString) {
    if (lineComment) {
    if [[ -f "$candidate" ]]; then
    knip.config.mjs knip.config.cjs package.json || true)"
    knip.json knip.jsonc .knip.json .knip.jsonc \
    knip.ts knip.js knip.config.ts knip.config.js \
    let lineComment = false, blockComment = false;
    let output = "", inString = false, escaped = false;
    output += character;
    printf '%s
    return ''
    return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
    }
    }
    }
    }
    }
    }
    } else if (character === "\"") inString = true;
  config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
  config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
  const fs = require("node:fs");
  const parseJsonc = source => {
  const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
  const path = require("node:path");
  const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
  const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
  done
  exit 0
  for (let index = 0; index < source.length; index += 1) {
  for candidate in "$@"; do
  fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
  if (sourcePath) {
  js|mjs|cjs|ts)
  let config = {};
  let lineComment = false, blockComment = false;
  let output = "", inString = false, escaped = false;
  local candidate
  return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
  rm -f "$output"
  source_config="$(discover_config \
  }
  }
  };
 not for current platform - ignored
"$KNIP_SKIP_JS_RUNTIME" -e '
") { lineComment = false; output += character; }
") { lineComment = false; output += character; }
");
");
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$code = @'
$env:KNIP_SKIP_SOURCE_CONFIG = $sourceConfig
$output = $env:KNIP_SKIP_OUTPUT
$pattern = $env:KNIP_SKIP_PATTERN
$sourceConfig = $env:KNIP_SKIP_SOURCE_CONFIG
& $env:KNIP_SKIP_JS_RUNTIME -e $code
'
' \
'@
POWERSHELL
case "${source_config##*.}" in
config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
const fs = require("node:fs");
const parseJsonc = source => {
const path = require("node:path");
const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
discover_config() {
esac
exit $LASTEXITCODE
export KNIP_SKIP_SOURCE_CONFIG="$source_config"
fi
fi
fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
function Find-Config([string[]]$candidates) {
if ($sourceConfig -match '\.(?:[cm]?js|ts)$') {
if ([string]::IsNullOrWhiteSpace($pattern)) {
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if (sourcePath) {
if [[ -z "$pattern" ]]; then
if [[ -z "$source_config" ]]; then
let config = {};
output="$KNIP_SKIP_OUTPUT"
source_config="$KNIP_SKIP_SOURCE_CONFIG"
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "config:skip" finished
task: "config:skip" started
task: "lint" finished
task: "lint" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --config "BASE-KNIP_INTERNAL_SKIP_CONFIG" OVR-EXTRA_ARGS'
task: [config:skip] pattern="$KNIP_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [lint] echo "✅ knip passed"
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
}
}
}
}
}
}
};

=== task: lint:fix ===







        'Add the skip pattern to the config ignore array or use JSON/JSONC.'
        'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config. ' +
        'knip.config.mjs', 'knip.config.cjs', 'package.json'
        'knip.json', 'knip.jsonc', '.knip.json', '.knip.jsonc',
        'knip.ts', 'knip.js', 'knip.config.ts', 'knip.config.js',
        continue;
        continue;
        else if (character === "\"") inString = false;
        else if (character === "\\") escaped = true;
        if (Test-Path -LiteralPath $candidate -PathType Leaf) { return $candidate }
        if (character === "
        if (character === "*" && next === "/") { blockComment = false; index += 1; }
        if (escaped) escaped = false;
      'Add the skip pattern to the config ignore array or use JSON/JSONC.' >&2
      'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config.' \
      blockComment = true; index += 1; continue;
      const character = source[index], next = source[index + 1];
      continue;
      continue;
      else if (character === "\"") inString = false;
      else if (character === "\\") escaped = true;
      if (!inString && character === "/" && next === "*") { blockComment = true; index += 1; continue; }
      if (!inString && character === "/" && next === "/") { lineComment = true; index += 1; continue; }
      if (blockComment) {
      if (character === "
      if (character === "*" && next === "/") { blockComment = false; index += 1; }
      if (escaped) escaped = false;
      if (inString) {
      if (lineComment) {
      lineComment = true; index += 1; continue;
      output += character;
      printf '%s' "$candidate"
      return
      }
      }
      } else if (character === "\"") inString = true;
    $sourceConfig = Find-Config @(
    )
    )
    ;;
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Write-Error (
    config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
    const character = source[index], next = source[index + 1];
    const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
    exit 0
    exit 2
    exit 2
    fi
    for (let index = 0; index < source.length; index += 1) {
    foreach ($candidate in $candidates) {
    if (!inString && character === "/" && next === "*") {
    if (!inString && character === "/" && next === "/") {
    if (blockComment) {
    if (inString) {
    if (lineComment) {
    if [[ -f "$candidate" ]]; then
    knip.config.mjs knip.config.cjs package.json || true)"
    knip.json knip.jsonc .knip.json .knip.jsonc \
    knip.ts knip.js knip.config.ts knip.config.js \
    let lineComment = false, blockComment = false;
    let output = "", inString = false, escaped = false;
    output += character;
    printf '%s
    return ''
    return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
    }
    }
    }
    }
    }
    }
    } else if (character === "\"") inString = true;
  config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
  config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
  const fs = require("node:fs");
  const parseJsonc = source => {
  const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
  const path = require("node:path");
  const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
  const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
  done
  exit 0
  for (let index = 0; index < source.length; index += 1) {
  for candidate in "$@"; do
  fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
  if (sourcePath) {
  js|mjs|cjs|ts)
  let config = {};
  let lineComment = false, blockComment = false;
  let output = "", inString = false, escaped = false;
  local candidate
  return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
  rm -f "$output"
  source_config="$(discover_config \
  }
  }
  };
 not for current platform - ignored
"$KNIP_SKIP_JS_RUNTIME" -e '
") { lineComment = false; output += character; }
") { lineComment = false; output += character; }
");
");
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$code = @'
$env:KNIP_SKIP_SOURCE_CONFIG = $sourceConfig
$output = $env:KNIP_SKIP_OUTPUT
$pattern = $env:KNIP_SKIP_PATTERN
$sourceConfig = $env:KNIP_SKIP_SOURCE_CONFIG
& $env:KNIP_SKIP_JS_RUNTIME -e $code
'
' \
'@
POWERSHELL
case "${source_config##*.}" in
config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
const fs = require("node:fs");
const parseJsonc = source => {
const path = require("node:path");
const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
discover_config() {
esac
exit $LASTEXITCODE
export KNIP_SKIP_SOURCE_CONFIG="$source_config"
fi
fi
fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
function Find-Config([string[]]$candidates) {
if ($sourceConfig -match '\.(?:[cm]?js|ts)$') {
if ([string]::IsNullOrWhiteSpace($pattern)) {
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if (sourcePath) {
if [[ -z "$pattern" ]]; then
if [[ -z "$source_config" ]]; then
let config = {};
output="$KNIP_SKIP_OUTPUT"
source_config="$KNIP_SKIP_SOURCE_CONFIG"
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "config:skip" finished
task: "config:skip" started
task: "lint:fix" finished
task: "lint:fix" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --fix --config "BASE-KNIP_INTERNAL_SKIP_CONFIG" OVR-EXTRA_ARGS'
task: [config:skip] pattern="$KNIP_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
}
}
}
}
}
}
};

=== task: production ===







        'Add the skip pattern to the config ignore array or use JSON/JSONC.'
        'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config. ' +
        'knip.config.mjs', 'knip.config.cjs', 'package.json'
        'knip.json', 'knip.jsonc', '.knip.json', '.knip.jsonc',
        'knip.ts', 'knip.js', 'knip.config.ts', 'knip.config.js',
        continue;
        continue;
        else if (character === "\"") inString = false;
        else if (character === "\\") escaped = true;
        if (Test-Path -LiteralPath $candidate -PathType Leaf) { return $candidate }
        if (character === "
        if (character === "*" && next === "/") { blockComment = false; index += 1; }
        if (escaped) escaped = false;
      'Add the skip pattern to the config ignore array or use JSON/JSONC.' >&2
      'KNIP_LINT_SKIP_PATTERN cannot be merged with a dynamic JS/TS Knip config.' \
      blockComment = true; index += 1; continue;
      const character = source[index], next = source[index + 1];
      continue;
      continue;
      else if (character === "\"") inString = false;
      else if (character === "\\") escaped = true;
      if (!inString && character === "/" && next === "*") { blockComment = true; index += 1; continue; }
      if (!inString && character === "/" && next === "/") { lineComment = true; index += 1; continue; }
      if (blockComment) {
      if (character === "
      if (character === "*" && next === "/") { blockComment = false; index += 1; }
      if (escaped) escaped = false;
      if (inString) {
      if (lineComment) {
      lineComment = true; index += 1; continue;
      output += character;
      printf '%s' "$candidate"
      return
      }
      }
      } else if (character === "\"") inString = true;
    $sourceConfig = Find-Config @(
    )
    )
    ;;
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    Write-Error (
    config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
    const character = source[index], next = source[index + 1];
    const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
    exit 0
    exit 2
    exit 2
    fi
    for (let index = 0; index < source.length; index += 1) {
    foreach ($candidate in $candidates) {
    if (!inString && character === "/" && next === "*") {
    if (!inString && character === "/" && next === "/") {
    if (blockComment) {
    if (inString) {
    if (lineComment) {
    if [[ -f "$candidate" ]]; then
    knip.config.mjs knip.config.cjs package.json || true)"
    knip.json knip.jsonc .knip.json .knip.jsonc \
    knip.ts knip.js knip.config.ts knip.config.js \
    let lineComment = false, blockComment = false;
    let output = "", inString = false, escaped = false;
    output += character;
    printf '%s
    return ''
    return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
    }
    }
    }
    }
    }
    }
    } else if (character === "\"") inString = true;
  config = path.basename(sourcePath) === "package.json" ? (parsed.knip ?? {}) : parsed;
  config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
  const fs = require("node:fs");
  const parseJsonc = source => {
  const parsed = parseJsonc(fs.readFileSync(sourcePath, "utf8"));
  const path = require("node:path");
  const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
  const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
  done
  exit 0
  for (let index = 0; index < source.length; index += 1) {
  for candidate in "$@"; do
  fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
  if (sourcePath) {
  js|mjs|cjs|ts)
  let config = {};
  let lineComment = false, blockComment = false;
  let output = "", inString = false, escaped = false;
  local candidate
  return JSON.parse(output.replace(/,\s*([}\]])/g, "$1"));
  rm -f "$output"
  source_config="$(discover_config \
  }
  }
  };
 not for current platform - ignored
"$KNIP_SKIP_JS_RUNTIME" -e '
") { lineComment = false; output += character; }
") { lineComment = false; output += character; }
");
");
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$code = @'
$env:KNIP_SKIP_SOURCE_CONFIG = $sourceConfig
$output = $env:KNIP_SKIP_OUTPUT
$pattern = $env:KNIP_SKIP_PATTERN
$sourceConfig = $env:KNIP_SKIP_SOURCE_CONFIG
& $env:KNIP_SKIP_JS_RUNTIME -e $code
'
' \
'@
POWERSHELL
case "${source_config##*.}" in
config = { ...config, ignore: [...(config.ignore ?? []), pattern] };
const fs = require("node:fs");
const parseJsonc = source => {
const path = require("node:path");
const pattern = process.env.KNIP_SKIP_PATTERN.replaceAll("\\", "/");
const sourcePath = process.env.KNIP_SKIP_SOURCE_CONFIG;
discover_config() {
esac
exit $LASTEXITCODE
export KNIP_SKIP_SOURCE_CONFIG="$source_config"
fi
fi
fs.writeFileSync(process.env.KNIP_SKIP_OUTPUT, JSON.stringify(config, null, 2) + "
function Find-Config([string[]]$candidates) {
if ($sourceConfig -match '\.(?:[cm]?js|ts)$') {
if ([string]::IsNullOrWhiteSpace($pattern)) {
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if (sourcePath) {
if [[ -z "$pattern" ]]; then
if [[ -z "$source_config" ]]; then
let config = {};
output="$KNIP_SKIP_OUTPUT"
source_config="$KNIP_SKIP_SOURCE_CONFIG"
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "config:skip" finished
task: "config:skip" started
task: "production" finished
task: "production" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --production --config "BASE-KNIP_INTERNAL_SKIP_CONFIG" OVR-EXTRA_ARGS'
task: [config:skip] pattern="$KNIP_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
}
}
}
}
}
}
};

=== task: upgrade ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:add" finished
task: "bun:add" started
task: "bun:install" finished
task: "bun:install" started
task: "upgrade" finished
task: "upgrade" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun add -g knip@latest OVR-EXTRA_ARGS'
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: version ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "version" finished
task: "version" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x knip --version '
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
