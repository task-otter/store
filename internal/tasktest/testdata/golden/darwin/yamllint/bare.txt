# module: yamllint
# case:   bare

=== task: ci ===
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "ci" finished
task: "ci" started
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "_install:unix" is up to date
task: Task "uv:_install:unix" is up to date
task: [ci] bash -lc 'export PATH="$HOME/.local/bin:$PATH"; _skip=""; if [ -z "$_skip" ]; then yamllint --strict  . ; else _files="$(mktemp)"; trap '"'"'rm -f "$_files"'"'"' EXIT; for _target in .; do if [ -d "$_target" ]; then find "$_target" -type f \( -name "*.yml" -o -name "*.yaml" \) -print0; elif [ -f "$_target" ]; then printf "%s\000" "$_target"; fi; done | task --silent --taskfile "$REPO/taskfiles/yamllint/../internal/skipfiles/Taskfile.yml" filter "SKIPFILES_PATTERN=$_skip" >"$_files"; if [ -s "$_files" ]; then xargs -0 yamllint --strict   <"$_files"; else echo "No matching YAML files found"; fi; fi && echo "✅ yamllint passed" || { echo "❌ yamllint failed"; exit 1; }'
task: status command bash -lc 'export PATH="$HOME/.local/bin:$PATH"; command -v yamllint >/dev/null 2>&1 && { [ -z "" ] || yamllint --version | grep -Fq ""; }' exited zero
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: config:init ===
" >> .yamllint
' > .yamllint
' >> .yamllint
' >> .yamllint
' >> .yamllint
' >> .yamllint
task: "config:init" finished
task: "config:init" started
task: [config:init] echo 'Created .yamllint with default configuration.'
task: [config:init] printf "    allowed-values: ['true', 'false']
task: [config:init] printf '    max: 120
task: [config:init] printf '  line-length:
task: [config:init] printf '  truthy:
task: [config:init] printf 'extends: default
task: [config:init] printf 'rules:
task: status command test -f .yamllint exited non-zero: exit status 1

=== task: install ===
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "_install:unix" is up to date
task: Task "uv:_install:unix" is up to date
task: status command bash -lc 'export PATH="$HOME/.local/bin:$PATH"; command -v yamllint >/dev/null 2>&1 && { [ -z "" ] || yamllint --version | grep -Fq ""; }' exited zero
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: install:undo ===
task: "_install:undo:unix" finished
task: "_install:undo:unix" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:unix] bash -lc 'export PATH="$HOME/.local/bin:$PATH"; uv tool uninstall yamllint'
task: [_install:undo:unix] echo 'Removing yamllint...'
task: [_install:undo:unix] echo 'yamllint removed.'
task: status command bash -lc 'export PATH="$HOME/.local/bin:$PATH"; ! command -v yamllint >/dev/null 2>&1' exited non-zero: exit status 1

=== task: lint ===
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "lint" finished
task: "lint" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "_install:unix" is up to date
task: Task "uv:_install:unix" is up to date
task: [lint] bash -lc 'export PATH="$HOME/.local/bin:$PATH"; _skip=""; if [ -z "$_skip" ]; then yamllint  . ; else _files="$(mktemp)"; trap '"'"'rm -f "$_files"'"'"' EXIT; for _target in .; do if [ -d "$_target" ]; then find "$_target" -type f \( -name "*.yml" -o -name "*.yaml" \) -print0; elif [ -f "$_target" ]; then printf "%s\000" "$_target"; fi; done | task --silent --taskfile "$REPO/taskfiles/yamllint/../internal/skipfiles/Taskfile.yml" filter "SKIPFILES_PATTERN=$_skip" >"$_files"; if [ -s "$_files" ]; then xargs -0 yamllint   <"$_files"; else echo "No matching YAML files found"; fi; fi && echo "✅ yamllint passed" || { echo "❌ yamllint failed"; exit 1; }'
task: status command bash -lc 'export PATH="$HOME/.local/bin:$PATH"; command -v yamllint >/dev/null 2>&1 && { [ -z "" ] || yamllint --version | grep -Fq ""; }' exited zero
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: lint:fix ===
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_install:yamlfix:unix" started
task: "_install:yamlfix:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "lint:fix" finished
task: "lint:fix" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "_install:unix" is up to date
task: Task "_install:yamlfix:unix" is up to date
task: Task "uv:_install:unix" is up to date
task: [lint:fix] bash -lc 'export PATH="$HOME/.local/bin:$PATH"; _t="."; [ -d "$_t" ] || _t="."; find "$_t" \( -name "*.yml" -o -name "*.yaml" \) ! -name "Taskfile.yml" ! -name "Taskfile.yaml" -print0 | xargs -0 yamlfix  '
task: skipping execution of task: $REPO/taskfiles/uv/Taskfile.yml:install
task: status command bash -lc 'export PATH="$HOME/.local/bin:$PATH"; command -v yamlfix >/dev/null 2>&1 && { [ -z "" ] || yamlfix --version | grep -Fq ""; }' exited zero
task: status command bash -lc 'export PATH="$HOME/.local/bin:$PATH"; command -v yamllint >/dev/null 2>&1 && { [ -z "" ] || yamllint --version | grep -Fq ""; }' exited zero
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: upgrade ===
task: "_upgrade:unix" finished
task: "_upgrade:unix" started
task: "_upgrade:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [_upgrade:unix] bash -lc 'export PATH="$HOME/.local/bin:$PATH"; uv tool install --force "yamllint"'
task: [_upgrade:unix] echo 'Upgrading yamllint via uv...'
task: [_upgrade:unix] echo 'yamllint upgraded.'

=== task: version ===
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: "version" finished
task: "version" started
task: Task "_install:unix" is up to date
task: Task "uv:_install:unix" is up to date
task: [version] bash -lc 'export PATH="$HOME/.local/bin:$PATH"; yamllint --version'
task: status command bash -lc 'export PATH="$HOME/.local/bin:$PATH"; command -v yamllint >/dev/null 2>&1 && { [ -z "" ] || yamllint --version | grep -Fq ""; }' exited zero
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero
