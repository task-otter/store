# module: jsonlint
# case:   both
# vars:
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   JSONLINT_LINT_SKIP_PATTERN=BASE-JSONLINT_LINT_SKIP_PATTERN
#   JSONLINT_VERSION=BASE-JSONLINT_VERSION
#   TARGETS=BASE-TARGETS
#   UV_LOAD=BASE-UV_LOAD
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   JSONLINT_LINT_SKIP_PATTERN_OVERRIDE=OVR-JSONLINT_LINT_SKIP_PATTERN
#   JSONLINT_VERSION_OVERRIDE=OVR-JSONLINT_VERSION
#   TARGETS_OVERRIDE=OVR-TARGETS
#   UV_LOAD_OVERRIDE=OVR-UV_LOAD

=== task: install ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "demjson3==BASE-JSONLINT_VERSION"'
task: [_install:unix] echo 'Installing the jsonlint CLI via uv...'
task: [_install:unix] echo 'jsonlint installed.'
task: status command bash -lc 'BASE-UV_LOAD; command -v jsonlint >/dev/null 2>&1 && { [ -z "BASE-JSONLINT_VERSION" ] || uv tool list 2>/dev/null | grep -Fq "demjson3 vBASE-JSONLINT_VERSION"; }' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: install:undo ===
task: "_install:undo:unix" finished
task: "_install:undo:unix" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:unix] bash -lc 'BASE-UV_LOAD; uv tool uninstall demjson3'
task: [_install:undo:unix] echo 'Removing jsonlint...'
task: [_install:undo:unix] echo 'jsonlint removed.'
task: status command bash -lc 'BASE-UV_LOAD; ! command -v jsonlint >/dev/null 2>&1' exited non-zero: exit status 1

=== task: lint ===
  filter ('SKIPFILES_PATTERN=' + $skipPattern));
if (-not $paths) { Write-Host 'No matching JSON files found'; exit 0 }; foreach ($path in $paths) { jsonlint OVR-EXTRA_ARGS $path; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }" not for current platform - ignored
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "lint" finished
task: "lint" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "demjson3==BASE-JSONLINT_VERSION"'
task: [_install:unix] echo 'Installing the jsonlint CLI via uv...'
task: [_install:unix] echo 'jsonlint installed.'
task: [lint] bash -lc 'BASE-UV_LOAD; _t="OVR-TARGETS"; _skip="OVR-JSONLINT_LINT_SKIP_PATTERN"; _files="$(mktemp)"; trap '"'"'rm -f "$_files"'"'"' EXIT; if [ -d "$_t" ]; then find "$_t" -name "*.json" -type f -print0 | task --silent --taskfile "$REPO/taskfiles/jsonlint/../internal/skipfiles/Taskfile.yml" filter "SKIPFILES_PATTERN=$_skip" >"$_files"; else printf "%s\000" "$_t" | task --silent --taskfile "$REPO/taskfiles/jsonlint/../internal/skipfiles/Taskfile.yml" filter "SKIPFILES_PATTERN=$_skip" >"$_files"; fi; if [ -s "$_files" ]; then xargs -0 jsonlint OVR-EXTRA_ARGS; else echo "No matching JSON files found under $_t"; fi && echo "✅ jsonlint passed" || { echo "❌ jsonlint failed"; exit 1; }'
task: [lint] powershell -NoProfile -ExecutionPolicy Bypass -Command "$ErrorActionPreference = 'Stop'; $t = 'OVR-TARGETS'; $skipPattern = 'OVR-JSONLINT_LINT_SKIP_PATTERN'; if (Test-Path -LiteralPath $t -PathType Container) { $files = Get-ChildItem -LiteralPath $t -Recurse -Filter *.json -File } else { $files = @(Get-Item -LiteralPath $t) }; $paths = @($files | ForEach-Object FullName | task --silent --taskfile '$REPO/taskfiles/jsonlint/../internal/skipfiles/Taskfile.yml'
task: status command bash -lc 'BASE-UV_LOAD; command -v jsonlint >/dev/null 2>&1 && { [ -z "BASE-JSONLINT_VERSION" ] || uv tool list 2>/dev/null | grep -Fq "demjson3 vBASE-JSONLINT_VERSION"; }' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: upgrade ===
task: "_upgrade:unix" finished
task: "_upgrade:unix" started
task: "_upgrade:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [_upgrade:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "demjson3==BASE-JSONLINT_VERSION"'
task: [_upgrade:unix] echo 'Upgrading jsonlint via uv...'
task: [_upgrade:unix] echo 'jsonlint upgraded.'

=== task: version ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: "version" finished
task: "version" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "demjson3==BASE-JSONLINT_VERSION"'
task: [_install:unix] echo 'Installing the jsonlint CLI via uv...'
task: [_install:unix] echo 'jsonlint installed.'
task: [version] bash -lc 'BASE-UV_LOAD; jsonlint --version'
task: [version] jsonlint --version not for current platform - ignored
task: status command bash -lc 'BASE-UV_LOAD; command -v jsonlint >/dev/null 2>&1 && { [ -z "BASE-JSONLINT_VERSION" ] || uv tool list 2>/dev/null | grep -Fq "demjson3 vBASE-JSONLINT_VERSION"; }' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero
