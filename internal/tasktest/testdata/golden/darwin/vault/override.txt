# module: vault
# case:   override
# vars:
#   APPROLE_MOUNT_OVERRIDE=OVR-APPROLE_MOUNT
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   FILE_OVERRIDE=OVR-FILE
#   KEYS_FILE_OVERRIDE=OVR-KEYS_FILE
#   KV_MOUNT_OVERRIDE=OVR-KV_MOUNT
#   ROLE_ID_OVERRIDE=OVR-ROLE_ID
#   ROOT_TOKEN_OVERRIDE=OVR-ROOT_TOKEN
#   SECRET_ID_OVERRIDE=OVR-SECRET_ID
#   SECRET_PATH_OVERRIDE=OVR-SECRET_PATH
#   SECRET_VERSION_OVERRIDE=OVR-SECRET_VERSION
#   SHARES_OVERRIDE=OVR-SHARES
#   SNAPSHOT_FILE_OVERRIDE=OVR-SNAPSHOT_FILE
#   THRESHOLD_OVERRIDE=OVR-THRESHOLD
#   VAULT_ADDR_OVERRIDE=OVR-VAULT_ADDR
#   VAULT_FILE_OVERRIDE=OVR-VAULT_FILE
#   VERSION_OVERRIDE=OVR-VERSION

=== task: health ===
task: "health" finished
task: "health" started
task: "jq:_install:linux" not for current platform - ignored
task: "jq:_install:macos" started
task: "jq:_install:windows" not for current platform - ignored
task: "jq:install" finished
task: "jq:install" started
task: Task "jq:_install:macos" is up to date
task: [health] curl -s "http://127.0.0.1:8200/v1/sys/health" | jq .
task: status command command -v jq >/dev/null 2>&1 && { [ -z "" ] || [ "$(jq --version)" = "jq-" ]; } exited zero

=== task: init ===

  -address="http://127.0.0.1:8200" \
  -format=json > "$TMP"
  -key-shares=OVR-SHARES \
  -key-threshold=OVR-THRESHOLD \
  echo "KEYS_FILE already exists: $KF. Move or remove it before running init again." >&2
  exit 1
TMP="${KF}.tmp.$$"
chmod 600 "$TMP"
echo "Vault initialized. Unseal keys and root token saved to $KF"
fi
if [ -e "$KF" ]; then
mv "$TMP" "$KF"
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "init" finished
task: "init" started
task: "install" finished
task: "install" started
task: Task "_install:macos" is up to date
task: [init] KF="OVR-KEYS_FILE"
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero
trap 'rm -f "$TMP"' EXIT
trap - EXIT
vault operator init \

=== task: install ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: Task "_install:macos" is up to date
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero

=== task: install:undo ===
task: "_install:undo:linux" not for current platform - ignored
task: "_install:undo:macos" finished
task: "_install:undo:macos" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:macos] brew uninstall hashicorp/tap/vault
task: [_install:undo:macos] echo 'Removing Vault CLI via Homebrew...'
task: [_install:undo:macos] echo 'Vault CLI removed.'
task: status command ! command -v vault >/dev/null 2>&1 exited non-zero: exit status 1

=== task: kv:get ===
--- exit: exit status 201
task: "kv:get" started
task: Failed to run task "kv:get": task: precondition not met
task: VAULT_TOKEN environment variable must be set by the caller.

=== task: login ===
--- exit: exit status 201
task: "login" started
task: Failed to run task "login": task: precondition not met
task: KEYS_FILE not found. Run task init first to generate OVR-KEYS_FILE.

=== task: login:approle ===

    "auth/${VAULT_LOGIN_APPROLE_MOUNT}/login" \
    -
    -address="http://127.0.0.1:8200" \
    -address="http://127.0.0.1:8200" \
    -field=token \
    -method=token \
    -no-print \
    role_id="$VAULT_LOGIN_ROLE_ID" \
    secret_id=- \
  | vault login \
  | vault write \
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "login:approle" finished
task: "login:approle" started
task: Task "_install:macos" is up to date
task: [login:approle] printf '%s' "$VAULT_LOGIN_SECRET_ID" \
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero

=== task: login:root-token ===

  | vault login -address="http://127.0.0.1:8200" -method=token -no-print -
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "login:root-token" finished
task: "login:root-token" started
task: Task "_install:macos" is up to date
task: [login:root-token] printf '%s' "$VAULT_LOGIN_ROOT_TOKEN" \
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero

=== task: peers ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "peers" finished
task: "peers" started
task: Task "_install:macos" is up to date
task: [peers] vault operator raft list-peers -address="http://127.0.0.1:8200"
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero

=== task: restore ===
--- exit: exit status 201
task: "restore" started
task: Failed to run task "restore": task: precondition not met
task: Snapshot file not found. Pass FILE=path/to/snapshot, or VAULT_FILE=path/to/snapshot from the root include.

=== task: root-token ===
--- exit: exit status 201
task: "jq:_install:linux" not for current platform - ignored
task: "jq:_install:macos" started
task: "jq:_install:windows" not for current platform - ignored
task: "jq:install" finished
task: "jq:install" started
task: "root-token" started
task: Failed to run task "root-token": task: precondition not met
task: KEYS_FILE not found. Run task init first to generate OVR-KEYS_FILE.
task: Task "jq:_install:macos" is up to date
task: status command command -v jq >/dev/null 2>&1 && { [ -z "" ] || [ "$(jq --version)" = "jq-" ]; } exited zero

=== task: seal ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "seal" finished
task: "seal" started
task: Task "_install:macos" is up to date
task: [seal] vault operator seal -address="http://127.0.0.1:8200"
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero

=== task: snapshot ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "snapshot" finished
task: "snapshot" started
task: Task "_install:macos" is up to date
task: [snapshot] vault operator raft snapshot save -address="http://127.0.0.1:8200" "OVR-VAULT_FILE"
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero

=== task: status ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "status" finished
task: "status" started
task: Task "_install:macos" is up to date
task: [status] vault status -address="http://127.0.0.1:8200" || true
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero

=== task: token:issue:approle ===

    "auth/${VAULT_LOGIN_APPROLE_MOUNT}/login" \
    -address="http://127.0.0.1:8200" \
    -field=token \
    role_id="$VAULT_LOGIN_ROLE_ID" \
    secret_id=-
  | vault write \
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "token:issue:approle" finished
task: "token:issue:approle" started
task: Task "_install:macos" is up to date
task: [token:issue:approle] printf '%s' "$VAULT_LOGIN_SECRET_ID" \
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero

=== task: token:revoke-self ===
--- exit: exit status 201
task: "token:revoke-self" started
task: Failed to run task "token:revoke-self": task: precondition not met
task: VAULT_TOKEN environment variable must be set.

=== task: unseal ===
--- exit: exit status 201
task: "unseal" started
task: Failed to run task "unseal": task: precondition not met
task: KEYS_FILE not found. Run task init first to generate OVR-KEYS_FILE.

=== task: upgrade ===
task: "_upgrade:linux" not for current platform - ignored
task: "_upgrade:macos" finished
task: "_upgrade:macos" started
task: "_upgrade:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [_upgrade:macos] brew update
task: [_upgrade:macos] brew upgrade hashicorp/tap/vault
task: [_upgrade:macos] echo 'Upgrading Vault CLI via Homebrew...'
task: [_upgrade:macos] echo 'Vault CLI upgraded.'
task: [_upgrade:macos] if condition not met - skipped

=== task: verify ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "verify" finished
task: "verify" started
task: Task "_install:macos" is up to date
task: [verify] vault status -address="http://127.0.0.1:8200"
task: [verify] vault version
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero

=== task: version ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "version" finished
task: "version" started
task: Task "_install:macos" is up to date
task: [version] vault version
task: status command command -v vault >/dev/null 2>&1 && { [ -z "" ] || vault version | grep -Fq "v"; } exited zero
