# module: hadolint
# case:   base
# vars:
#   CONFIG=BASE-CONFIG
#   DOCKERFILE=BASE-DOCKERFILE
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   HADOLINT_LINT_SKIP_PATTERN=BASE-HADOLINT_LINT_SKIP_PATTERN
#   HADOLINT_VERSION=BASE-HADOLINT_VERSION

=== task: install ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" finished
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: [_install:macos] brew install hadolint
task: [_install:macos] echo 'Installing hadolint via Homebrew...'
task: [_install:macos] echo 'hadolint installed.'
task: status command command -v hadolint >/dev/null 2>&1 && hadolint --version | grep -Fq "BASE-HADOLINT_VERSION" exited non-zero: exit status 1

=== task: install:undo ===
task: "_install:undo:linux" not for current platform - ignored
task: "_install:undo:macos" finished
task: "_install:undo:macos" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:macos] brew uninstall hadolint
task: [_install:undo:macos] echo 'Removing hadolint via Homebrew...'
task: [_install:undo:macos] echo 'hadolint removed.'
task: status command ! command -v hadolint >/dev/null 2>&1 exited non-zero: exit status 1

=== task: lint ===

      filter "SKIPFILES_PATTERN=$skip_pattern" |
    "$dockerfile" BASE-EXTRA_ARGS \
    $dockerfile BASE-EXTRA_ARGS;
    && echo "✅ hadolint passed" || { echo "❌ hadolint failed"; exit 1; }
    --config "BASE-CONFIG" \
    --config 'BASE-CONFIG'
    filter ('SKIPFILES_PATTERN=' + $skipPattern)
    task --silent --taskfile "$REPO/taskfiles/hadolint/../internal/skipfiles/Taskfile.yml" \
    tr -d '\0'
  "$dockerfile" BASE-EXTRA_ARGS  \
  $dockerfile BASE-EXTRA_ARGS ;
  $dockerfile |
  && echo "✅ hadolint passed" || { echo "❌ hadolint failed"; exit 1; }
  --config "BASE-CONFIG" \
  --config 'BASE-CONFIG'
  echo "✅ hadolint passed (Dockerfile skipped)"
  exit
  exit $LASTEXITCODE
  exit 0
  hadolint 
  hadolint \
  printf '%s\000' "$dockerfile" |
  task --silent --taskfile '$REPO/taskfiles/hadolint/../internal/skipfiles/Taskfile.yml'
)" ]; then
); if (-not $paths) { Write-Host 'Dockerfile skipped'; exit 0 }; hadolint
dockerfile='BASE-DOCKERFILE'
exit $LASTEXITCODE" not for current platform - ignored
fi
fi
hadolint \
if [ -z "$(
if [ -z "$skip_pattern" ]; then
task: "_install:linux" not for current platform - ignored
task: "_install:macos" finished
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "lint" finished
task: "lint" started
task: [_install:macos] brew install hadolint
task: [_install:macos] echo 'Installing hadolint via Homebrew...'
task: [_install:macos] echo 'hadolint installed.'
task: [lint] powershell -NoProfile -ExecutionPolicy Bypass -Command "$skipPattern='BASE-HADOLINT_LINT_SKIP_PATTERN'; $dockerfile='BASE-DOCKERFILE'; if (-not $skipPattern) {
task: [lint] skip_pattern='BASE-HADOLINT_LINT_SKIP_PATTERN'
task: status command command -v hadolint >/dev/null 2>&1 && hadolint --version | grep -Fq "BASE-HADOLINT_VERSION" exited non-zero: exit status 1
}; $paths=@(

=== task: upgrade ===
task: "_upgrade:linux" not for current platform - ignored
task: "_upgrade:macos" finished
task: "_upgrade:macos" started
task: "_upgrade:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [_upgrade:macos] brew upgrade hadolint
task: [_upgrade:macos] echo 'Upgrading hadolint via Homebrew...'
task: [_upgrade:macos] echo 'hadolint upgraded.'
task: [_upgrade:macos] if condition not met - skipped

=== task: version ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" finished
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "version" finished
task: "version" started
task: [_install:macos] brew install hadolint
task: [_install:macos] echo 'Installing hadolint via Homebrew...'
task: [_install:macos] echo 'hadolint installed.'
task: [version] hadolint --version
task: status command command -v hadolint >/dev/null 2>&1 && hadolint --version | grep -Fq "BASE-HADOLINT_VERSION" exited non-zero: exit status 1
