# module: biome/node/nvm/pnpm
# case:   both
# vars:
#   BIOME_DEFAULT_TARGETS=BASE-BIOME_DEFAULT_TARGETS
#   BIOME_FMT_SKIP_PATTERN=BASE-BIOME_FMT_SKIP_PATTERN
#   BIOME_INTERNAL_SKIP_CONFIG=BASE-BIOME_INTERNAL_SKIP_CONFIG
#   BIOME_LINT_SKIP_PATTERN=BASE-BIOME_LINT_SKIP_PATTERN
#   BIOME_TARGETS=BASE-BIOME_TARGETS
#   CONFIG=BASE-CONFIG
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   TARGETS=BASE-TARGETS
#   VERSION=BASE-VERSION
#   BIOME_DEFAULT_TARGETS_OVERRIDE=OVR-BIOME_DEFAULT_TARGETS
#   BIOME_FMT_SKIP_PATTERN_OVERRIDE=OVR-BIOME_FMT_SKIP_PATTERN
#   BIOME_INTERNAL_SKIP_CONFIG_OVERRIDE=OVR-BIOME_INTERNAL_SKIP_CONFIG
#   BIOME_LINT_SKIP_PATTERN_OVERRIDE=OVR-BIOME_LINT_SKIP_PATTERN
#   BIOME_TARGETS_OVERRIDE=OVR-BIOME_TARGETS
#   CONFIG_OVERRIDE=OVR-CONFIG
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   TARGETS_OVERRIDE=OVR-TARGETS
#   VERSION_OVERRIDE=OVR-VERSION

=== task: cache:clean ===
task: "cache:clean" finished
task: "cache:clean" started
task: [cache:clean] if [ -d .cache/biome ]; then rm -rf .cache/biome; fi
task: [cache:clean] if [ -d node_modules/.cache/biome ]; then rm -rf node_modules/.cache/biome; fi

=== task: check ===










            $sourceConfig = $candidate
            break
        if (Test-Path -LiteralPath $candidate -PathType Leaf) {
        }
      printf '%s' "$candidate"
      return
    "$(json_escape "$source_config")" "$includes" >"$output"
    $additionalPattern -ne $pattern) {
    $config.extends = @($sourceConfig.Replace('\', '/'))
    $includes += '!' + $additionalPattern.Replace('\', '/')
    $includes += '!' + $pattern.Replace('\', '/')
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    [string]::IsNullOrWhiteSpace($additionalPattern)) {
    exit 0
    fi
    foreach ($candidate in @('biome.json', 'biome.jsonc')) {
    if [[ -f "$candidate" ]]; then
    }
  done
  exit 0
  for candidate in "$@"; do
  includes+=",\"!$(json_escape "$additional_pattern")\""
  includes+=",\"!$(json_escape "$pattern")\""
  local candidate
  local value="$1"
  printf '%s' "$value"
  printf '{"extends":["%s"],"files":{"includes":[%s]}}
  printf '{"files":{"includes":[%s]}}
  rm -f "$output"
  source_config="$(discover_config biome.json biome.jsonc || true)"
  value="${value//$'
  value="${value//\"/\\\"}"
  value="${value//\\/\\\\}"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$additionalPattern = $env:BIOME_SKIP_ADDITIONAL_PATTERN
$config = [ordered]@{}
$config | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $output -Encoding UTF8
$config.files = [ordered]@{ includes = $includes }
$includes = @('**')
$output = $env:BIOME_SKIP_OUTPUT
$pattern = $env:BIOME_SKIP_PATTERN
$sourceConfig = $env:BIOME_SKIP_SOURCE_CONFIG
' "$includes" >"$output"
' \
'/\
POWERSHELL
additional_pattern="$BIOME_SKIP_ADDITIONAL_PATTERN"
discover_config() {
else
fi
fi
fi
fi
fi
if (-not [string]::IsNullOrWhiteSpace($additionalPattern) -and
if (-not [string]::IsNullOrWhiteSpace($pattern)) {
if (-not [string]::IsNullOrWhiteSpace($sourceConfig)) {
if ([string]::IsNullOrWhiteSpace($pattern) -and
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if [[ -n "$additional_pattern" && "$additional_pattern" != "$pattern" ]]; then
if [[ -n "$pattern" ]]; then
if [[ -n "$source_config" ]]; then
if [[ -z "$pattern" && -z "$additional_pattern" ]]; then
if [[ -z "$source_config" ]]; then
includes="\"**\""
json_escape() {
output="$BIOME_SKIP_OUTPUT"
source_config="$BIOME_SKIP_SOURCE_CONFIG"
task: "check" finished
task: "check" started
task: "config:skip" finished
task: "config:skip" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [check] echo "✅ biome passed"
task: [config:skip] pattern="$BIOME_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [pnpm:_exec:unix] bash -c "biome check --config-path "BASE-BIOME_INTERNAL_SKIP_CONFIG"  OVR-BIOME_TARGETS  OVR-EXTRA_ARGS "
}
}
}
}
}
}
}
}"

=== task: check:write ===










            $sourceConfig = $candidate
            break
        if (Test-Path -LiteralPath $candidate -PathType Leaf) {
        }
      printf '%s' "$candidate"
      return
    "$(json_escape "$source_config")" "$includes" >"$output"
    $additionalPattern -ne $pattern) {
    $config.extends = @($sourceConfig.Replace('\', '/'))
    $includes += '!' + $additionalPattern.Replace('\', '/')
    $includes += '!' + $pattern.Replace('\', '/')
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    [string]::IsNullOrWhiteSpace($additionalPattern)) {
    exit 0
    fi
    foreach ($candidate in @('biome.json', 'biome.jsonc')) {
    if [[ -f "$candidate" ]]; then
    }
  done
  exit 0
  for candidate in "$@"; do
  includes+=",\"!$(json_escape "$additional_pattern")\""
  includes+=",\"!$(json_escape "$pattern")\""
  local candidate
  local value="$1"
  printf '%s' "$value"
  printf '{"extends":["%s"],"files":{"includes":[%s]}}
  printf '{"files":{"includes":[%s]}}
  rm -f "$output"
  source_config="$(discover_config biome.json biome.jsonc || true)"
  value="${value//$'
  value="${value//\"/\\\"}"
  value="${value//\\/\\\\}"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$additionalPattern = $env:BIOME_SKIP_ADDITIONAL_PATTERN
$config = [ordered]@{}
$config | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $output -Encoding UTF8
$config.files = [ordered]@{ includes = $includes }
$includes = @('**')
$output = $env:BIOME_SKIP_OUTPUT
$pattern = $env:BIOME_SKIP_PATTERN
$sourceConfig = $env:BIOME_SKIP_SOURCE_CONFIG
' "$includes" >"$output"
' \
'/\
POWERSHELL
additional_pattern="$BIOME_SKIP_ADDITIONAL_PATTERN"
discover_config() {
else
fi
fi
fi
fi
fi
if (-not [string]::IsNullOrWhiteSpace($additionalPattern) -and
if (-not [string]::IsNullOrWhiteSpace($pattern)) {
if (-not [string]::IsNullOrWhiteSpace($sourceConfig)) {
if ([string]::IsNullOrWhiteSpace($pattern) -and
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if [[ -n "$additional_pattern" && "$additional_pattern" != "$pattern" ]]; then
if [[ -n "$pattern" ]]; then
if [[ -n "$source_config" ]]; then
if [[ -z "$pattern" && -z "$additional_pattern" ]]; then
if [[ -z "$source_config" ]]; then
includes="\"**\""
json_escape() {
output="$BIOME_SKIP_OUTPUT"
source_config="$BIOME_SKIP_SOURCE_CONFIG"
task: "check:write" finished
task: "check:write" started
task: "config:skip" finished
task: "config:skip" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [check:write] echo "✅ biome fixes applied"
task: [config:skip] pattern="$BIOME_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [pnpm:_exec:unix] bash -c "biome check --write --config-path "BASE-BIOME_INTERNAL_SKIP_CONFIG"  OVR-BIOME_TARGETS  OVR-EXTRA_ARGS "
}
}
}
}
}
}
}
}"

=== task: ci ===










            $sourceConfig = $candidate
            break
        if (Test-Path -LiteralPath $candidate -PathType Leaf) {
        }
      printf '%s' "$candidate"
      return
    "$(json_escape "$source_config")" "$includes" >"$output"
    $additionalPattern -ne $pattern) {
    $config.extends = @($sourceConfig.Replace('\', '/'))
    $includes += '!' + $additionalPattern.Replace('\', '/')
    $includes += '!' + $pattern.Replace('\', '/')
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    [string]::IsNullOrWhiteSpace($additionalPattern)) {
    exit 0
    fi
    foreach ($candidate in @('biome.json', 'biome.jsonc')) {
    if [[ -f "$candidate" ]]; then
    }
  done
  exit 0
  for candidate in "$@"; do
  includes+=",\"!$(json_escape "$additional_pattern")\""
  includes+=",\"!$(json_escape "$pattern")\""
  local candidate
  local value="$1"
  printf '%s' "$value"
  printf '{"extends":["%s"],"files":{"includes":[%s]}}
  printf '{"files":{"includes":[%s]}}
  rm -f "$output"
  source_config="$(discover_config biome.json biome.jsonc || true)"
  value="${value//$'
  value="${value//\"/\\\"}"
  value="${value//\\/\\\\}"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$additionalPattern = $env:BIOME_SKIP_ADDITIONAL_PATTERN
$config = [ordered]@{}
$config | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $output -Encoding UTF8
$config.files = [ordered]@{ includes = $includes }
$includes = @('**')
$output = $env:BIOME_SKIP_OUTPUT
$pattern = $env:BIOME_SKIP_PATTERN
$sourceConfig = $env:BIOME_SKIP_SOURCE_CONFIG
' "$includes" >"$output"
' \
'/\
POWERSHELL
additional_pattern="$BIOME_SKIP_ADDITIONAL_PATTERN"
discover_config() {
else
fi
fi
fi
fi
fi
if (-not [string]::IsNullOrWhiteSpace($additionalPattern) -and
if (-not [string]::IsNullOrWhiteSpace($pattern)) {
if (-not [string]::IsNullOrWhiteSpace($sourceConfig)) {
if ([string]::IsNullOrWhiteSpace($pattern) -and
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if [[ -n "$additional_pattern" && "$additional_pattern" != "$pattern" ]]; then
if [[ -n "$pattern" ]]; then
if [[ -n "$source_config" ]]; then
if [[ -z "$pattern" && -z "$additional_pattern" ]]; then
if [[ -z "$source_config" ]]; then
includes="\"**\""
json_escape() {
output="$BIOME_SKIP_OUTPUT"
source_config="$BIOME_SKIP_SOURCE_CONFIG"
task: "ci" finished
task: "ci" started
task: "config:skip" finished
task: "config:skip" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [ci] echo "✅ biome passed"
task: [config:skip] pattern="$BIOME_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [pnpm:_exec:unix] bash -c "biome ci --config-path "BASE-BIOME_INTERNAL_SKIP_CONFIG"  OVR-BIOME_TARGETS  OVR-EXTRA_ARGS "
}
}
}
}
}
}
}
}"

=== task: config:init ===
task: "config:init" finished
task: "config:init" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [pnpm:_exec:unix] bash -c "biome init OVR-EXTRA_ARGS"
task: status command test -f biome.json || test -f biome.jsonc exited non-zero: exit status 1

=== task: config:skip ===










            $sourceConfig = $candidate
            break
        if (Test-Path -LiteralPath $candidate -PathType Leaf) {
        }
      printf '%s' "$candidate"
      return
    "$(json_escape "$source_config")" "$includes" >"$output"
    $additionalPattern -ne $pattern) {
    $config.extends = @($sourceConfig.Replace('\', '/'))
    $includes += '!' + $additionalPattern.Replace('\', '/')
    $includes += '!' + $pattern.Replace('\', '/')
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    [string]::IsNullOrWhiteSpace($additionalPattern)) {
    exit 0
    fi
    foreach ($candidate in @('biome.json', 'biome.jsonc')) {
    if [[ -f "$candidate" ]]; then
    }
  done
  exit 0
  for candidate in "$@"; do
  includes+=",\"!$(json_escape "$additional_pattern")\""
  includes+=",\"!$(json_escape "$pattern")\""
  local candidate
  local value="$1"
  printf '%s' "$value"
  printf '{"extends":["%s"],"files":{"includes":[%s]}}
  printf '{"files":{"includes":[%s]}}
  rm -f "$output"
  source_config="$(discover_config biome.json biome.jsonc || true)"
  value="${value//$'
  value="${value//\"/\\\"}"
  value="${value//\\/\\\\}"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$additionalPattern = $env:BIOME_SKIP_ADDITIONAL_PATTERN
$config = [ordered]@{}
$config | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $output -Encoding UTF8
$config.files = [ordered]@{ includes = $includes }
$includes = @('**')
$output = $env:BIOME_SKIP_OUTPUT
$pattern = $env:BIOME_SKIP_PATTERN
$sourceConfig = $env:BIOME_SKIP_SOURCE_CONFIG
' "$includes" >"$output"
' \
'/\
POWERSHELL
additional_pattern="$BIOME_SKIP_ADDITIONAL_PATTERN"
discover_config() {
else
fi
fi
fi
fi
fi
if (-not [string]::IsNullOrWhiteSpace($additionalPattern) -and
if (-not [string]::IsNullOrWhiteSpace($pattern)) {
if (-not [string]::IsNullOrWhiteSpace($sourceConfig)) {
if ([string]::IsNullOrWhiteSpace($pattern) -and
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if [[ -n "$additional_pattern" && "$additional_pattern" != "$pattern" ]]; then
if [[ -n "$pattern" ]]; then
if [[ -n "$source_config" ]]; then
if [[ -z "$pattern" && -z "$additional_pattern" ]]; then
if [[ -z "$source_config" ]]; then
includes="\"**\""
json_escape() {
output="$BIOME_SKIP_OUTPUT"
source_config="$BIOME_SKIP_SOURCE_CONFIG"
task: "config:skip" finished
task: "config:skip" started
task: [config:skip] pattern="$BIOME_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
}
}
}
}
}
}
}
}"

=== task: fix ===










            $sourceConfig = $candidate
            break
        if (Test-Path -LiteralPath $candidate -PathType Leaf) {
        }
      printf '%s' "$candidate"
      return
    "$(json_escape "$source_config")" "$includes" >"$output"
    $additionalPattern -ne $pattern) {
    $config.extends = @($sourceConfig.Replace('\', '/'))
    $includes += '!' + $additionalPattern.Replace('\', '/')
    $includes += '!' + $pattern.Replace('\', '/')
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    [string]::IsNullOrWhiteSpace($additionalPattern)) {
    exit 0
    fi
    foreach ($candidate in @('biome.json', 'biome.jsonc')) {
    if [[ -f "$candidate" ]]; then
    }
  done
  exit 0
  for candidate in "$@"; do
  includes+=",\"!$(json_escape "$additional_pattern")\""
  includes+=",\"!$(json_escape "$pattern")\""
  local candidate
  local value="$1"
  printf '%s' "$value"
  printf '{"extends":["%s"],"files":{"includes":[%s]}}
  printf '{"files":{"includes":[%s]}}
  rm -f "$output"
  source_config="$(discover_config biome.json biome.jsonc || true)"
  value="${value//$'
  value="${value//\"/\\\"}"
  value="${value//\\/\\\\}"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$additionalPattern = $env:BIOME_SKIP_ADDITIONAL_PATTERN
$config = [ordered]@{}
$config | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $output -Encoding UTF8
$config.files = [ordered]@{ includes = $includes }
$includes = @('**')
$output = $env:BIOME_SKIP_OUTPUT
$pattern = $env:BIOME_SKIP_PATTERN
$sourceConfig = $env:BIOME_SKIP_SOURCE_CONFIG
' "$includes" >"$output"
' \
'/\
POWERSHELL
additional_pattern="$BIOME_SKIP_ADDITIONAL_PATTERN"
discover_config() {
else
fi
fi
fi
fi
fi
if (-not [string]::IsNullOrWhiteSpace($additionalPattern) -and
if (-not [string]::IsNullOrWhiteSpace($pattern)) {
if (-not [string]::IsNullOrWhiteSpace($sourceConfig)) {
if ([string]::IsNullOrWhiteSpace($pattern) -and
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if [[ -n "$additional_pattern" && "$additional_pattern" != "$pattern" ]]; then
if [[ -n "$pattern" ]]; then
if [[ -n "$source_config" ]]; then
if [[ -z "$pattern" && -z "$additional_pattern" ]]; then
if [[ -z "$source_config" ]]; then
includes="\"**\""
json_escape() {
output="$BIOME_SKIP_OUTPUT"
source_config="$BIOME_SKIP_SOURCE_CONFIG"
task: "check:write" finished
task: "check:write" started
task: "config:skip" finished
task: "config:skip" started
task: "fix" finished
task: "fix" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [check:write] echo "✅ biome fixes applied"
task: [config:skip] pattern="$BIOME_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [pnpm:_exec:unix] bash -c "biome check --write --config-path "BASE-BIOME_INTERNAL_SKIP_CONFIG"  OVR-BIOME_TARGETS  OVR-EXTRA_ARGS "
}
}
}
}
}
}
}
}"

=== task: fmt ===










            $sourceConfig = $candidate
            break
        if (Test-Path -LiteralPath $candidate -PathType Leaf) {
        }
      printf '%s' "$candidate"
      return
    "$(json_escape "$source_config")" "$includes" >"$output"
    $additionalPattern -ne $pattern) {
    $config.extends = @($sourceConfig.Replace('\', '/'))
    $includes += '!' + $additionalPattern.Replace('\', '/')
    $includes += '!' + $pattern.Replace('\', '/')
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    [string]::IsNullOrWhiteSpace($additionalPattern)) {
    exit 0
    fi
    foreach ($candidate in @('biome.json', 'biome.jsonc')) {
    if [[ -f "$candidate" ]]; then
    }
  done
  exit 0
  for candidate in "$@"; do
  includes+=",\"!$(json_escape "$additional_pattern")\""
  includes+=",\"!$(json_escape "$pattern")\""
  local candidate
  local value="$1"
  printf '%s' "$value"
  printf '{"extends":["%s"],"files":{"includes":[%s]}}
  printf '{"files":{"includes":[%s]}}
  rm -f "$output"
  source_config="$(discover_config biome.json biome.jsonc || true)"
  value="${value//$'
  value="${value//\"/\\\"}"
  value="${value//\\/\\\\}"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$additionalPattern = $env:BIOME_SKIP_ADDITIONAL_PATTERN
$config = [ordered]@{}
$config | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $output -Encoding UTF8
$config.files = [ordered]@{ includes = $includes }
$includes = @('**')
$output = $env:BIOME_SKIP_OUTPUT
$pattern = $env:BIOME_SKIP_PATTERN
$sourceConfig = $env:BIOME_SKIP_SOURCE_CONFIG
' "$includes" >"$output"
' \
'/\
POWERSHELL
additional_pattern="$BIOME_SKIP_ADDITIONAL_PATTERN"
discover_config() {
else
fi
fi
fi
fi
fi
if (-not [string]::IsNullOrWhiteSpace($additionalPattern) -and
if (-not [string]::IsNullOrWhiteSpace($pattern)) {
if (-not [string]::IsNullOrWhiteSpace($sourceConfig)) {
if ([string]::IsNullOrWhiteSpace($pattern) -and
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if [[ -n "$additional_pattern" && "$additional_pattern" != "$pattern" ]]; then
if [[ -n "$pattern" ]]; then
if [[ -n "$source_config" ]]; then
if [[ -z "$pattern" && -z "$additional_pattern" ]]; then
if [[ -z "$source_config" ]]; then
includes="\"**\""
json_escape() {
output="$BIOME_SKIP_OUTPUT"
source_config="$BIOME_SKIP_SOURCE_CONFIG"
task: "config:skip" finished
task: "config:skip" started
task: "fmt" finished
task: "fmt" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [config:skip] pattern="$BIOME_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [fmt] echo "✅ biome fixes applied"
task: [pnpm:_exec:unix] bash -c "biome format --write --config-path "BASE-BIOME_INTERNAL_SKIP_CONFIG"  OVR-BIOME_TARGETS  OVR-EXTRA_ARGS "
}
}
}
}
}
}
}
}"

=== task: fmt:check ===










            $sourceConfig = $candidate
            break
        if (Test-Path -LiteralPath $candidate -PathType Leaf) {
        }
      printf '%s' "$candidate"
      return
    "$(json_escape "$source_config")" "$includes" >"$output"
    $additionalPattern -ne $pattern) {
    $config.extends = @($sourceConfig.Replace('\', '/'))
    $includes += '!' + $additionalPattern.Replace('\', '/')
    $includes += '!' + $pattern.Replace('\', '/')
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    [string]::IsNullOrWhiteSpace($additionalPattern)) {
    exit 0
    fi
    foreach ($candidate in @('biome.json', 'biome.jsonc')) {
    if [[ -f "$candidate" ]]; then
    }
  done
  exit 0
  for candidate in "$@"; do
  includes+=",\"!$(json_escape "$additional_pattern")\""
  includes+=",\"!$(json_escape "$pattern")\""
  local candidate
  local value="$1"
  printf '%s' "$value"
  printf '{"extends":["%s"],"files":{"includes":[%s]}}
  printf '{"files":{"includes":[%s]}}
  rm -f "$output"
  source_config="$(discover_config biome.json biome.jsonc || true)"
  value="${value//$'
  value="${value//\"/\\\"}"
  value="${value//\\/\\\\}"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$additionalPattern = $env:BIOME_SKIP_ADDITIONAL_PATTERN
$config = [ordered]@{}
$config | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $output -Encoding UTF8
$config.files = [ordered]@{ includes = $includes }
$includes = @('**')
$output = $env:BIOME_SKIP_OUTPUT
$pattern = $env:BIOME_SKIP_PATTERN
$sourceConfig = $env:BIOME_SKIP_SOURCE_CONFIG
' "$includes" >"$output"
' \
'/\
POWERSHELL
additional_pattern="$BIOME_SKIP_ADDITIONAL_PATTERN"
discover_config() {
else
fi
fi
fi
fi
fi
if (-not [string]::IsNullOrWhiteSpace($additionalPattern) -and
if (-not [string]::IsNullOrWhiteSpace($pattern)) {
if (-not [string]::IsNullOrWhiteSpace($sourceConfig)) {
if ([string]::IsNullOrWhiteSpace($pattern) -and
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if [[ -n "$additional_pattern" && "$additional_pattern" != "$pattern" ]]; then
if [[ -n "$pattern" ]]; then
if [[ -n "$source_config" ]]; then
if [[ -z "$pattern" && -z "$additional_pattern" ]]; then
if [[ -z "$source_config" ]]; then
includes="\"**\""
json_escape() {
output="$BIOME_SKIP_OUTPUT"
source_config="$BIOME_SKIP_SOURCE_CONFIG"
task: "config:skip" finished
task: "config:skip" started
task: "fmt:check" finished
task: "fmt:check" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [config:skip] pattern="$BIOME_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [pnpm:_exec:unix] bash -c "biome format --config-path "BASE-BIOME_INTERNAL_SKIP_CONFIG"  OVR-BIOME_TARGETS  OVR-EXTRA_ARGS "
}
}
}
}
}
}
}
}"

=== task: help ===
task: "help" finished
task: "help" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [pnpm:_exec:unix] bash -c "biome --help OVR-EXTRA_ARGS"

=== task: init ===
task: "config:init" finished
task: "config:init" started
task: "init" finished
task: "init" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [pnpm:_exec:unix] bash -c "biome init OVR-EXTRA_ARGS"
task: status command test -f biome.json || test -f biome.jsonc exited non-zero: exit status 1

=== task: install ===
task: "install" started
task: Task "install" is up to date
task: status command node -e "const installed = require('./node_modules/@biomejs/biome/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install:undo ===
task: "install:undo" finished
task: "install:undo" started
task: "pnpm:_pnpm:unix" finished
task: "pnpm:_pnpm:unix" started
task: "pnpm:_pnpm:windows" not for current platform - ignored
task: "pnpm:remove" finished
task: "pnpm:remove" started
task: [pnpm:_pnpm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack pnpm remove -g @biomejs/biome OVR-EXTRA_ARGS'

=== task: lint ===










            $sourceConfig = $candidate
            break
        if (Test-Path -LiteralPath $candidate -PathType Leaf) {
        }
      printf '%s' "$candidate"
      return
    "$(json_escape "$source_config")" "$includes" >"$output"
    $additionalPattern -ne $pattern) {
    $config.extends = @($sourceConfig.Replace('\', '/'))
    $includes += '!' + $additionalPattern.Replace('\', '/')
    $includes += '!' + $pattern.Replace('\', '/')
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    [string]::IsNullOrWhiteSpace($additionalPattern)) {
    exit 0
    fi
    foreach ($candidate in @('biome.json', 'biome.jsonc')) {
    if [[ -f "$candidate" ]]; then
    }
  done
  exit 0
  for candidate in "$@"; do
  includes+=",\"!$(json_escape "$additional_pattern")\""
  includes+=",\"!$(json_escape "$pattern")\""
  local candidate
  local value="$1"
  printf '%s' "$value"
  printf '{"extends":["%s"],"files":{"includes":[%s]}}
  printf '{"files":{"includes":[%s]}}
  rm -f "$output"
  source_config="$(discover_config biome.json biome.jsonc || true)"
  value="${value//$'
  value="${value//\"/\\\"}"
  value="${value//\\/\\\\}"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$additionalPattern = $env:BIOME_SKIP_ADDITIONAL_PATTERN
$config = [ordered]@{}
$config | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $output -Encoding UTF8
$config.files = [ordered]@{ includes = $includes }
$includes = @('**')
$output = $env:BIOME_SKIP_OUTPUT
$pattern = $env:BIOME_SKIP_PATTERN
$sourceConfig = $env:BIOME_SKIP_SOURCE_CONFIG
' "$includes" >"$output"
' \
'/\
POWERSHELL
additional_pattern="$BIOME_SKIP_ADDITIONAL_PATTERN"
discover_config() {
else
fi
fi
fi
fi
fi
if (-not [string]::IsNullOrWhiteSpace($additionalPattern) -and
if (-not [string]::IsNullOrWhiteSpace($pattern)) {
if (-not [string]::IsNullOrWhiteSpace($sourceConfig)) {
if ([string]::IsNullOrWhiteSpace($pattern) -and
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if [[ -n "$additional_pattern" && "$additional_pattern" != "$pattern" ]]; then
if [[ -n "$pattern" ]]; then
if [[ -n "$source_config" ]]; then
if [[ -z "$pattern" && -z "$additional_pattern" ]]; then
if [[ -z "$source_config" ]]; then
includes="\"**\""
json_escape() {
output="$BIOME_SKIP_OUTPUT"
source_config="$BIOME_SKIP_SOURCE_CONFIG"
task: "config:skip" finished
task: "config:skip" started
task: "lint" finished
task: "lint" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [config:skip] pattern="$BIOME_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [lint] echo "✅ biome passed"
task: [pnpm:_exec:unix] bash -c "biome lint --config-path "BASE-BIOME_INTERNAL_SKIP_CONFIG"  OVR-BIOME_TARGETS  OVR-EXTRA_ARGS "
}
}
}
}
}
}
}
}"

=== task: lint:fix ===










            $sourceConfig = $candidate
            break
        if (Test-Path -LiteralPath $candidate -PathType Leaf) {
        }
      printf '%s' "$candidate"
      return
    "$(json_escape "$source_config")" "$includes" >"$output"
    $additionalPattern -ne $pattern) {
    $config.extends = @($sourceConfig.Replace('\', '/'))
    $includes += '!' + $additionalPattern.Replace('\', '/')
    $includes += '!' + $pattern.Replace('\', '/')
    Remove-Item -LiteralPath $output -Force -ErrorAction SilentlyContinue
    [string]::IsNullOrWhiteSpace($additionalPattern)) {
    exit 0
    fi
    foreach ($candidate in @('biome.json', 'biome.jsonc')) {
    if [[ -f "$candidate" ]]; then
    }
  done
  exit 0
  for candidate in "$@"; do
  includes+=",\"!$(json_escape "$additional_pattern")\""
  includes+=",\"!$(json_escape "$pattern")\""
  local candidate
  local value="$1"
  printf '%s' "$value"
  printf '{"extends":["%s"],"files":{"includes":[%s]}}
  printf '{"files":{"includes":[%s]}}
  rm -f "$output"
  source_config="$(discover_config biome.json biome.jsonc || true)"
  value="${value//$'
  value="${value//\"/\\\"}"
  value="${value//\\/\\\\}"
 not for current platform - ignored
# Nothing to skip: drop the overlay a previous run left behind so the
# next task falls back to the project config instead of a stale one.
$ErrorActionPreference = 'Stop'
$additionalPattern = $env:BIOME_SKIP_ADDITIONAL_PATTERN
$config = [ordered]@{}
$config | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $output -Encoding UTF8
$config.files = [ordered]@{ includes = $includes }
$includes = @('**')
$output = $env:BIOME_SKIP_OUTPUT
$pattern = $env:BIOME_SKIP_PATTERN
$sourceConfig = $env:BIOME_SKIP_SOURCE_CONFIG
' "$includes" >"$output"
' \
'/\
POWERSHELL
additional_pattern="$BIOME_SKIP_ADDITIONAL_PATTERN"
discover_config() {
else
fi
fi
fi
fi
fi
if (-not [string]::IsNullOrWhiteSpace($additionalPattern) -and
if (-not [string]::IsNullOrWhiteSpace($pattern)) {
if (-not [string]::IsNullOrWhiteSpace($sourceConfig)) {
if ([string]::IsNullOrWhiteSpace($pattern) -and
if ([string]::IsNullOrWhiteSpace($sourceConfig)) {
if [[ -n "$additional_pattern" && "$additional_pattern" != "$pattern" ]]; then
if [[ -n "$pattern" ]]; then
if [[ -n "$source_config" ]]; then
if [[ -z "$pattern" && -z "$additional_pattern" ]]; then
if [[ -z "$source_config" ]]; then
includes="\"**\""
json_escape() {
output="$BIOME_SKIP_OUTPUT"
source_config="$BIOME_SKIP_SOURCE_CONFIG"
task: "config:skip" finished
task: "config:skip" started
task: "lint:fix" finished
task: "lint:fix" started
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: [config:skip] pattern="$BIOME_SKIP_PATTERN"
task: [config:skip] powershell -NoProfile -ExecutionPolicy Bypass -Command - <<'POWERSHELL'
task: [lint:fix] echo "✅ biome fixes applied"
task: [pnpm:_exec:unix] bash -c "biome lint --write --config-path "BASE-BIOME_INTERNAL_SKIP_CONFIG"  OVR-BIOME_TARGETS  OVR-EXTRA_ARGS "
}
}
}
}
}
}
}
}"

=== task: upgrade ===
task: "pnpm:_pnpm:unix" finished
task: "pnpm:_pnpm:unix" started
task: "pnpm:_pnpm:windows" not for current platform - ignored
task: "pnpm:add" finished
task: "pnpm:add" started
task: "upgrade" finished
task: "upgrade" started
task: [pnpm:_pnpm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack pnpm add -g @biomejs/biome@latest OVR-EXTRA_ARGS'

=== task: version ===
task: "pnpm:_exec:unix" finished
task: "pnpm:_exec:unix" started
task: "pnpm:_exec:windows" not for current platform - ignored
task: "pnpm:exec" finished
task: "pnpm:exec" started
task: "version" finished
task: "version" started
task: [pnpm:_exec:unix] bash -c "biome --version "
