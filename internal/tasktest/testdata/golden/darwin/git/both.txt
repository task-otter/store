# module: git
# case:   both
# vars:
#   BASE=BASE-BASE
#   BODY=BASE-BODY
#   BRANCH=BASE-BRANCH
#   CLONE_DIR=BASE-CLONE_DIR
#   COMMIT=BASE-COMMIT
#   COMMIT_MSG=BASE-COMMIT_MSG
#   EMAIL=BASE-EMAIL
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   FILES=BASE-FILES
#   MERGE_METHOD=BASE-MERGE_METHOD
#   MESSAGE=BASE-MESSAGE
#   NAME=BASE-NAME
#   NOTES=BASE-NOTES
#   OWNER=BASE-OWNER
#   REMOTE=BASE-REMOTE
#   REPO=BASE-REPO
#   STASH_INDEX=BASE-STASH_INDEX
#   TAG=BASE-TAG
#   TITLE=BASE-TITLE
#   URL=BASE-URL
#   VERSION=BASE-VERSION
#   BASE_OVERRIDE=OVR-BASE
#   BODY_OVERRIDE=OVR-BODY
#   BRANCH_OVERRIDE=OVR-BRANCH
#   CLONE_DIR_OVERRIDE=OVR-CLONE_DIR
#   COMMIT_MSG_OVERRIDE=OVR-COMMIT_MSG
#   COMMIT_OVERRIDE=OVR-COMMIT
#   EMAIL_OVERRIDE=OVR-EMAIL
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   FILES_OVERRIDE=OVR-FILES
#   MERGE_METHOD_OVERRIDE=OVR-MERGE_METHOD
#   MESSAGE_OVERRIDE=OVR-MESSAGE
#   NAME_OVERRIDE=OVR-NAME
#   NOTES_OVERRIDE=OVR-NOTES
#   OWNER_OVERRIDE=OVR-OWNER
#   REMOTE_OVERRIDE=OVR-REMOTE
#   REPO_OVERRIDE=OVR-REPO
#   STASH_INDEX_OVERRIDE=OVR-STASH_INDEX
#   TAG_OVERRIDE=OVR-TAG
#   TITLE_OVERRIDE=OVR-TITLE
#   URL_OVERRIDE=OVR-URL
#   VERSION_OVERRIDE=OVR-VERSION

=== task: add ===
task: "add" finished
task: "add" started
task: [add] git add BASE-FILES BASE-EXTRA_ARGS

=== task: add:all ===
task: "add:all" finished
task: "add:all" started
task: [add:all] git add --all

=== task: auth:setup ===

      | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null
      | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
    _key="signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg"
    brew install "gh${VERSION:+@$VERSION}"
    brew install "gh${VERSION:+@$VERSION}"
    echo "deb [arch=$(dpkg --print-architecture) $_key] https://cli.github.com/packages stable main" \
    echo 'Arch Linux detected. Installing gh via pacman...'
    echo 'CentOS/RHEL detected. Installing gh via yum...'
    echo 'Debian/Ubuntu detected. Installing gh via apt...'
    echo 'ERROR: Homebrew is required to install gh on macOS.'
    echo 'ERROR: No supported package manager detected.'
    echo 'Fedora/RHEL detected. Installing gh via dnf...'
    echo 'Homebrew detected. Installing gh via brew...'
    echo 'Install Homebrew from https://brew.sh and then re-run: task install'
    echo 'See https://cli.github.com/manual/installation'
    echo 'macOS detected. Installing gh via Homebrew...'
    echo 'openSUSE detected. Installing gh via zypper...'
    echo 'snap detected. Installing gh via snap...'
    exit 1
    exit 1
    sudo apt-get install -y "gh${VERSION:+=$VERSION}"
    sudo apt-get install -y wget
    sudo apt-get update
    sudo apt-get update
    sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg
    sudo dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo dnf install -y "gh${VERSION:+-$VERSION}" --repo gh-cli
    sudo dnf install -y 'dnf-command(config-manager)'
    sudo mkdir -p -m 755 /etc/apt/keyrings
    sudo pacman -S --noconfirm github-cli
    sudo snap install gh
    sudo yum install -y "gh${VERSION:+-$VERSION}"
    sudo yum-config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper addrepo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper install -y "gh${VERSION:+-$VERSION}"
    sudo zypper ref
    wget -qO- https://cli.github.com/packages/githubcli-archive-keyring.gpg \
  echo "ERROR: Unsupported OS: $OS"
  echo 'See https://cli.github.com/manual/installation'
  elif command -v brew >/dev/null 2>&1; then
  elif command -v dnf >/dev/null 2>&1; then
  elif command -v pacman >/dev/null 2>&1; then
  elif command -v snap >/dev/null 2>&1; then
  elif command -v yum >/dev/null 2>&1; then
  elif command -v zypper >/dev/null 2>&1; then
  else
  else
  exit 1
  fi
  fi
  if command -v apt-get >/dev/null 2>&1; then
  if command -v brew >/dev/null 2>&1; then
OS="$(uname -s)"
elif [ "$OS" = "Linux" ]; then
else
fi
if [ "$OS" = "Darwin" ]; then
task: "auth:setup" finished
task: "auth:setup" started
task: "gh:_install:auto:unix" finished
task: "gh:_install:auto:unix" started
task: "gh:_install:auto:windows" not for current platform - ignored
task: "gh:auth:setup-git" finished
task: "gh:auth:setup-git" started
task: "gh:install" finished
task: "gh:install" started
task: [gh:_install:auto:unix] VERSION="BASE-VERSION"
task: [gh:auth:setup-git] gh auth setup-git
task: status command command -v gh >/dev/null 2>&1 && { [ -z "BASE-VERSION" ] || gh --version | grep -Fqx "gh version BASE-VERSION"; } exited non-zero: exit status 1

=== task: branch:create ===
task: "branch:create" finished
task: "branch:create" started
task: [branch:create] git switch -c BASE-BRANCH BASE-EXTRA_ARGS

=== task: branch:delete ===
task: "branch:delete" finished
task: "branch:delete" started
task: [branch:delete] git branch -d BASE-BRANCH BASE-EXTRA_ARGS

=== task: branch:list ===
task: "branch:list" finished
task: "branch:list" started
task: [branch:list] git branch -a BASE-EXTRA_ARGS

=== task: branch:rename ===
task: "branch:rename" finished
task: "branch:rename" started
task: [branch:rename] git branch -m BASE-BRANCH

=== task: branch:switch ===
task: "branch:switch" finished
task: "branch:switch" started
task: [branch:switch] git switch BASE-BRANCH BASE-EXTRA_ARGS

=== task: clean ===
task: "clean" finished
task: "clean" started
task: [clean] git clean -fd BASE-EXTRA_ARGS

=== task: clone ===

      | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null
      | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
    _key="signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg"
    brew install "gh${VERSION:+@$VERSION}"
    brew install "gh${VERSION:+@$VERSION}"
    echo "deb [arch=$(dpkg --print-architecture) $_key] https://cli.github.com/packages stable main" \
    echo 'Arch Linux detected. Installing gh via pacman...'
    echo 'CentOS/RHEL detected. Installing gh via yum...'
    echo 'Debian/Ubuntu detected. Installing gh via apt...'
    echo 'ERROR: Homebrew is required to install gh on macOS.'
    echo 'ERROR: No supported package manager detected.'
    echo 'Fedora/RHEL detected. Installing gh via dnf...'
    echo 'Homebrew detected. Installing gh via brew...'
    echo 'Install Homebrew from https://brew.sh and then re-run: task install'
    echo 'See https://cli.github.com/manual/installation'
    echo 'macOS detected. Installing gh via Homebrew...'
    echo 'openSUSE detected. Installing gh via zypper...'
    echo 'snap detected. Installing gh via snap...'
    exit 1
    exit 1
    sudo apt-get install -y "gh${VERSION:+=$VERSION}"
    sudo apt-get install -y wget
    sudo apt-get update
    sudo apt-get update
    sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg
    sudo dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo dnf install -y "gh${VERSION:+-$VERSION}" --repo gh-cli
    sudo dnf install -y 'dnf-command(config-manager)'
    sudo mkdir -p -m 755 /etc/apt/keyrings
    sudo pacman -S --noconfirm github-cli
    sudo snap install gh
    sudo yum install -y "gh${VERSION:+-$VERSION}"
    sudo yum-config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper addrepo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper install -y "gh${VERSION:+-$VERSION}"
    sudo zypper ref
    wget -qO- https://cli.github.com/packages/githubcli-archive-keyring.gpg \
  echo "ERROR: Unsupported OS: $OS"
  echo 'See https://cli.github.com/manual/installation'
  elif command -v brew >/dev/null 2>&1; then
  elif command -v dnf >/dev/null 2>&1; then
  elif command -v pacman >/dev/null 2>&1; then
  elif command -v snap >/dev/null 2>&1; then
  elif command -v yum >/dev/null 2>&1; then
  elif command -v zypper >/dev/null 2>&1; then
  else
  else
  exit 1
  fi
  fi
  if command -v apt-get >/dev/null 2>&1; then
  if command -v brew >/dev/null 2>&1; then
OS="$(uname -s)"
elif [ "$OS" = "Linux" ]; then
else
fi
if [ "$OS" = "Darwin" ]; then
task: "clone" finished
task: "clone" started
task: "gh:_install:auto:unix" finished
task: "gh:_install:auto:unix" started
task: "gh:_install:auto:windows" not for current platform - ignored
task: "gh:install" finished
task: "gh:install" started
task: [clone] gh repo clone BASE-OWNER/BASE-REPO BASE-CLONE_DIR BASE-EXTRA_ARGS
task: [gh:_install:auto:unix] VERSION="BASE-VERSION"
task: status command command -v gh >/dev/null 2>&1 && { [ -z "BASE-VERSION" ] || gh --version | grep -Fqx "gh version BASE-VERSION"; } exited non-zero: exit status 1

=== task: commit ===
task: "commit" finished
task: "commit" started
task: [commit] git commit -m "BASE-COMMIT_MSG" BASE-EXTRA_ARGS

=== task: commit:amend ===
task: "commit:amend" finished
task: "commit:amend" started
task: [commit:amend] git commit --amend -m "BASE-COMMIT_MSG" BASE-EXTRA_ARGS

=== task: config:list ===
task: "config:list" finished
task: "config:list" started
task: [config:list] git config --list BASE-EXTRA_ARGS

=== task: config:user ===
task: "config:user" finished
task: "config:user" started
task: [config:user] git config --global user.email "BASE-EMAIL"
task: [config:user] git config --global user.name "BASE-NAME"

=== task: diff ===
task: "diff" finished
task: "diff" started
task: [diff] git diff BASE-FILES BASE-EXTRA_ARGS

=== task: diff:staged ===
task: "diff:staged" finished
task: "diff:staged" started
task: [diff:staged] git diff --staged BASE-EXTRA_ARGS

=== task: fetch ===
task: "fetch" finished
task: "fetch" started
task: [fetch] git fetch BASE-REMOTE --prune BASE-EXTRA_ARGS

=== task: help ===
task: "help" finished
task: "help" started
task: [help] git help

=== task: init ===
task: "init" finished
task: "init" started
task: [init] git init --initial-branch=BASE-BRANCH BASE-EXTRA_ARGS

=== task: install ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" finished
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: [_install:macos] brew install "git@BASE-VERSION"
task: [_install:macos] echo 'Installing git via Homebrew...'
task: [_install:macos] echo 'git installed.'
task: status command command -v git >/dev/null 2>&1 && { [ -z "BASE-VERSION" ] || git --version | grep -Fqx "git version BASE-VERSION"; } exited non-zero: exit status 1

=== task: install:undo ===
task: "_install:undo:linux" not for current platform - ignored
task: "_install:undo:macos" finished
task: "_install:undo:macos" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:macos] brew uninstall git
task: [_install:undo:macos] echo 'Removing git via Homebrew...'
task: [_install:undo:macos] echo 'git removed.'
task: status command ! command -v git >/dev/null 2>&1 exited non-zero: exit status 1

=== task: log ===
task: "log" finished
task: "log" started
task: [log] git log --format="%C(yellow)%h%Creset %C(blue)%ad%Creset %s %C(green)<%an>%Creset" --date=short BASE-EXTRA_ARGS

=== task: log:graph ===
task: "log:graph" finished
task: "log:graph" started
task: [log:graph] git log --oneline --graph --decorate --all BASE-EXTRA_ARGS

=== task: pr:create ===

      | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null
      | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
    _key="signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg"
    brew install "gh${VERSION:+@$VERSION}"
    brew install "gh${VERSION:+@$VERSION}"
    echo "deb [arch=$(dpkg --print-architecture) $_key] https://cli.github.com/packages stable main" \
    echo 'Arch Linux detected. Installing gh via pacman...'
    echo 'CentOS/RHEL detected. Installing gh via yum...'
    echo 'Debian/Ubuntu detected. Installing gh via apt...'
    echo 'ERROR: Homebrew is required to install gh on macOS.'
    echo 'ERROR: No supported package manager detected.'
    echo 'Fedora/RHEL detected. Installing gh via dnf...'
    echo 'Homebrew detected. Installing gh via brew...'
    echo 'Install Homebrew from https://brew.sh and then re-run: task install'
    echo 'See https://cli.github.com/manual/installation'
    echo 'macOS detected. Installing gh via Homebrew...'
    echo 'openSUSE detected. Installing gh via zypper...'
    echo 'snap detected. Installing gh via snap...'
    exit 1
    exit 1
    sudo apt-get install -y "gh${VERSION:+=$VERSION}"
    sudo apt-get install -y wget
    sudo apt-get update
    sudo apt-get update
    sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg
    sudo dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo dnf install -y "gh${VERSION:+-$VERSION}" --repo gh-cli
    sudo dnf install -y 'dnf-command(config-manager)'
    sudo mkdir -p -m 755 /etc/apt/keyrings
    sudo pacman -S --noconfirm github-cli
    sudo snap install gh
    sudo yum install -y "gh${VERSION:+-$VERSION}"
    sudo yum-config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper addrepo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper install -y "gh${VERSION:+-$VERSION}"
    sudo zypper ref
    wget -qO- https://cli.github.com/packages/githubcli-archive-keyring.gpg \
  echo "ERROR: Unsupported OS: $OS"
  echo 'See https://cli.github.com/manual/installation'
  elif command -v brew >/dev/null 2>&1; then
  elif command -v dnf >/dev/null 2>&1; then
  elif command -v pacman >/dev/null 2>&1; then
  elif command -v snap >/dev/null 2>&1; then
  elif command -v yum >/dev/null 2>&1; then
  elif command -v zypper >/dev/null 2>&1; then
  else
  else
  exit 1
  fi
  fi
  if command -v apt-get >/dev/null 2>&1; then
  if command -v brew >/dev/null 2>&1; then
OS="$(uname -s)"
elif [ "$OS" = "Linux" ]; then
else
fi
if [ "$OS" = "Darwin" ]; then
task: "gh:_install:auto:unix" finished
task: "gh:_install:auto:unix" started
task: "gh:_install:auto:windows" not for current platform - ignored
task: "gh:install" finished
task: "gh:install" started
task: "pr:create" finished
task: "pr:create" started
task: [gh:_install:auto:unix] VERSION="BASE-VERSION"
task: [pr:create] gh pr create --title "BASE-TITLE" --base "BASE-BASE" --body "BASE-BODY"
task: [pr:create] git push BASE-REMOTE BASE-BRANCH BASE-EXTRA_ARGS
task: status command command -v gh >/dev/null 2>&1 && { [ -z "BASE-VERSION" ] || gh --version | grep -Fqx "gh version BASE-VERSION"; } exited non-zero: exit status 1

=== task: pr:open ===

      | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null
      | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
    _key="signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg"
    brew install "gh${VERSION:+@$VERSION}"
    brew install "gh${VERSION:+@$VERSION}"
    echo "deb [arch=$(dpkg --print-architecture) $_key] https://cli.github.com/packages stable main" \
    echo 'Arch Linux detected. Installing gh via pacman...'
    echo 'CentOS/RHEL detected. Installing gh via yum...'
    echo 'Debian/Ubuntu detected. Installing gh via apt...'
    echo 'ERROR: Homebrew is required to install gh on macOS.'
    echo 'ERROR: No supported package manager detected.'
    echo 'Fedora/RHEL detected. Installing gh via dnf...'
    echo 'Homebrew detected. Installing gh via brew...'
    echo 'Install Homebrew from https://brew.sh and then re-run: task install'
    echo 'See https://cli.github.com/manual/installation'
    echo 'macOS detected. Installing gh via Homebrew...'
    echo 'openSUSE detected. Installing gh via zypper...'
    echo 'snap detected. Installing gh via snap...'
    exit 1
    exit 1
    sudo apt-get install -y "gh${VERSION:+=$VERSION}"
    sudo apt-get install -y wget
    sudo apt-get update
    sudo apt-get update
    sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg
    sudo dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo dnf install -y "gh${VERSION:+-$VERSION}" --repo gh-cli
    sudo dnf install -y 'dnf-command(config-manager)'
    sudo mkdir -p -m 755 /etc/apt/keyrings
    sudo pacman -S --noconfirm github-cli
    sudo snap install gh
    sudo yum install -y "gh${VERSION:+-$VERSION}"
    sudo yum-config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper addrepo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper install -y "gh${VERSION:+-$VERSION}"
    sudo zypper ref
    wget -qO- https://cli.github.com/packages/githubcli-archive-keyring.gpg \
  echo "ERROR: Unsupported OS: $OS"
  echo 'See https://cli.github.com/manual/installation'
  elif command -v brew >/dev/null 2>&1; then
  elif command -v dnf >/dev/null 2>&1; then
  elif command -v pacman >/dev/null 2>&1; then
  elif command -v snap >/dev/null 2>&1; then
  elif command -v yum >/dev/null 2>&1; then
  elif command -v zypper >/dev/null 2>&1; then
  else
  else
  exit 1
  fi
  fi
  if command -v apt-get >/dev/null 2>&1; then
  if command -v brew >/dev/null 2>&1; then
OS="$(uname -s)"
elif [ "$OS" = "Linux" ]; then
else
fi
if [ "$OS" = "Darwin" ]; then
task: "gh:_install:auto:unix" finished
task: "gh:_install:auto:unix" started
task: "gh:_install:auto:windows" not for current platform - ignored
task: "gh:install" finished
task: "gh:install" started
task: "pr:open" finished
task: "pr:open" started
task: [gh:_install:auto:unix] VERSION="BASE-VERSION"
task: [pr:open] gh pr view --web
task: status command command -v gh >/dev/null 2>&1 && { [ -z "BASE-VERSION" ] || gh --version | grep -Fqx "gh version BASE-VERSION"; } exited non-zero: exit status 1

=== task: pull ===
task: "pull" finished
task: "pull" started
task: [pull] git pull BASE-REMOTE BASE-BRANCH BASE-EXTRA_ARGS

=== task: push ===
task: "push" finished
task: "push" started
task: [push] git push BASE-REMOTE BASE-BRANCH BASE-EXTRA_ARGS

=== task: push:force ===
task: "push:force" finished
task: "push:force" started
task: [push:force] git push BASE-REMOTE BASE-BRANCH --force-with-lease BASE-EXTRA_ARGS

=== task: release:create ===

      | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null
      | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
    _key="signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg"
    brew install "gh${VERSION:+@$VERSION}"
    brew install "gh${VERSION:+@$VERSION}"
    echo "deb [arch=$(dpkg --print-architecture) $_key] https://cli.github.com/packages stable main" \
    echo 'Arch Linux detected. Installing gh via pacman...'
    echo 'CentOS/RHEL detected. Installing gh via yum...'
    echo 'Debian/Ubuntu detected. Installing gh via apt...'
    echo 'ERROR: Homebrew is required to install gh on macOS.'
    echo 'ERROR: No supported package manager detected.'
    echo 'Fedora/RHEL detected. Installing gh via dnf...'
    echo 'Homebrew detected. Installing gh via brew...'
    echo 'Install Homebrew from https://brew.sh and then re-run: task install'
    echo 'See https://cli.github.com/manual/installation'
    echo 'macOS detected. Installing gh via Homebrew...'
    echo 'openSUSE detected. Installing gh via zypper...'
    echo 'snap detected. Installing gh via snap...'
    exit 1
    exit 1
    sudo apt-get install -y "gh${VERSION:+=$VERSION}"
    sudo apt-get install -y wget
    sudo apt-get update
    sudo apt-get update
    sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg
    sudo dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo dnf install -y "gh${VERSION:+-$VERSION}" --repo gh-cli
    sudo dnf install -y 'dnf-command(config-manager)'
    sudo mkdir -p -m 755 /etc/apt/keyrings
    sudo pacman -S --noconfirm github-cli
    sudo snap install gh
    sudo yum install -y "gh${VERSION:+-$VERSION}"
    sudo yum-config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper addrepo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper install -y "gh${VERSION:+-$VERSION}"
    sudo zypper ref
    wget -qO- https://cli.github.com/packages/githubcli-archive-keyring.gpg \
  echo "ERROR: Unsupported OS: $OS"
  echo 'See https://cli.github.com/manual/installation'
  elif command -v brew >/dev/null 2>&1; then
  elif command -v dnf >/dev/null 2>&1; then
  elif command -v pacman >/dev/null 2>&1; then
  elif command -v snap >/dev/null 2>&1; then
  elif command -v yum >/dev/null 2>&1; then
  elif command -v zypper >/dev/null 2>&1; then
  else
  else
  exit 1
  fi
  fi
  if command -v apt-get >/dev/null 2>&1; then
  if command -v brew >/dev/null 2>&1; then
OS="$(uname -s)"
elif [ "$OS" = "Linux" ]; then
else
fi
if [ "$OS" = "Darwin" ]; then
task: "gh:_install:auto:unix" finished
task: "gh:_install:auto:unix" started
task: "gh:_install:auto:windows" not for current platform - ignored
task: "gh:install" finished
task: "gh:install" started
task: "release:create" finished
task: "release:create" started
task: [gh:_install:auto:unix] VERSION="BASE-VERSION"
task: [release:create] gh release create BASE-TAG --title "BASE-TITLE" --notes "BASE-NOTES"
task: [release:create] git push BASE-REMOTE BASE-TAG
task: [release:create] git tag -a BASE-TAG -m "BASE-TITLE"
task: status command command -v gh >/dev/null 2>&1 && { [ -z "BASE-VERSION" ] || gh --version | grep -Fqx "gh version BASE-VERSION"; } exited non-zero: exit status 1

=== task: remote:add ===
task: "remote:add" finished
task: "remote:add" started
task: [remote:add] git remote add BASE-NAME BASE-URL

=== task: remote:list ===
task: "remote:list" finished
task: "remote:list" started
task: [remote:list] git remote -v

=== task: remote:remove ===
task: "remote:remove" finished
task: "remote:remove" started
task: [remote:remove] git remote remove BASE-NAME

=== task: remote:set-url ===
task: "remote:set-url" finished
task: "remote:set-url" started
task: [remote:set-url] git remote set-url BASE-NAME BASE-URL

=== task: reset:hard ===
task: "reset:hard" finished
task: "reset:hard" started
task: [reset:hard] git reset --hard BASE-COMMIT BASE-EXTRA_ARGS

=== task: reset:soft ===
task: "reset:soft" finished
task: "reset:soft" started
task: [reset:soft] git reset --soft BASE-COMMIT BASE-EXTRA_ARGS

=== task: stash ===
task: "stash" finished
task: "stash" started
task: [stash] git stash push -m "BASE-MESSAGE" BASE-EXTRA_ARGS

=== task: stash:drop ===
--- exit: exit status 1
template: :1: unexpected "{" in command

=== task: stash:list ===
task: "stash:list" finished
task: "stash:list" started
task: [stash:list] git stash list

=== task: stash:pop ===
--- exit: exit status 1
template: :1: unexpected "{" in command

=== task: status ===
task: "status" finished
task: "status" started
task: [status] git status

=== task: sync ===

      | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null
      | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
    _key="signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg"
    brew install "gh${VERSION:+@$VERSION}"
    brew install "gh${VERSION:+@$VERSION}"
    echo "deb [arch=$(dpkg --print-architecture) $_key] https://cli.github.com/packages stable main" \
    echo 'Arch Linux detected. Installing gh via pacman...'
    echo 'CentOS/RHEL detected. Installing gh via yum...'
    echo 'Debian/Ubuntu detected. Installing gh via apt...'
    echo 'ERROR: Homebrew is required to install gh on macOS.'
    echo 'ERROR: No supported package manager detected.'
    echo 'Fedora/RHEL detected. Installing gh via dnf...'
    echo 'Homebrew detected. Installing gh via brew...'
    echo 'Install Homebrew from https://brew.sh and then re-run: task install'
    echo 'See https://cli.github.com/manual/installation'
    echo 'macOS detected. Installing gh via Homebrew...'
    echo 'openSUSE detected. Installing gh via zypper...'
    echo 'snap detected. Installing gh via snap...'
    exit 1
    exit 1
    sudo apt-get install -y "gh${VERSION:+=$VERSION}"
    sudo apt-get install -y wget
    sudo apt-get update
    sudo apt-get update
    sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg
    sudo dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo dnf install -y "gh${VERSION:+-$VERSION}" --repo gh-cli
    sudo dnf install -y 'dnf-command(config-manager)'
    sudo mkdir -p -m 755 /etc/apt/keyrings
    sudo pacman -S --noconfirm github-cli
    sudo snap install gh
    sudo yum install -y "gh${VERSION:+-$VERSION}"
    sudo yum-config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper addrepo https://cli.github.com/packages/rpm/gh-cli.repo
    sudo zypper install -y "gh${VERSION:+-$VERSION}"
    sudo zypper ref
    wget -qO- https://cli.github.com/packages/githubcli-archive-keyring.gpg \
  echo "ERROR: Unsupported OS: $OS"
  echo 'See https://cli.github.com/manual/installation'
  elif command -v brew >/dev/null 2>&1; then
  elif command -v dnf >/dev/null 2>&1; then
  elif command -v pacman >/dev/null 2>&1; then
  elif command -v snap >/dev/null 2>&1; then
  elif command -v yum >/dev/null 2>&1; then
  elif command -v zypper >/dev/null 2>&1; then
  else
  else
  exit 1
  fi
  fi
  if command -v apt-get >/dev/null 2>&1; then
  if command -v brew >/dev/null 2>&1; then
OS="$(uname -s)"
elif [ "$OS" = "Linux" ]; then
else
fi
if [ "$OS" = "Darwin" ]; then
task: "gh:_install:auto:unix" finished
task: "gh:_install:auto:unix" started
task: "gh:_install:auto:windows" not for current platform - ignored
task: "gh:install" finished
task: "gh:install" started
task: "sync" finished
task: "sync" started
task: [gh:_install:auto:unix] VERSION="BASE-VERSION"
task: [sync] gh repo sync --branch BASE-BRANCH BASE-EXTRA_ARGS
task: status command command -v gh >/dev/null 2>&1 && { [ -z "BASE-VERSION" ] || gh --version | grep -Fqx "gh version BASE-VERSION"; } exited non-zero: exit status 1

=== task: tag:create ===
task: "tag:create" finished
task: "tag:create" started
task: [tag:create] git tag -a BASE-TAG -m "BASE-MESSAGE" BASE-EXTRA_ARGS

=== task: tag:delete ===
task: "tag:delete" finished
task: "tag:delete" started
task: [tag:delete] git push BASE-REMOTE --delete BASE-TAG
task: [tag:delete] git tag -d BASE-TAG

=== task: tag:list ===
task: "tag:list" finished
task: "tag:list" started
task: [tag:list] git tag --sort=-v:refname BASE-EXTRA_ARGS

=== task: tag:push ===
task: "tag:push" finished
task: "tag:push" started
task: [tag:push] git push BASE-REMOTE BASE-TAG BASE-EXTRA_ARGS

=== task: upgrade ===
task: "_upgrade:linux" not for current platform - ignored
task: "_upgrade:macos" finished
task: "_upgrade:macos" started
task: "_upgrade:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [_upgrade:macos] brew install git
task: [_upgrade:macos] echo 'Upgrading git via Homebrew...'
task: [_upgrade:macos] echo 'git upgraded.'
task: [_upgrade:macos] if condition not met - skipped

=== task: version ===
task: "version" finished
task: "version" started
task: [version] git --version
