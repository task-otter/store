# module: npm/nvm
# case:   bare

=== task: add ===
--- exit: exit status 206
task: Task "add" cancelled because it is missing required variables: PACKAGES

=== task: audit ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "audit" finished
task: "audit" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm audit'

=== task: audit:fix ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "audit:fix" finished
task: "audit:fix" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm audit fix'

=== task: audit:json ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "audit:json" finished
task: "audit:json" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm audit --json'

=== task: audit:report ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "audit:report" finished
task: "audit:report" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm audit'

=== task: build ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "build" finished
task: "build" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm run build'

=== task: cache:clean ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "cache:clean" finished
task: "cache:clean" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm cache clean --force'

=== task: ci ===
--- exit: exit status 201
task: "_ci:unix" started
task: "ci" started
task: Failed to run task "ci": task: Failed to run task "_ci:unix": task: precondition not met
task: package-lock.json not found. npm ci requires a lockfile.

=== task: clean ===
task: "_clean:unix" started
task: "_clean:windows" not for current platform - ignored
task: "clean" finished
task: "clean" started
task: Task "_clean:unix" is up to date
task: status command test ! -d node_modules exited zero

=== task: clean:all ===
task: "_clean:all:unix" finished
task: "_clean:all:unix" started
task: "_clean:all:windows" not for current platform - ignored
task: "clean:all" finished
task: "clean:all" started
task: [_clean:all:unix] echo Done.
task: [_clean:all:unix] echo Removing node_modules and package-lock.json...
task: [_clean:all:unix] rm -f package-lock.json
task: [_clean:all:unix] rm -rf node_modules

=== task: dev ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "dev" finished
task: "dev" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm run dev'

=== task: doctor ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "doctor" finished
task: "doctor" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm doctor'

=== task: exec ===
--- exit: exit status 206
task: Task "exec" cancelled because it is missing required variables: BINARY

=== task: format ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "format" finished
task: "format" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm run format'

=== task: install ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm install'

=== task: install:undo ===
task: "install:undo" finished
task: "install:undo" started
task: [install:undo] echo 'Remove Node.js instead: task nvm:install:undo.'
task: [install:undo] echo 'npm ships bundled with Node.js and cannot be removed independently.'

=== task: lint ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "lint" finished
task: "lint" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm run lint'

=== task: manager:pin ===
--- exit: exit status 206
task: Task "manager:pin" cancelled because it is missing required variables: PACKAGE_MANAGER_VERSION

=== task: manager:setup ===
task: "corepack:_corepack:unix" finished
task: "corepack:_corepack:unix" started
task: "corepack:_corepack:windows" not for current platform - ignored
task: "corepack:_install:unix" finished
task: "corepack:_install:unix" started
task: "corepack:_install:windows" not for current platform - ignored
task: "corepack:enable" finished
task: "corepack:enable" started
task: "corepack:install" finished
task: "corepack:install" started
task: "corepack:setup" finished
task: "corepack:setup" started
task: "manager:setup" finished
task: "manager:setup" started
task: [corepack:_corepack:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run the install task for this Corepack Taskfile."; exit 1; }; corepack enable'
task: [corepack:_install:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; npm install --global "corepack@0.34.0"'
task: status command bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 && corepack --version | grep -Fqx "0.34.0"' exited non-zero: exit status 1
task: status command test -s "$HOME/.nvm/nvm.sh" exited zero

=== task: node:setup ===

  task nvm:node:install
  task nvm:node:install VERSION="$NODE_VERSION"
else
fi
task: "_node:setup:unix" finished
task: "_node:setup:unix" started
task: "_node:setup:windows" not for current platform - ignored
task: "node:setup" finished
task: "node:setup" started
task: [_node:setup:unix] if [ -n "$NODE_VERSION" ]; then

=== task: outdated ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "outdated" finished
task: "outdated" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm outdated'

=== task: outdated:strict ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "outdated:strict" finished
task: "outdated:strict" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm outdated'

=== task: remove ===
--- exit: exit status 206
task: Task "remove" cancelled because it is missing required variables: PACKAGES

=== task: run ===
--- exit: exit status 206
task: Task "run" cancelled because it is missing required variables: SCRIPT

=== task: test ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "test" finished
task: "test" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm test'

=== task: typecheck ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "typecheck" finished
task: "typecheck" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm run typecheck'

=== task: update ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "update" finished
task: "update" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm update'

=== task: upgrade ===
task: "_npm:unix" finished
task: "_npm:unix" started
task: "_npm:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [_npm:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm install -g npm@latest'

=== task: version ===
task: "_version:unix" finished
task: "_version:unix" started
task: "_version:windows" not for current platform - ignored
task: "version" finished
task: "version" started
task: [_version:unix] bash -c 'export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; if [ -f .node-version ] || [ -f .nvmrc ]; then nvm use; fi; node --version; npm --version'
