# module: bruno/node/fnm/npm
# case:   override
# vars:
#   COLLECTION_OVERRIDE=OVR-COLLECTION
#   ENV_OVERRIDE=OVR-ENV
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   VERSION_OVERRIDE=OVR-VERSION

=== task: ci ===
task: "_run" finished
task: "_run" started
task: "ci" finished
task: "ci" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- bru run OVR-COLLECTION --env "OVR-ENV" --bail OVR-EXTRA_ARGS'

=== task: help ===
task: "help" finished
task: "help" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- bru --help OVR-EXTRA_ARGS'

=== task: install ===
task: "install" started
task: Task "install" is up to date
task: status command node -e "const installed = require('./node_modules/@usebruno/cli/package.json'); const requested = 'OVR-VERSION'; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install:undo ===
task: "install:undo" finished
task: "install:undo" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:remove" finished
task: "npm:remove" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm uninstall -g @usebruno/cli OVR-EXTRA_ARGS'

=== task: run ===
task: "_run" finished
task: "_run" started
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: "run" finished
task: "run" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- bru run OVR-COLLECTION --env "OVR-ENV" OVR-EXTRA_ARGS'

=== task: upgrade ===
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:add" finished
task: "npm:add" started
task: "upgrade" finished
task: "upgrade" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm install -g @usebruno/cli@latest OVR-EXTRA_ARGS'

=== task: version ===
task: "npm:_npm:unix" finished
task: "npm:_npm:unix" started
task: "npm:_npm:windows" not for current platform - ignored
task: "npm:exec" finished
task: "npm:exec" started
task: "version" finished
task: "version" started
task: [npm:_npm:unix] bash -c 'export PATH="$HOME/.local/share/fnm:$HOME/.local/bin:$PATH"; if [ -f .node-version ] || [ -f .nvmrc ]; then fnm use; fi; command -v corepack >/dev/null 2>&1 || { echo "corepack is not available. Run: task manager:setup"; exit 1; }; corepack npm exec -- bru --version '
