# module: djlint
# case:   both
# vars:
#   DJLINT_FMT_SKIP_PATTERN=BASE-DJLINT_FMT_SKIP_PATTERN
#   DJLINT_LINT_SKIP_PATTERN=BASE-DJLINT_LINT_SKIP_PATTERN
#   DJLINT_VERSION=BASE-DJLINT_VERSION
#   EXTRA_ARGS=BASE-EXTRA_ARGS
#   TARGETS=BASE-TARGETS
#   UV_LOAD=BASE-UV_LOAD
#   DJLINT_FMT_SKIP_PATTERN_OVERRIDE=OVR-DJLINT_FMT_SKIP_PATTERN
#   DJLINT_LINT_SKIP_PATTERN_OVERRIDE=OVR-DJLINT_LINT_SKIP_PATTERN
#   DJLINT_VERSION_OVERRIDE=OVR-DJLINT_VERSION
#   EXTRA_ARGS_OVERRIDE=OVR-EXTRA_ARGS
#   TARGETS_OVERRIDE=OVR-TARGETS
#   UV_LOAD_OVERRIDE=OVR-UV_LOAD

=== task: fmt ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_run" finished
task: "_run" started
task: "fmt" finished
task: "fmt" started
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "djlint==BASE-DJLINT_VERSION"'
task: [_install:unix] echo 'Installing djLint via uv...'
task: [_install:unix] echo 'djLint installed.'
task: [_run] bash -lc 'BASE-UV_LOAD; djlint --extend-exclude "OVR-DJLINT_FMT_SKIP_PATTERN" OVR-TARGETS --reformat OVR-EXTRA_ARGS'
task: [_run] djlint OVR-TARGETS --reformat OVR-EXTRA_ARGS not for current platform - ignored
task: status command bash -lc 'BASE-UV_LOAD; command -v djlint >/dev/null 2>&1 && { [ -z "BASE-DJLINT_VERSION" ] || djlint --version | grep -Fq "BASE-DJLINT_VERSION"; }' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: fmt:check ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_run" finished
task: "_run" started
task: "fmt:check" finished
task: "fmt:check" started
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "djlint==BASE-DJLINT_VERSION"'
task: [_install:unix] echo 'Installing djLint via uv...'
task: [_install:unix] echo 'djLint installed.'
task: [_run] bash -lc 'BASE-UV_LOAD; djlint --extend-exclude "OVR-DJLINT_FMT_SKIP_PATTERN" OVR-TARGETS --check OVR-EXTRA_ARGS && echo "✅ djlint formatting check passed" || { echo "❌ djlint formatting check failed"; exit 1; }'
task: [_run] djlint OVR-TARGETS --check OVR-EXTRA_ARGS not for current platform - ignored
task: status command bash -lc 'BASE-UV_LOAD; command -v djlint >/dev/null 2>&1 && { [ -z "BASE-DJLINT_VERSION" ] || djlint --version | grep -Fq "BASE-DJLINT_VERSION"; }' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: install ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "djlint==BASE-DJLINT_VERSION"'
task: [_install:unix] echo 'Installing djLint via uv...'
task: [_install:unix] echo 'djLint installed.'
task: status command bash -lc 'BASE-UV_LOAD; command -v djlint >/dev/null 2>&1 && { [ -z "BASE-DJLINT_VERSION" ] || djlint --version | grep -Fq "BASE-DJLINT_VERSION"; }' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: install:undo ===
task: "_install:undo:unix" finished
task: "_install:undo:unix" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:unix] bash -lc 'BASE-UV_LOAD; uv tool uninstall djlint'
task: [_install:undo:unix] echo 'Removing djLint...'
task: [_install:undo:unix] echo 'djLint removed.'
task: status command bash -lc 'BASE-UV_LOAD; ! command -v djlint >/dev/null 2>&1' exited non-zero: exit status 1

=== task: lint ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "_run" finished
task: "_run" started
task: "install" finished
task: "install" started
task: "lint" finished
task: "lint" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "djlint==BASE-DJLINT_VERSION"'
task: [_install:unix] echo 'Installing djLint via uv...'
task: [_install:unix] echo 'djLint installed.'
task: [_run] bash -lc 'BASE-UV_LOAD; djlint --extend-exclude "OVR-DJLINT_LINT_SKIP_PATTERN" OVR-TARGETS --lint OVR-EXTRA_ARGS && echo "✅ djlint passed" || { echo "❌ djlint failed"; exit 1; }'
task: [_run] djlint OVR-TARGETS --lint OVR-EXTRA_ARGS not for current platform - ignored
task: status command bash -lc 'BASE-UV_LOAD; command -v djlint >/dev/null 2>&1 && { [ -z "BASE-DJLINT_VERSION" ] || djlint --version | grep -Fq "BASE-DJLINT_VERSION"; }' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero

=== task: upgrade ===
task: "_upgrade:unix" finished
task: "_upgrade:unix" started
task: "_upgrade:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [_upgrade:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "djlint==BASE-DJLINT_VERSION"'
task: [_upgrade:unix] echo 'Upgrading djLint via uv...'
task: [_upgrade:unix] echo 'djLint upgraded.'

=== task: version ===
task: "_install:unix" finished
task: "_install:unix" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "uv:_install:unix" started
task: "uv:_install:windows" not for current platform - ignored
task: "uv:install" finished
task: "uv:install" started
task: "version" finished
task: "version" started
task: Task "uv:_install:unix" is up to date
task: [_install:unix] bash -lc 'BASE-UV_LOAD; uv tool install --force "djlint==BASE-DJLINT_VERSION"'
task: [_install:unix] echo 'Installing djLint via uv...'
task: [_install:unix] echo 'djLint installed.'
task: [version] bash -lc 'BASE-UV_LOAD; djlint --version'
task: [version] djlint --version not for current platform - ignored
task: status command bash -lc 'BASE-UV_LOAD; command -v djlint >/dev/null 2>&1 && { [ -z "BASE-DJLINT_VERSION" ] || djlint --version | grep -Fq "BASE-DJLINT_VERSION"; }' exited non-zero: exit status 1
task: status command uv_bin="$HOME/.local/bin/uv"; if [ ! -x "$uv_bin" ]; then uv_bin="$(command -v uv 2>/dev/null)"; fi; [ -n "$uv_bin" ] && [ -x "$uv_bin" ] && { [ -z "" ] || "$uv_bin" --version | grep -Fqx "uv "; } exited zero
