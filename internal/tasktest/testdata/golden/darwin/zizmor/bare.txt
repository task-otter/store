# module: zizmor
# case:   bare

=== task: install ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: Task "_install:macos" is up to date
task: dynamic variable: "uname -m" result: "arm64"
task: status command command -v zizmor >/dev/null 2>&1 && zizmor --version | grep -Fq "1.25.2" exited zero

=== task: install:undo ===
task: "_install:undo:unix" finished
task: "_install:undo:unix" started
task: "_install:undo:windows" not for current platform - ignored
task: "install:undo" finished
task: "install:undo" started
task: [_install:undo:unix] echo 'Removing zizmor...'
task: [_install:undo:unix] echo 'zizmor removed.'
task: [_install:undo:unix] if condition not met - skipped
task: [_install:undo:unix] sudo rm -f /usr/local/bin/zizmor
task: status command ! command -v zizmor >/dev/null 2>&1 exited non-zero: exit status 1

=== task: lint ===

      Where-Object { $_.Extension -in @('.yml', '.yaml') }
    && echo "✅ zizmor audit passed" || { echo "❌ zizmor audit found issues"; exit 1; }
    --offline
    --offline \
    .github \
    .github;
    Get-ChildItem -LiteralPath $_ -Recurse -File |
    filter ('SKIPFILES_PATTERN=' + $skipPattern));
    find "$target" -type f \( -name '*.yml' -o -name '*.yaml' \) -print0
    printf '%s\000' "$target"
   <"$file_list" \
   @paths;
  && echo "✅ zizmor audit passed" || { echo "❌ zizmor audit found issues"; exit 1; }
  --offline
  --offline \
  echo "✅ zizmor audit passed (no matching workflows found)"
  elif [ -f "$target" ]; then
  env -u GH_TOKEN zizmor \
  exit
  exit $LASTEXITCODE
  exit 0
  fi
  filter "SKIPFILES_PATTERN=$skip_pattern" >"$file_list"
  if (Test-Path -LiteralPath $_ -PathType Container) {
  if [ -d "$target" ]; then
  task --silent --taskfile '$REPO/taskfiles/zizmor/../internal/skipfiles/Taskfile.yml'
  zizmor 
  } elseif (Test-Path -LiteralPath $_ -PathType Leaf) { Get-Item -LiteralPath $_ }
done | task --silent --taskfile "$REPO/taskfiles/zizmor/../internal/skipfiles/Taskfile.yml" \
fi
fi
file_list="$(mktemp)"
for target in .github; do
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Write-Host '✅ zizmor audit passed'" not for current platform - ignored
if (-not $paths) { Write-Host 'No matching workflows found'; exit 0 }; zizmor
if [ ! -s "$file_list" ]; then
if [ -z "$skip_pattern" ]; then
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "lint" finished
task: "lint" started
task: Task "_install:macos" is up to date
task: [lint] powershell -NoProfile -ExecutionPolicy Bypass -Command "Remove-Item Env:GH_TOKEN -ErrorAction SilentlyContinue; $skipPattern=''; if (-not $skipPattern) {
task: [lint] skip_pattern=''
task: dynamic variable: "uname -m" result: "arm64"
task: status command command -v zizmor >/dev/null 2>&1 && zizmor --version | grep -Fq "1.25.2" exited zero
trap 'rm -f "$file_list"' EXIT
xargs -0 env -u GH_TOKEN zizmor \
}); $paths=@($files | ForEach-Object FullName |
}; $targets='.github'; $files=@($targets.Split(' ') | ForEach-Object {

=== task: upgrade ===
  aarch64-apple-darwin.tar.gz"
task: "_upgrade:unix" finished
task: "_upgrade:unix" started
task: "_upgrade:windows" not for current platform - ignored
task: "upgrade" finished
task: "upgrade" started
task: [_upgrade:unix] curl -fsSL -o "/tmp/taskotter-zizmor-1.25.2.tar.gz" "https://github.com/woodruffw/zizmor/releases/download/v1.25.2/zizmor-
task: [_upgrade:unix] echo 'Upgrading zizmor to v1.25.2...'
task: [_upgrade:unix] echo 'zizmor upgraded.'
task: [_upgrade:unix] if condition not met - skipped
task: [_upgrade:unix] mkdir -p "/tmp/taskotter-zizmor-1.25.2"
task: [_upgrade:unix] rm -rf "/tmp/taskotter-zizmor-1.25.2.tar.gz" "/tmp/taskotter-zizmor-1.25.2"
task: [_upgrade:unix] sudo install -m 0755 "$(find "/tmp/taskotter-zizmor-1.25.2" -type f -name zizmor -print -quit)" /usr/local/bin/zizmor
task: [_upgrade:unix] tar -xzf "/tmp/taskotter-zizmor-1.25.2.tar.gz" -C "/tmp/taskotter-zizmor-1.25.2"
task: dynamic variable: "uname -m" result: "arm64"
task: dynamic variable: "uname -s" result: "Darwin"

=== task: version ===
task: "_install:linux" not for current platform - ignored
task: "_install:macos" started
task: "_install:windows" not for current platform - ignored
task: "install" finished
task: "install" started
task: "version" finished
task: "version" started
task: Task "_install:macos" is up to date
task: [version] zizmor --version
task: dynamic variable: "uname -m" result: "arm64"
task: status command command -v zizmor >/dev/null 2>&1 && zizmor --version | grep -Fq "1.25.2" exited zero
