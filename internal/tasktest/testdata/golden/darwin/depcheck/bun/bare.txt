# module: depcheck/bun
# case:   bare

=== task: ci ===
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "ci" finished
task: "ci" started
task: "lint" finished
task: "lint" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x depcheck . '
task: [ci] echo "✅ depcheck ci passed"
task: [lint] echo "✅ depcheck passed"
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: help ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "help" finished
task: "help" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x depcheck --help '
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: ignores ===
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "ignores" finished
task: "ignores" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x depcheck . --ignores="" '
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: install ===
task: "install" started
task: Task "install" is up to date
task: status command node -e "const installed = require('./node_modules/depcheck/package.json'); const requested = ''; process.exit(!requested || installed.version === requested ? 0 : 1)" exited zero

=== task: install:undo ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:install" finished
task: "bun:install" started
task: "bun:remove" finished
task: "bun:remove" started
task: "install:undo" finished
task: "install:undo" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun remove -g depcheck '
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: json ===
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "json" finished
task: "json" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x depcheck . --json '
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: lint ===
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "lint" finished
task: "lint" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x depcheck . '
task: [lint] echo "✅ depcheck passed"
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: skip-missing ===
task: "_run" finished
task: "_run" started
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "skip-missing" finished
task: "skip-missing" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x depcheck . --skip-missing=true '
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: upgrade ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:add" finished
task: "bun:add" started
task: "bun:install" finished
task: "bun:install" started
task: "upgrade" finished
task: "upgrade" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun add -g depcheck@latest '
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero

=== task: version ===
task: "bun:_bun:unix" finished
task: "bun:_bun:unix" started
task: "bun:_bun:windows" not for current platform - ignored
task: "bun:_install:unix" started
task: "bun:_install:windows" not for current platform - ignored
task: "bun:exec" finished
task: "bun:exec" started
task: "bun:install" finished
task: "bun:install" started
task: "version" finished
task: "version" started
task: Task "bun:_install:unix" is up to date
task: [bun:_bun:unix] bash -lc 'export BUN_INSTALL="$HOME/.bun"; export PATH="$BUN_INSTALL/bin:$PATH"; bun x depcheck --version '
task: status command bun_bin="$HOME/.bun/bin/bun"; if [ ! -x "$bun_bin" ]; then bun_bin="$(command -v bun 2>/dev/null)"; fi; [ -n "$bun_bin" ] && [ -x "$bun_bin" ] && { [ -z "" ] || [ "$("$bun_bin" --version)" = "" ]; } exited zero
